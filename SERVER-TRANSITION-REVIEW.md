# 길드원 따라가기 서버·웹 전환 검토

검토일은 2026-09-05이며, 현재 v0.3.13 로컬 앱의 구조와 공식 공개 문서를 기준으로 한다. 다음 구조는 제안이며, 서버 공개·넥슨 로그인 신청·광고 계약·유료 결제는 실행하지 않았다.

## 판단

다수 사용자에게 지속적인 성장 기록을 제공하고 광고·멤버십을 운영하려면 중앙 수집 서버와 웹 중심 구조가 적합하다. 같은 캐릭터는 한 번 수집하고 여러 사용자에게 결과를 전달하므로 중복 API 호출을 줄이며, 휴대폰이 꺼져 있어도 서버에 기록이 남는다. 개인용·오프라인 사용에는 현재 로컬 앱의 장점이 남는다.

다만 운영 서버를 만들 수 있는 것, 넥슨 로그인을 사용할 수 있는 것, 광고·유료 기능을 제공할 수 있는 것은 각각 별도의 판단이다. 후자의 두 항목은 현재 키 발급만으로 확보된 권한이 아니다.

## 넥슨 로그인과 대표캐릭터 자동 연결

NEXON Open ID는 실제로 존재한다. 공식 소개에는 지정된 프렌즈 제작자에게 제공한다고 명시되어 있고, 도입 공지는 메이플스토리 파트너스 제작자를 대상으로 설명한다. 서비스 단계 API 키를 가지고 있다는 이유만으로 바로 로그인 버튼을 구현할 수 있다고 전제해서는 안 된다. [기능 소개](https://openapi.nexon.com/ko/data-util/introduction/), [도입 공지](https://openapi.nexon.com/support/notice/2726294/).

운영자의 API 키로 공개 캐릭터·길드 정보를 조회하는 권한과, 방문자가 실제 그 캐릭터를 소유한다는 것을 확인하는 권한은 다르다. 운영자 키만으로 방문자 계정의 대표캐릭터가 자동 식별되지는 않는다. 승인 후 제공되는 로그인·동의 범위와 계정 캐릭터 조회 명세를 확인해야 한다. 공개 문서만으로 ‘대표캐릭터’ 필드의 제공까지 확인하지는 못했다.

초기 웹 버전은 별도 서비스 계정 또는 비로그인 닉네임 선택으로 공개 기록을 보여줄 수 있다. 이때 닉네임 등록을 소유 인증으로 취급하지 않는다. 넥슨 연동 승인 후에는 승인된 계정 연결 경로를 추가하고, 대표 설정은 공식 제공 여부에 따라 사용자 확인을 거친다. 넥슨 비밀번호를 자체 서버에 입력받는 방식은 사용하지 않는다.

## API 제공량과 추가 키

공식 안내의 서비스 단계 한도는 초당 500건·일일 20,000,000건이며 허용량은 애플리케이션별 합산이다. 동일 애플리케이션의 두 번째 키는 교체·유출 대응에 사용할 수 있지만 호출량을 두 배로 만들지 않는다. 동일 서비스용 애플리케이션을 여러 개 등록해 호출량을 분산할 수 있는지는 별도로 확인해야 하며, 승인 없는 키 순환을 용량 설계의 전제로 두지 않는다. [넥슨 사전 준비 안내](https://openapi.nexon.com/ko/guide/prepare-in-advance/).

대략적인 일일 요청량은 아래처럼 계산할 수 있다. 이는 캐릭터 기본 정보 한 엔드포인트를 조회하는 산술 추정이며 응답 크기나 서버 처리 성능 측정이 아니다.

`일일 요청 ≈ 중복 제거한 캐릭터 수 × (1440 ÷ 조회 간격(분) + 일일 기록 1회) + 길드·식별자 조회 + 재시도 + 신규 과거 기록 보충`

| 고유 캐릭터 수 | 5분 간격 + 일일 기록 | 15분 간격 + 일일 기록 |
| --- | ---: | ---: |
| 10,000 | 2,890,000건/일 | 970,000건/일 |
| 50,000 | 14,450,000건/일 | 4,850,000건/일 |
| 100,000 | 28,900,000건/일 | 9,700,000건/일 |

최초 31개 스냅샷은 신규 고유 캐릭터 수 × 31건이 추가되므로 신규 사용자의 가입 순간에 무제한으로 실행하지 않는다. 요청을 시간에 분산하고, 일일 예산에 재시도·가입용 여유를 남긴다. 현재 페이지를 여러 명이 보고 있다고 수집 주기를 각자 추가하지 않는다.

평균 15분의 원천 데이터 지연이 있어 잦은 조회가 게임의 즉시 실시간성을 보장하지 않는다. 자정 경계는 이번 로컬 수정과 같은 관측 시각 기반 추정·이후 보정 방식을 서버에서도 공유한다. [메이플스토리 API 안내](https://openapi.nexon.com/game/maplestory/?id=15).

## 제안 구조

```text
NEXON Open API
       ↓ 운영자 API 키로 중복 제거 수집
수집 작업자 ──→ 공통 경험치 계산 ──→ 데이터베이스
                                      ↓
                            사용자별 조회·권한 API
                              ↙             ↘
                         PC/모바일 웹     Android 앱·홈 위젯
```

서버의 수집 일정은 방문자의 브라우저 실행과 분리한다. 웹 접속이나 새로고침은 먼저 캐시된 결과를 읽고, 오래된 경우에만 중복 제거된 갱신 요청을 등록한다. API 키는 서버에서만 사용하며 웹 JavaScript·APK·공개 응답에 넣지 않는다. 결과 제공용 내부 API는 이 서비스의 화면·권한에 맞게 제한하며 원본 넥슨 API를 범용 중계하는 사업으로 확장하지 않는다.

초기 구현은 하나의 코드베이스에 수집 작업자와 웹 API 실행 모드를 두는 정도로 충분하다. 대규모 분산 시스템을 처음부터 구성할 필요는 없다. 현재 Rust 경험치 계산과 검증을 독립 모듈로 옮기고 React 화면은 데이터 통신 부분을 교체해 재사용할 수 있다.

다중 사용자 전환에는 DB 변경이 필수다. 현재 캐릭터 테이블의 대표·즐겨찾기 전역 플래그와 단일 길드 설정을 그대로 서버에 올리면 사용자의 설정이 서로 섞인다.

- 공통 영역은 캐릭터 식별 이력, 길드 식별자, 길드·날짜별 소속, 원본 관측, 일간 증가량으로 둔다.
- 사용자별 영역은 계정, 로그인 연결, 대표캐릭터, 즐겨찾기와 정렬, 화면 설정, 멤버십 권한으로 둔다.
- 계정·기기 세션과 구독 권한은 서버가 판단한다. 브라우저나 APK의 ‘유료 사용자’ 값만 신뢰하지 않는다.
- 원본 관측 시각·응답 시각·계산 버전을 보존하고, 새 계산은 이전 파생값과 비교한 뒤 전환한다.
- 새 설치는 과거 30일부터 시작하되 기존 로컬 기록을 서버로 가져올지는 별도 선택으로 둔다. 가져오기를 허용하더라도 변조 가능한 업로드 값을 공용 랭킹의 원본으로 즉시 채택하지 않는다.

## 호스팅 장비 선택

| 선택 | 유리한 점 | 확인할 점 |
| --- | --- | --- |
| 현재 최신 PC | 개발·부하 테스트가 쉽고 여유 자원이 클 가능성이 있다. | 사용 중 재부팅·절전·게임 부하가 서비스 중단으로 이어질 수 있다. |
| 구형 사무용 PC를 전용 헤드리스 서버로 사용 | 일상 작업과 분리해 계속 켜둘 수 있다. | CPU보다 SSD 상태, 메모리, 지원되는 OS, 인터넷 업로드, 전력 사용량부터 측정한다. |
| 외부 서버 또는 혼합 운영 | 집의 전원·회선과 분리하고 유료 서비스 운영 경계를 정하기 쉽다. | 월 비용, 백업·장애 대응과 데이터 이전 경로가 필요하다. |

베타는 전용 사무용 PC가 지원되는 OS와 안정적인 SSD를 갖췄다면 후보가 된다. 정확한 사양을 확인하지 않았으므로 동시 접속자 수나 수집 캐릭터 수를 보장하지 않는다. 최소 72시간 동안 수집 지연, 메모리·DB 증가량, CPU, 실제 전력과 응답 지연을 측정한 뒤 선택한다.

외부 접근은 HTTPS 역방향 프록시 또는 터널로 웹 서비스만 공개하고 DB·원격 관리 포트를 직접 공개하지 않는다. 구형 OS를 그대로 인터넷에 노출하는 방식은 피한다. 유동 IP·공유기·CGNAT 여부도 확인해야 한다. 개인 PC의 프로젝트 폴더 전체를 서비스 루트로 공개하지 않는다.

공개 유료 서비스로 전환할 때는 백업 복구 시험과 집 회선·정전 시 대응이 선행되어야 한다. 최신 PC 한 대가 구형 전용 PC보다 항상 운영에 유리한 것은 아니다. 서버 전환은 운영자의 장애 책임이 늘어나는 선택이다.

## 모바일은 수집 경로를 바꾸되 홈 위젯은 유지

Android 앱은 넥슨 전체 길드를 직접 수집하는 대신 서버의 사용자별 요약·랭킹을 받아 로컬 캐시에 저장하도록 바꿀 수 있다. 기존 Kotlin 홈 위젯과 이미지 처리, 오프라인 마지막 기록 표시를 재사용한다. 앱 업데이트마다 집계 알고리즘을 배포할 필요가 줄고, 다른 기기에서도 같은 계정의 즐겨찾기를 유지하기 쉽다.

서버가 계속 수집한다는 것과 휴대폰 홈 위젯이 즉시 다시 그려진다는 것은 별개다. Android 백그라운드 실행 제약은 남으며, 서버 데이터가 새로워도 위젯 화면 반영은 기기 실행 시점에 따라 지연될 수 있다. [Android 주기 작업 명세](https://developer.android.com/reference/androidx/work/PeriodicWorkRequest).

사용자가 폐기한 휴대폰 알림을 다시 도입하지 않는다. 위젯 디자인과 대기 모션도 이번 전환 검토에서는 보류한다.

## 광고·유료 멤버십

공개 약관은 대가를 받는 프로그램에 별도 안내·정책 준수를 요구하며, 회사 승낙 없는 데이터·저작물의 영리 이용을 제한한다. 따라서 광고도 무조건 허용된다고 단정하지 않는다. 광고 게재, 광고 제거 구독, 분석·즐겨찾기 확장 등 유료 기능을 구체적으로 적어 넥슨에 허용 범위를 확인해야 한다. 로고·캐릭터 이미지의 상업적 사용과 결과 재제공 범위도 함께 확인한다. [넥슨 Open API 약관 제5조·제6조](https://openapi.nexon.com/ko/support/terms/).

메이플 API 페이지에는 크롤링한 데이터의 30일 이내 갱신 의무가 안내되어 있다. 누적 역사 기록·원본 관측의 장기 보존에 적용되는 범위는 명확한 답변을 받아 정책으로 정해야 한다. 이 문구만으로 기존 사용자의 데이터를 임의 삭제하지 않는다. [API 페이지 고지](https://openapi.nexon.com/game/maplestory/).

승인 이후 멤버십 후보로는 광고 제거, 추가 비교 슬롯, 개인화 분석·내보내기 등이 있다. 장기 기록 제공은 보존 조건 확인 후에만 후보로 확정한다. 원천 데이터 지연을 무시한 ‘완전 실시간’이나 게임 공식 인증으로 오해할 표현을 판매 문구로 쓰지 않는다. 무료·유료 기능과 가격은 아직 결정하지 않았다.

정기결제는 별도 결제 모듈로 분리한다. PG 결제창으로 결제수단 등록을 처리하고, 서버에 구독 상태·결제 이력·청구 주기를 둔다. 웹훅의 진위·중복·순서 역전 처리, 승인 결과 재조회, 중복 청구 방지, 해지·환불·결제 실패 후 권한 처리가 필요하다. API 조회 실패를 결제 실패로 취급하거나 앱 화면 상태만으로 구독을 갱신하지 않는다. 예를 들어 토스페이먼츠는 자동결제에 추가 심사·계약이 필요하고 정기 호출은 서비스에서 구현하도록 안내한다. [공식 빌링 안내](https://docs.tosspayments.com/guides/v2/billing).

향후 Google Play에 배포하는 앱의 디지털 멤버십에는 Play 결제 정책과 지역별 대체 결제 프로그램 조건도 적용된다. APK 직접 배포와 Play 배포의 조건을 구분해야 한다. 앱에 웹 결제 링크만 넣으면 모든 조건을 피할 수 있다고 전제하지 않는다. [Google Play 결제 정책](https://support.google.com/googleplay/android-developer/answer/9858738?hl=ko).

수익성은 방문자 수만으로 판단하지 않는다. 월 광고 수익 + 유료 회원 수 × 실수령 구독료에서 호스팅·전력·결제 수수료·환불·유지보수 비용을 빼서 검토한다. 광고 단가와 전환율을 측정하기 전에는 예상 수익을 확정하지 않는다.

## 권장 단계와 승인 전 준비할 것

1. 이번 로컬 계산 수정과 동일한 입력·결과 기준을 서버 엔진의 회귀 테스트로 사용한다.
2. 넥슨 로그인 연동 자격, 광고·멤버십, 장기 보존·재제공 조건에 대한 답변을 확보한다.
3. 별도 테스트 DB로 중앙 수집과 닉네임 선택형 웹 베타를 만든다. 기존 로컬 앱과 데이터는 보존한다.
4. 승인된 넥슨 계정 연결 또는 사용자가 선택한 별도 로그인 방식을 연결한다.
5. Android를 서버 조회형으로 전환하고, 같은 관측 버전에서 웹·앱·위젯이 일치하는지 확인한다.
6. 정전·재부팅·중복 작업·429·누락일·동의 철회·백업 복구 시험을 통과한 뒤 공개 범위를 늘린다.
7. 수익화 승인과 운영 조건 확인 후 결제 모듈을 테스트 환경에서 검증하고 실제 청구를 개시한다.

첫 구현 방향으로는 ‘전용 PC의 중앙 수집 + 닉네임 선택형 웹 베타 + 기존 Android 유지’를 제안한다. 넥슨 로그인 승인 전 사용할 임시 로그인 방식, 전용 PC의 사양·운영 가능 시간, 초기 공개 범위는 사용자 결정이 필요하다. 이 검토서는 해당 결정을 대신하지 않는다.

## 넥슨에 확인할 질문 초안

- 길드원 따라가기는 운영자 키로 공개 캐릭터와 길드를 중복 제거 수집하고 사용자별 웹·Android 화면에 랭킹·일간 성장 기록을 제공합니다. 이 제공 방식과 장기 기록 보존·30일 갱신 의무의 적용 범위를 확인하고 싶습니다.
- NEXON Open ID 사용 자격과 신청 절차, 승인 후 소유 캐릭터 목록·대표캐릭터 조회 지원 범위를 알려주시기 바랍니다.
- 웹 광고, 광고 제거 구독, 추가 비교 슬롯·분석 기능의 유료 정기결제를 각각 허용하는지와 별도 승낙 절차를 확인하고 싶습니다.
- 서비스 규모가 커질 경우 한도 상향 또는 동일 서비스의 여러 애플리케이션 운영이 가능한지, 공식 신청 절차가 있는지 확인하고 싶습니다.

문의는 자동 전송하지 않았다. 이 내용과 공개할 서비스 정보를 검토한 후 발송할 수 있다.
