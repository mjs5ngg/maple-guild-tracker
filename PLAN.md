# 메이플 길드 경험치 트래커 구현 계획

## 개인 조회 공개 출처 연결

- 기존 Cloudflare 계정의 Pages 무료 정적 호스팅에 개인 조회 빌드만 배포한다. 서버 키·DB·운영 화면은 포함하지 않는다.
- 개인 출처에는 광고나 분석 스크립트를 넣지 않고 CSP로 NEXON 직접 통신만 허용한다. ngrok 공용 주소와 상호 출처를 빌드에 고정한다.
- 공개 파일과 응답 헤더를 검증한 뒤 공용 서버 주소 및 OAuth 연결을 진행한다. 도메인 구매나 유료 플랜 변경은 하지 않는다.

## 2026-09-10 익명 구독 부하 제한

- 베타 신규 기기 생성은 서비스 전체 시간당 100회, 설정 저장은 사용자별 시간당 10회로 제한한다. 기존 기기 세션 재사용·기록 조회는 제한에서 제외한다.
- DB 원자적 카운터로 동시 요청과 서버 재시작 후에도 제한을 유지한다. 전달된 IP 헤더는 신뢰하지 않는다.
- 실제 PostgreSQL 테스트로 제한 초과·시간 만료·동시 요청·기존 세션 재사용을 검증한다. 대규모 공격 방어 전체가 완료된 것으로 간주하지 않는다.

## 2026-09-10 비로그인 화면 연결

- 기기 세션 생성 후 설정·순위 화면을 열고 로그인은 선택적인 기기 간 동기화로 표시한다.
- 만료된 계정은 자동 익명 전환하지 않고 로그아웃 복구를 제공한다. 설정 저장 후 로그인 구분을 보존한다.
- 초기화 순서와 실패 회귀 테스트 및 웹 빌드를 검증한다. 익명 남용 제한과 공개 출처 연결 전 운영 배포는 보류한다.

## 승인된 ngrok 인증 연결

- 브라우저의 기존 인증 토큰을 출력 없이 일회용 루프백 입력 폼으로 전달하여 root600 환경 파일에 저장한다. 입력 서버는 5분 이내 종료하며 요청 로그와 토큰 응답을 남기지 않는다.
- 서비스3100만 터널 연결 후 공개 상태 API를 확인한다. 운영3103은 공개하지 않는다. 주소/로그인/개인키 미완료 항목은 연결 성공과 구분한다.

## 테스트 배포와 운영 대시보드

- 운영 대시보드는 루프백3103 별도 리스너로 제공하며 ngrok3100 공개 경로와 분리한다. 읽기 전용 DB 상태·최근 수집20회·주소·배포 확인 상태를 표시한다.
- ngrok 실제 연결/외부 응답을 확인하기 전에는 배포 완료로 표시하지 않는다. 인증정보·계정ID·원본 오류로그는 반환하지 않는다. 사용량은 연동 전 미확인으로 표시한다.

## ngrok 무료 주소 배포 준비

- 사용자 요청으로 ngrok 무료 할당 도메인을 우선 사용한다. 유료 전환/도메인 구매는 하지 않는다.
- 공식 Linux 바이너리 설치 후 계정 인증·할당 주소를 확인한다. 자동 시작 구성은 검증하되 공개 전에는 활성화하지 않는다.
- 개인 키 화면은 별도 정적 출처가 필요하다. 동일 ngrok 호스트의 하위 경로로 합쳐 출처 격리를 해제하지 않는다.
- 무료 한도는 계정 화면으로 확인한다. 공식 한도 문서는 월20,000회/1GB, 최신 가격표는 일회성 크레딧을 표시하여 차이가 있다.

## 비로그인 기본 서비스 전환 — 2026-09-09 최신 결정

- 첫 구현 단위는 별도 HttpOnly 기기 쿠키의 익명 구독 API다. 로그인 쿠키와 분리하여 로그인/로그아웃으로 익명 설정이 사라지지 않게 한다. 서버 저장은 자동 수집 구독을 위한 것이며 소셜 계정 연결은 자동 수행하지 않는다.

- 로그인 없이 조회·대표 지정·즐겨찾기·개인 키 새로고침을 제공한다. 로그인은 기기 간 설정 동기화에만 사용한다.
- 기기 로컬 설정과 서버 수집 구독을 분리한다. 익명 구독도 168시간 비활성·중복 대상 합집합 규칙을 적용하며, 임의 계정 생성으로 수집 대상을 무제한 늘리지 못하도록 제한한다.
- 로그인 전후 설정은 묵시적으로 덮어쓰지 않는다. 기존 계정 설정과 로컬 설정이 충돌하면 선택적으로 가져오게 한다.
- 선행 성능 작업으로 화면용 필드만 전송한다. 이후 목록/이력 분리, 버전 기반 조회, 제한 병렬 수집을 적용한다. DB 원본은 유지한다.
- AdFit·직접 후원 배너·기타 광고망의 적합성을 비교한다. 광고 가입/약관 동의/실광고 게재는 별도 단계다.
- 도메인 구매 없이 사용할 고정 HTTPS 주소를 조사한다. 임시 터널을 정식 운영 주소로 간주하지 않으며, 기존 Linux 서버는 유지한다.

## 개인 조회 저장 제한 대응

- 브라우저 저장 차단 시 개인 조회 화면이 중단되지 않도록 저장 접근을 안전하게 처리한다. 저장 실패는 안내하며 키는 입력 상태에서만 사용한다.
- 저장 차단 회귀 테스트·웹 빌드 후 반영한다. 실제 키 CORS/범위 실증은 별도 미완료로 유지한다.

## 최우선 목표 — 외부 사용자 공개 베타

- 공개 HTTPS 두 출처 → 공개 Google 신규 로그인 → 대표/길드 조회 → 개인 키 새로고침 전체 흐름을 출시 기준으로 삼는다.
- 운영 보강을 후순위로 이동한다. 결제/계정 승인이 필요한 공개 주소 작업과 독립적인 핵심 기능 실증을 진행하고, 완료된 백업 검증을 반복하지 않는다.
- 현재 루프백 서버를 보존하면서 공식 Cloudflare Tunnel 도구와 고정 도메인 연결을 준비한다. 도메인/계정 없이 임시 공개 링크를 정식 배포처럼 제공하지 않는다.

## 조회 복구 오류 표시

- 대시보드 조회 오류를 사용자 작업 오류와 분리하고 성공 시 조회 오류만 해제한다. 마지막 정상 데이터는 실패 중에도 보존한다.
- 실패 후 성공 회귀 테스트와 웹 빌드를 검증하고 Linux 정적 화면에 반영한다.

## 백업 여유 공간 검사

- 백업 시작 전 원본 백업 경로와 D 복사 경로의 여유 공간을 검사한다. 기본 5GiB 미만이면 실패 처리하고 삭제하지 않는다.
- 검사 계산의 경계값 테스트와 실제 systemd 실행을 검증한다. 별도 사용자 알림 전송이나 실시간 감시를 구현한 것으로 표시하지 않는다.

## 별도 디스크 백업

- D의 새 서비스 전용 폴더에 사용자·SYSTEM·관리자 접근만 허용한다. 정상 dump만 복사하고 해시를 확인하며 기존 파일은 덮어쓰거나 삭제하지 않는다.
- 백업 서비스 후처리에 연결하여 복사 실패도 서비스 실패로 표시한다. 실제 복사본 검증 후 기록한다.

## 운영 백업 복원 시험

- 활성화한 백업 서비스가 생성한 실제 아카이브를 새 검증 DB로 복원한다. 운영 DB는 변경하지 않으며 설정/원본 기록의 개수와 제약 조건 복원 성공을 확인한다.

## Windows 로그인 시 Linux 실행 유지

- 현재 Windows 사용자 로그인 시 숨김 PowerShell 작업으로 해당 사용자의 WSL을 실행 유지한다. 중복 작업은 만들지 않고 작업 스케줄러 재시도를 설정한다.
- 실제 재부팅 없이 작업 수동 실행과 서버 상태를 검증한다. 로그인 전 자동 실행은 이 구성의 범위가 아니다.

## 실제 Linux 운영 전환

- Windows 수집 완료 후 서버만 중지하고 최신 DB를 새 Linux 17 운영 DB에 복원한다. 기존 Windows DB/바이너리와 이전 검증 DB를 보존한다.
- 운영자 키와 Google 설정은 표준 입력으로 Linux 보호 파일에 전달하며 콘솔·Git에 노출하지 않는다. 서비스/백업 활성화 후 실제 상태와 수집을 확인한다.
- 전환 실패 시 Linux 서비스를 중지하고 Windows 서버를 복원한다. Windows 부팅 연동은 별도로 구성·검증한다.

## Linux 서비스 복구 시험

- 실제 운영 서버 대신 별도 포트·복원 DB·키 없는 임시 systemd 서비스로 권한 제한과 비정상 종료 후 재시작을 시험한다.
- 시험 서비스만 종료하고 운영 서비스 자동 시작이나 키 이전을 완료했다고 표시하지 않는다.

## 병행 서버 실행 검증

- 서버의 고정 루프백 포트를 환경 설정으로 선택할 수 있게 하되 기본 3100/3101과 외부 바인딩 금지는 유지한다.
- 별도 포트에서 복원 DB를 연결한 Linux 서버의 상태·정적 화면을 검증한다. 운영 키 없이 실행하여 중복 수집을 방지한다.

## PostgreSQL 17 병행 준비

- 공식 PGDG 저장소를 사용해 Linux에 17을 병행 설치한다. 기존 16 개발 클러스터와 Windows 운영 서버는 보존한다.
- 새 포트에서 표본 백업/복원과 서버 테스트를 검증한다. 실제 운영 전환은 실제 백업 복원 및 설정 검증 이후 별도 수행한다.

## 비파괴 Linux 복원 준비

- 복원은 명시된 새 DB에만 수행하고 기존 DB에는 쓰지 않는다. 실패 시에도 원본 아카이브와 새 DB를 보존하여 원인을 확인할 수 있게 한다.
- 임시 표본 DB로 정상 복원·중복 대상 거부·잘못된 아카이브 거부를 검증한다. Windows PostgreSQL 17에서 Linux 16으로의 운영 이전은 버전 정합성 확인 전 수행하지 않는다.

## 정기 백업 운영 구성

- Linux 운영 전환을 위한 매일 04:30 KST 백업 타이머와 제한된 권한의 백업 서비스를 추가한다. 중지 중 놓친 실행은 재기동 후 보충한다.
- 백업 디렉터리만 쓰기 허용하고 파일 600 권한, 실패 partial 분리, 기존 백업 보존을 유지한다. 별도 임시 DB 복원과 systemd 구성 검증을 수행한다.
- 실제 운영 DB는 Windows에 있으므로 빈 Linux DB의 예약 백업을 운영 완료로 간주하지 않고 운영 이전 후 활성화한다.

## 계정 탈퇴 준비

- 로그인된 본인의 설정·소셜 연결·모든 세션·진행 중 연결 요청을 트랜잭션으로 삭제한다. 공용 캐릭터 및 성장 기록은 삭제하지 않는다.
- 웹에서 탈퇴 문구를 직접 입력하도록 확인하고 출처 검증을 유지한다. 실제 사용자 계정은 테스트로 삭제하지 않는다.

## 가입 전 획득량 복원과 공개 배포 설정 점검

- 최신 사용자 지시에 따라 길드 기간 순위도 가입 전 획득량을 포함한다. 날짜별 원본 명단은 보존하되 경험치 합산 필터로 사용하지 않는다.
- 웹 빌드가 .env의 공개 출처를 무시하는 문제를 수정하고 HTTPS·서버/대시보드 일치·개인 키 별도 출처를 검증한다. 로컬 개발은 유지한다.
- 공개 주소·HTTPS·계정 운영 전환·서비스 복구와 개인정보/탈퇴 준비를 배포 잔여 작업으로 정리한다. 배포 미완료 상태에서 광고를 연결하지 않는다.

## 날짜별 명단과 웹 기간 순위 연결

- 현재 순위 대상은 길드 명단을 기준으로 하고, 기간 획득량은 날짜별 명단에 포함된 날만 합산한다. 즐겨찾기 순위와 개인 성장 그래프는 소속과 무관하게 유지한다.
- 과거 닉네임은 해당 날짜의 동일 OCID 스냅샷으로 확인한다. 명단 또는 해당 날짜 캐릭터 기록 누락은 확인 불가로 남긴다. 가입 전 날짜는 제외하고 미확인 날짜는 부분 수집으로 표시한다.

## 날짜별 길드 명단 이관 1단계

- 날짜 지정 공식 guild/basic 응답을 현재 명단과 분리 보존한다. 성공한 빈 명단과 미수집을 구분하고 실패는 영속 큐로 재시도한다.
- 최근 30일 누락 명단을 활성 길드만 대상으로 주기당 최대 30건 보충한다. 현재 명단을 과거 날짜에 복제하지 않는다.
- 원본 날짜 명단 확보를 먼저 검증하고 기간 순위 포함 규칙·과거 탈퇴자 캐릭터 보충은 후속 단계로 분리한다.

## 대시보드 DB 조회 묶음 처리

- 캐릭터마다 과거 기록과 자정 기준점을 조회하는 2N회 왕복을 대상별 묶음 조회 두 번으로 줄인다. 사용자별 대상 필터, 날짜 범위, 기준점 선택과 응답 형식은 유지한다.
- 실제 PostgreSQL 회귀 테스트로 서로 다른 캐릭터 기록의 혼입, 날짜 범위와 기준점 선택을 검증한다. 실행 서버는 기존 수집 완료 후 교체한다.

현재 완료·진행 중·할 일·보류 상태는 `checklist.md` 맨 위의 현재 전체 현황을 기준으로 한다. 이 문서의 아래 계획은 누적 이력이며 과거 계획이 최근 사용자 결정을 덮어쓰지 않는다. 각 작업 단위의 상태 변경은 체크리스트에, 이유·검증 결과는 `context-notes.md`에 반영한다.

## 실제 첫 수집 상태 표시 검증

- Google 로그인과 대표 저장 후 실제 길드 수집을 검증한다.
- 진행 중인 수집을 대기로 오인하지 않도록 서버의 실행 상태를 화면에 전달한다. 완료·일부 실패·중단을 구별하고 상태 문구 회귀 테스트와 웹 빌드로 확인한다.

## Google OAuth 승인 후 로컬 연결

- 승인된 Google 웹 클라이언트의 키를 출력 없이 서버에 연결한다. 브라우저 다운로드가 동작하지 않아 루프백·임시 토큰·5분 제한의 일회용 설정 입력 도구를 사용한다.
- 기존 .env의 다른 값은 유지하고 설정 유효성·잘못된 요청 거부를 테스트한다. 실제 로그인 후 자동 수집 설정 상태를 확인한다.

## 사용자 개입 최소화와 수집 회귀 수정

- 로그인·본인 확인·결제는 사용자 승인 단계로 남기고, 공급자 콘솔 입력·연결·검증은 대행 가능한 순서로 정리한다.
- 한 주기에서 실패한 대표 조회를 사용자 수만큼 반복하거나 다시 조회하는 문제를 막는다.
- 동일 OCID 닉네임 변경 직후에도 새 이름으로 구독 대상을 계산하도록 수정한다. PostgreSQL·모의 API 회귀 테스트로 검증한다.

## Linux 실행 절차와 복구 준비

- Windows 전용 웹 빌드 명령을 Node 기반 공통 명령으로 바꾸고 두 운영체제에서 검증한다.
- Linux 서비스 단위와 비밀 정보 없는 로컬 DB 백업 도구를 제공한다. 기존 Windows 서버와 충돌하지 않도록 서비스는 자동 활성화하지 않는다.
- 백업을 별도 검증 DB에 복원하여 확인한다. 기존 DB·API 키 이전과 공개 배포는 이 단계에 포함하지 않는다.

## 재부팅 후 Ubuntu 서버 환경 검증

- Ubuntu 24.04 WSL2를 설치하고 별도 Linux 개발 사용자·컴파일 도구·PostgreSQL을 구성한다.
- Linux 파일시스템의 별도 체크아웃에서 Rust 서버 테스트를 수행한다. 기존 Windows DB와 키 저장소는 이동·삭제하지 않는다.
- Windows 보안 저장소를 Linux 파일로 복사하지 않고 현재 Windows 서버 연결은 보존한다. Linux 검증 완료 여부와 실행 방법을 기록한다.

## 기존 키 연결 및 WSL 준비

- 사용자 승인에 따라 Windows 자격 증명 관리자의 기존 앱 키를 서버 메모리에서만 읽는다. 키를 파일·로그·Git에 복사하지 않는다.
- 인증 조회로 키 사용 가능 여부를 검증하고 WSL/Ubuntu 설치를 진행한다. 관리자 승인과 재부팅은 사용자 작업을 강제로 종료하지 않고 필요한 시점에 알린다.

## 2026-09-07 후속 서버 수집 안정화

- 과거 보충을 PostgreSQL 작업 큐로 보존하고 캐릭터별로 공정하게 처리한다. 실패 대상은 지연 재시도하며 재시작 시 이어서 진행한다.
- 최신 스냅샷과 관측 기록을 원자적으로 저장하고 동일 OCID의 닉네임 변경 이력을 보존한다. 동일 닉네임의 다른 OCID는 자동 병합하지 않는다.
- 실제 개발 DB에서 큐 공정성·재시도·168시간 제외·닉네임 변경 보존을 검증한다. 운영 키와 소셜 발급 정보 없이 가능한 범위까지 수행한다.

## 2026-09-07 로컬 중앙 서버·웹 전환

- 기존 앱을 유지하면서 독립 Rust/Axum/PostgreSQL 서버와 React 웹 진입점을 추가한다.
- 168시간 활성 사용자 기준으로 대표·현재 길드·즐겨찾기 수집 대상을 합치고 15분마다 수집한다.
- Google·카카오·네이버 인증과 서버 세션을 구성하되 미발급 연결은 사용 가능으로 표시하지 않는다.
- 개인 키는 별도 브라우저 출처에서만 보관·사용한다. 개발키는 대표만, 서비스키는 중복 제거한 모든 대상의 최신 자료만 조회한다. 공용 기록에 쓰지 않는다.
- 현재 PC에서 빌드·회귀 테스트·로컬 HTTP 검증을 수행한다. 외부 배포, 도메인 구매, 기존 DB 변경은 하지 않는다.

## 2026-09-05 계산 안정화와 서버 서비스 전환 검토

- 누락일을 건너뛴 증가량의 일간 오분류, 부분 수집의 정상 표시, UTC 갱신 시각 표시와 이미지 캐시 손상을 수정한다.
- 오늘 획득량은 길드 수집일과 분리하고, 전일 종료 기록이 없으면 자정에 가장 가까운 수집 표본으로 추정한다. 공식 기준 확보 시 재계산하며 설정에서 오차와 보정 가능성을 안내한다.
- 원본 기록은 보존하고 기존 잘못된 파생값은 트랜잭션으로 수정한다. 기간 포함 범위와 동점 정렬은 사용자 답변 후에만 변경한다.
- 오늘의 0을 성장 평균에 포함하는 정책은 유지하며 위젯 디자인·모션은 변경하지 않는다.
- 중앙 수집 서버·웹·모바일 구조의 유효성, 넥슨 로그인 권한, 호출 한도, 광고·멤버십 조건을 공식 문서로 검토한다. 서버 공개·결제 도입은 이번 검토 범위에 포함하지 않는다.
- 회귀 테스트와 Windows·Android 빌드, APK 전송을 검증하고 변경과 남은 결정을 기록한다.

Windows 10·11에서 동작하는 개인용 Tauri 2 앱을 개발한다. NEXON Open API의 길드 및 캐릭터 기본 정보를 SQLite에 날짜별로 저장하고, 버전이 고정된 레벨별 필요 경험치표로 일간·7일·30일 획득 경험치를 계산한다.

대시보드는 길드 전체 순위, 대표 캐릭터와의 격차, 즐겨찾기 및 날짜별 비교 그래프를 제공한다. 항상 위 미니 위젯은 `나＋즐겨찾기`와 `길드 상위` 모드를 한 번의 클릭으로 전환한다.

API 키는 Windows 자격 증명 관리자에만 보관한다. 환산주스탯 사이트에는 접근하지 않으며 모든 화면에 `Data based on NEXON Open API`를 표시한다.

검증은 Rust 단위·통합 테스트, TypeScript 빌드, Tauri Windows 번들 빌드 순서로 진행한다.

## 2026-08-20 실사용 개선 계획

- 단일 날짜만 수집된 상태를 과거 보충 완료로 오판하지 않도록 누락 스냅샷 검사를 수정한다.
- 전체 UI 배율과 캐릭터 이미지 배율을 앱 안에서 즉시 조절하고 로컬에 기억한다.
- 현재 길드원 중 대표 캐릭터를 변경할 수 있게 한다.
- 획득 경험치를 현재 레벨 필요 경험치 대비 비율과 한국어 절대 단위로 함께 표시한다.
- 화면은 30초마다 로컬 데이터를 갱신하고, 공식 API의 평균 15분 지연을 안내하면서 설정한 분 간격으로 최신 정보를 수집한다.
- 최근 최신 정보 표본에서 경험치가 증가한 캐릭터는 활동 중으로 표시하고 공식 이미지의 walk1·walk2 프레임을 번갈아 보여준다.

## 2026-08-22 표시 정확성 개선 계획

- 캐릭터 이미지의 최소·기본·최대 배율을 모두 높이고 이미지가 배경 칸을 더 가득 채우도록 조정한다.
- 걷기 애니메이션은 서로 다른 액션인 `A02`와 `A03`을 교대하지 않고 공식 `walk1`의 `A02.0`부터 `A02.3` 프레임을 순환한다.
- 사냥 중 판정은 최근 20분 안의 두 최신 표본 사이에서 계산 가능한 경험치가 1 이상 증가한 경우라는 현재 기준을 유지하고 화면에 근거를 안내한다.
- 현재 경험치 비율은 NEXON 기본 정보의 `character_exp_rate` 원본을 사용하고, 오늘 획득량은 최신 완료일 스냅샷과 최신 실시간 표본의 차이로 별도 계산한다.
- 대시보드와 위젯의 경험치 표시는 `30.123% (+2.2조)` 형식으로 통일한다.

## 2026-08-22 화면·비교 기능 후속 개선 계획

- 캐릭터 이미지 자체를 배경 칸 안에서 추가 확대하고 기본 화면 배율을 두 단계 높인다.
- 경험치 순위는 선택 기간 획득량 기준을 유지하되 `나와의 격차`는 대표 캐릭터와 대상의 현재 레벨·현재 경험치를 모두 반영한 성장 위치 차이로 계산한다.
- 즐겨찾기 카드의 대표 캐릭터 배지와 순위표 대표 행 중앙 이동을 구현한다.
- 다크·라이트 테마를 전환하고 네이티브 제목 표시줄과 스크롤바에도 같은 테마를 적용한다.
- 성장 그래프 색상은 캐릭터 ID에 따라 결정하여 즐겨찾기 추가와 순서 변화에도 기존 선 색상을 유지한다.
- 미니 위젯의 목록 제한을 제거하고 전체 대상이 스크롤되도록 한다.
- 기존 브랜드를 복제하지 않는 단풍잎 계열의 독자적인 앱 아이콘을 생성해 Windows·트레이·설치본 자산에 적용한다.

## 2026-08-22 즐겨찾기·오늘 성장 흐름 개선 계획

- 카드 제목은 `즐겨찾기`로 간결하게 표시하고 외부 캐릭터 지원 설명은 유지한다.
- 대표 캐릭터가 아닌 즐겨찾기 카드에는 해제 가능한 별 버튼을 표시한다.
- 현재 길드원 즐겨찾기 카드는 클릭 시 순위표의 해당 행으로 이동하고 외부 캐릭터 카드는 이동 동작을 제공하지 않는다.
- 완료일 성장 기록 뒤에 최신 API 표본으로 계산한 KST 오늘 획득 경험치를 추가한다.

## 2026-08-22 랭킹·배율·그래프 후속 개선 계획

- Windows 실행 파일·창·트레이·설치 바로가기 아이콘을 동일한 단풍잎 자산으로 명시하고 작업 표시줄 캐시 갱신 경로를 마련한다.
- 기존 캐릭터 이미지 150%의 물리 크기를 새 표시 기준 100%로 정의한다.
- 화면과 이미지 배율을 대시보드에서 위젯으로 즉시 전달하고 배율 조절 팝업은 확대 영역 밖에 고정한다.
- 대시보드 랭킹을 전체 성장 위치와 기간 획득량 기준으로 전환하고 열 제목과 표시 수치를 기준에 맞게 변경한다.
- 위젯 랭킹을 오늘 획득량과 전체 성장 위치 기준으로 전환하며 괄호 안에는 항상 오늘 획득량을 표시한다.
- 성장 그래프를 부드러운 선·꺾은선·막대 형식으로 전환하고 기간별 선택을 로컬에 저장한다.

## 2026-08-22 대시보드·위젯 랭킹 정합성 개선 계획

- 메인 길드 랭킹과 위젯 길드 상위는 동일하게 최신 길드원만 대상으로 사용한다.
- 외부 즐겨찾기는 즐겨찾기 카드와 위젯 `나＋즐겨찾기` 모드에서만 비교한다.
- 두 화면의 최초 랭킹 기준을 전체 경험치로 통일하고 공통 필터·정렬 함수를 테스트한다.

## 2026-08-22 서비스 API 키 발급 도움말 계획

- API 키 설정창에 작은 도움말 버튼을 추가해 서비스 단계 등록 절차를 팝업으로 안내한다.
- NEXON 공식 등록 요건과 앱에 맞는 서비스명·개발 환경·소개·API 활용 목적 예시를 제공한다.
- 배포 GitHub 주소와 권장 입력 문구를 항목별로 복사할 수 있게 한다.
- API 키 자체는 GitHub나 공개 화면에 복사하지 않도록 보안 주의사항을 표시한다.

## 2026-08-22 오늘 경험치·활동 상태 정합성 개선 계획

- 대시보드의 일간 기간별 순위와 요약은 완료일 증가량이 아닌 최신 API 표본으로 계산한 오늘 증가량을 사용한다.
- 사냥 중 판정은 마지막 두 표본만 보지 않고 최근 20분 범위의 성장 여부를 사용해 동일 값 새로고침에도 상태를 보존한다.
- 미니 위젯의 대시보드 버튼은 최소화된 메인 창을 해제한 뒤 이전 창 크기로 복원하고 포커스한다.
- 날짜 기준, 활동 상태와 창 복원 경로에 회귀 테스트를 추가하고 Windows 배포본을 다시 생성한다.

## 2026-08-22 탐색·즐겨찾기·최초 도움말 개선 계획

- 본문의 실제 섹션 순서에 맞춰 왼쪽 메뉴에서 성장 기록을 길드 순위보다 먼저 배치한다.
- 즐겨찾기 카드에 명시적인 드래그 손잡이를 추가하고 손잡이에서 시작한 드래그만 순서 변경으로 처리한다.
- 즐겨찾기 순서는 로컬 설정에 저장해 새로고침과 앱 재시작 뒤에도 유지한다.
- 최초 설정 화면에서 서비스 API 키 발급 도움말을 바로 열 수 있게 기존 안내창을 재사용한다.

## 2026-08-22 즐겨찾기 드래그 동작 수정 계획

- Windows WebView에서 불안정한 HTML5 드래그 이벤트 의존을 제거한다.
- 손잡이의 포인터 누름과 이동을 직접 추적해 다른 카드 위를 지나면 목록을 즉시 재배치한다.
- 포인터를 놓거나 취소하면 드래그 상태를 종료하고 마지막 순서를 로컬에 유지한다.

## 2026-08-22 즐겨찾기 기본 정렬 개선 계획

- 저장된 사용자 순서가 없으면 즐겨찾기를 레벨, 현재 경험치, 닉네임 순으로 정렬한다.
- 즐겨찾기 제목 옆에 기본 정렬 복원 버튼을 제공한다.
- 현재 순서가 기본 정렬과 같으면 버튼을 비활성화하고 드래그로 달라졌을 때만 활성화한다.

## 2026-08-22 즐겨찾기 연속 드래그 수정 계획

- 재배치 후 드래그 중인 카드가 마우스 아래로 들어오면 이전 대상 캐릭터 잠금을 해제한다.
- 마우스 버튼을 놓지 않고 앞뒤 위치를 반복해서 오갈 수 있게 한다.
- 세 번째에서 두 번째, 다시 세 번째로 이동하는 순서 계산을 회귀 테스트한다.

## 2026-08-23 Android 앱 이식 계획

- 기존 React·Rust·SQLite·NEXON API 계층을 공유하고 Windows 전용 트레이·위젯·자동 시작만 데스크톱 조건부 코드로 분리한다.
- Android API 키는 SharedPreferences에 저장하되 Android Keystore 키로 암호화하는 네이티브 Keyring 저장소를 사용한다.
- 휴대폰에서는 사이드바 대신 모바일 탐색 메뉴를 제공하고 상단 도구, 카드, 표, 모달과 스크롤바를 작은 화면에 맞춘다.
- Windows와 Android 설정·권한 파일을 플랫폼별로 분리하고 Android 7.0 이상을 대상으로 초기화한다.
- Windows 회귀 테스트와 빌드를 유지하면서 Android 프로젝트를 초기화하고 설치 가능한 APK를 생성한다.
- Play Store 서명·등록과 다른 앱 위 오버레이 위젯은 이번 범위에서 제외한다.

## 2026-08-23 통합 배포 자동화 계획

- 기존 Windows 배포 명령이 PC 설치본과 포터블 EXE를 만든 직후 같은 버전의 Android ARM64 APK도 생성하게 한다.
- Windows와 Android 결과물은 모두 프로젝트의 `release` 폴더에 저장한다.
- Android 빌드가 실패하면 통합 배포 명령도 실패시켜 버전별 산출물이 어긋나지 않게 한다.

## 2026-08-23 Android 최초 설정 정지 개선 계획

- 최초 설정의 캐릭터 식별, 기본 정보, 길드 확인, 보안 저장과 완료 단계를 화면 진행률로 즉시 전달한다.
- Android Keystore 호출을 차단 작업으로 분리하고 제한 시간 뒤에는 사용자가 재시도할 수 있는 오류를 반환한다.
- 수정된 Android APK와 Windows 배포본을 통합 빌드해 같은 `release` 폴더에 저장한다.

## 2026-08-23 Android Keystore 컨텍스트 초기화 계획

- 메인 Rust 라이브러리를 Android 액티비티 시작 시 먼저 불러온다.
- 키 저장 라이브러리의 JNI 초기화 함수에 Android 애플리케이션 컨텍스트를 전달한 뒤 Tauri 런타임을 시작한다.
- 실제 Android 패키징으로 Kotlin과 JNI 연결을 검증하고 새 패치 버전 APK를 생성한다.

## 2026-08-23 Android 초기 동기화·터치·뒤로가기 개선 계획

- 첫 과거 동기화에 일부 실패가 있으면 누락 작업을 한 번 즉시 재시도하고 오늘 실시간 동기화까지 완료한 뒤 대시보드로 이동한다.
- Android WebView의 기본 터치 하이라이트와 차트 포커스 테두리를 제거하되 앱이 정의한 버튼 눌림 효과는 유지한다.
- 도움말, 설정과 직접 지정 날짜창은 브라우저 기록 스택으로 중첩하고 Android 뒤로가기로 가장 위 창부터 닫는다.

## 2026-08-23 Android 즐겨찾기 순위·아바타 복원 계획

- 모바일 하단 탐색에 즐겨찾기 탭을 추가하고 즐겨찾기 캐릭터만 현재 순위 기준으로 표시하는 표를 제공한다.
- 즐겨찾기 순위표는 길드 밖 캐릭터도 포함하며 길드 순위와 같은 경험치·격차·상태 정보를 표시한다.
- Android 앱이 백그라운드에서 복귀하면 사냥 중 아바타 이미지와 걷기 프레임을 첫 프레임부터 다시 불러온다.
- 백그라운드 동안에는 걷기 프레임 전환을 멈춰 WebView의 정지된 이미지 요청이 누적되지 않게 한다.

## 2026-08-23 대시보드 선택 기억·흰 배경 아이콘 계획

- 대시보드의 일간·7일·30일·직접 지정 기간을 로컬 설정에 저장하고 Android와 Windows 재실행 시 복원한다.
- 전체 경험치·기간별 경험치 랭킹 기준을 같은 방식으로 저장해 즐겨찾기와 길드 순위표가 이전 상태를 공유한다.
- 잘못되거나 이전 버전에서 남은 설정값은 안전한 기본값인 최근 7일과 전체 경험치로 되돌린다.
- 기존 단풍잎 원본 모양은 유지하고 흰색 정사각형 배경을 합성해 Windows와 Android 아이콘 세트를 다시 생성한다.

## 2026-08-23 즐겨찾기 화면·수동 활동 확인·아이콘 여백 계획

- 즐겨찾기 순위는 메인 대시보드 본문에서 제거하고 데스크톱 사이드바와 모바일 하단의 독립 탭에서만 표시한다.
- 성장 기록과 길드 순위 탭은 메인 화면으로 전환한 뒤 기존 섹션으로 이동하게 한다.
- 수동 동기화는 최신 표본을 먼저 확보하고 일반 동기화 뒤 다시 수집해 사냥 중 판정에 필요한 두 시점 표본을 만들며, 짧은 후속 표본도 예약한다.
- 오랜 미사용 뒤 첫 최신 표본만으로는 최근 사냥 여부를 단정하지 않고 두 공식 API 표본의 실제 경험치 증가를 계속 기준으로 사용한다.
- 흰 배경 단풍잎 아이콘은 원본을 중앙 76% 크기로 축소해 Windows와 Android 런처 마스크에서 가장자리가 잘리지 않게 한다.

## 2026-08-23 Android 즐겨찾기 경험치 알림 계획

- 대표 캐릭터를 제외한 즐겨찾기의 최신 레벨·경험치를 별도 기준점으로 저장하고 실제 증가가 확인될 때만 알림을 보낸다.
- API 키는 기존 Android Keystore에서 Rust 계층이 직접 읽으며 Kotlin 설정이나 로그에 복제하지 않는다.
- WorkManager의 15분 주기 작업과 앱 시작 즉시 작업을 구성해 앱 화면이 닫힌 동안에도 가능한 범위에서 감시한다.
- Android 13 이상의 알림 권한을 요청하고 캐릭터별 증가량·현재 경험치율 알림을 중복 없이 발송한다.
- 권한이 없거나 작업 실패·장기 미실행이 감지되면 앱을 사용하는 동안 30분 간격으로 네이티브 안내창을 표시한다.
- 강제 종료와 절전 정책에 따른 지연은 설정 안내에 명확히 표시하고 서버·FCM은 추가하지 않는다.

## 2026-08-23 앱 시작 사냥 상태 동기화 계획

- 설정된 대시보드가 처음 마운트되면 일별 동기화를 기다리지 않고 전체 추적 캐릭터의 최신 표본을 즉시 수집한다.
- 최근 20분 표본이 남아 있으면 새 표본과 바로 비교하고, 비교 기준이 부족한 경우 1분 뒤 후속 표본을 수집한다.
- 초기 수집과 후속 수집이 끝날 때마다 대시보드와 미니 위젯이 읽는 동일한 로컬 상태를 갱신한다.
- Windows 숨김·위젯 시작과 Android 일반 시작에서 동일한 React 대시보드 흐름을 사용한다.

## 2026-08-23 앱 시작 사냥 판정 보강 계획

- 실제 기록에서 프런트엔드의 1분 후속 타이머가 실행되지 않은 사례를 기준으로 시작 수집 예약을 Rust 런타임으로 옮긴다.
- 설정 완료 여부를 짧게 확인한 뒤 즉시 최신 표본을 수집하고, 90초 및 추가 210초 뒤에 확인 표본을 수집한다.
- 일별 누락 동기화는 기존대로 계속 수행하되 시작 확인 표본은 별도 작업으로 예약해 서로의 완료 순서에 의존하지 않게 한다.
- 창 최소화와 WebView 타이머 제한을 받아도 프로세스가 실행 중이면 후속 표본 수집이 이어지게 한다.

## 2026-08-23 모바일 알림 상태·PC 순위 전환 계획

- Android 네이티브 플러그인이 런타임 알림 권한, 시스템 알림 활성화, 감시 작업 상태와 마지막 성공 시각을 반환한다.
- 모바일 설정창에 알림 가능 여부를 큰 상태 배지와 세부 항목으로 표시하고 시스템 설정 이동·즉시 재확인 버튼을 제공한다.
- PC 좌측 메뉴의 길드 순위 아래에 작은 즐겨찾기 순위 전환 탭을 배치한다.
- 작은 탭을 누르면 대시보드 하단 순위표만 교체하고 선택한 순위 탭이 큰 위치로, 이전 탭이 작은 위치로 서로 바뀐다.
- Android의 기존 하단 즐겨찾기 독립 화면은 유지한다.

## 2026-08-23 모바일 알림 상태 표시 보정 계획

- 백그라운드 작업 응답의 JSON `null`을 실제 오류 문자열과 구분하고 기존에 잘못 저장된 `"null"` 값도 정리한다.
- 알림 권한뿐 아니라 최근 백그라운드 감시 성공 여부까지 전체 알림 가능 판정에 포함한다.
- 감시 시작 중과 실제 사용 불가 상태를 구분하고 상태에 맞는 사용자 조치를 설정창에 표시한다.
- 설정창 우상단 도구 버튼을 일반 레이아웃으로 분리해 알림 상태 카드와 겹치지 않게 한다.

## 2026-08-23 설정 도움말 분리 계획

- 기존 API 키 발급 도움말 버튼을 API 키 변경 영역 제목 가까이 옮긴다.
- 알림 상태 카드에 별도 도움말 버튼을 추가해 두 도움말의 목적을 시각적으로 분리한다.
- 알림 도움말에 감시 대상, 실행 주기, 알림 조건, 상태 표시와 문제 해결 절차를 설명한다.
- 도움말 팝업은 Android 뒤로가기에서 설정창보다 먼저 닫히게 한다.

## 2026-08-23 앱 사용 중 알림 감시 계획

- 시스템 알림 발송은 화면 표시 여부와 무관하게 유지하되 전면 화면에서는 별도 확인 주기를 사용한다.
- Android 앱이 전면으로 들어올 때 즐겨찾기 경험치를 즉시 확인한다.
- 앱 사용 중에는 5분마다 확인하고 화면을 벗어나면 전면 확인만 중지한다.
- 최초 알림 권한 허용 직후에도 즉시 확인해 다음 백그라운드 주기를 기다리지 않게 한다.

## 2026-08-23 앱 종료 후 지속 알림 감시 계획

- WorkManager 주기 작업은 지연 가능한 보조 수단으로 유지하고 사용자에게 보이는 전경 감시 서비스를 추가한다.
- 앱을 연 상태에서 서비스를 시작해 화면 종료 후에도 5분마다 즐겨찾기 경험치를 직접 확인한다.
- 서비스 실행 중에는 저중요도 상시 알림을 표시하고 경험치 증가 알림은 기존 채널로 별도 발송한다.
- Android 15 이상 데이터 동기화 서비스 제한보다 앞서 서비스를 안전하게 종료하고 WorkManager로 전환한다.
- 앱 강제 중지는 운영체제 정책상 감시 불가 상태임을 도움말에 명시한다.

## 2026-08-26 모바일 알림 폐기·Android 홈 위젯 계획

- 모바일 알림 화면, 권한, 전경 서비스, WorkManager, 네이티브 연결과 경험치 알림 판정 코드를 모두 제거한다.
- 기존 설치 DB의 알림 기준점 테이블은 파괴적 마이그레이션을 피하기 위한 미사용 호환 데이터로만 남긴다.
- 대형 위젯은 참고 프로젝트의 헤더·요약·반복 행·갱신 시각 구조만 차용하고 길드원 따라가기의 색상과 구성 요소로 새로 디자인한다.
- 대형 위젯에는 대표 캐릭터를 반드시 포함한 즐겨찾기 캐릭터의 오늘 경험치 순위를 표시하며 세로 크기에 맞춰 표시 행 수를 조절한다.
- 고정형 1×2 위젯과 2×2 위젯에는 대표 캐릭터 이미지, 현재 경험치율과 오늘 획득량을 표시한다.
- 앱이 대시보드 데이터를 읽을 때 위젯용 최소 스냅샷을 Android 전용 저장소와 이미지 캐시에 갱신하고 모든 홈 위젯을 즉시 다시 그린다.
- 홈 위젯을 누르면 길드원 따라가기 앱을 열며, 저장된 데이터가 없으면 앱에서 먼저 동기화하도록 안내한다.

## 2026-08-27 Android 홈 위젯 재설계 계획

- 대형 위젯의 가중치 기반 고정 행을 Android 컬렉션 위젯의 스크롤 목록으로 교체하고 각 행 높이를 일정하게 유지한다.
- 즐겨찾기가 적으면 행을 상단부터 차례로 배치하고 남은 공간은 비우며, 많으면 같은 높이의 목록을 세로 스크롤한다.
- 기존 2×1 대표 위젯과 공급자 등록을 삭제하고 가로 3칸·세로 2칸의 주간 성장 위젯으로 교체한다.
- 주간 성장 위젯은 세로 크기를 고정하고 가로만 3칸에서 최대 5칸까지 조절되도록 최소·최대 리사이즈 폭을 지정한다.
- 주간 성장 위젯의 왼쪽에는 대표 캐릭터 이미지를, 오른쪽에는 현재 경험치율·최근 7일 총 획득량·7일 일별 막대를 표시한다.

## 2026-08-27 대형 위젯 추가 실패 수정 계획

- 실제 기기의 런처·AppWidget 로그에서 대형 위젯 생성 실패 원인을 확인한다.
- 대형 위젯의 컬렉션 서비스 연결과 `RemoteViews` 구성을 최소 범위로 수정한다.
- 대형 위젯 추가·스크롤·빈 목록 표시를 검증하고 Android APK를 다시 배포한다.

## 2026-08-27 대형 위젯 재진단과 3×2 수치형 전환 계획

- 연결 기기가 있으면 런처 예외를 수집하고, 없으면 대형 위젯에만 포함되는 행 이미지·컬렉션 전달량을 제거한 최소 구성부터 검증한다.
- 대형 목록의 캐릭터 이미지는 위젯 전달 전에 최대 96픽셀로 축소해 `RemoteViews` 전송 크기를 줄인다.
- 3×2 위젯은 캐릭터 이미지와 그래프를 제거하고 최근 7일의 날짜·레벨·경험치율·일일 획득량을 줄 단위로 표시한다.
- 하단에는 7일 일평균 획득량, 현재 레벨의 남은 경험치량과 단순 추정 레벨업 날짜를 표시한다.
- 대형·주간·2×2 위젯은 공통 색상 체계와 여백을 사용하되 정보 위계와 화면 비율에 맞춰 각각 새로 구성한다.

## 2026-08-28 Android 위젯 인플레이트 실패 수정 계획

- 정상인 2×2 위젯과 실패한 대형·3×2 위젯의 레이아웃 차이를 기준으로 공통 실패 요소를 제거한다.
- `RemoteViews`에서 허용되지 않는 일반 `View` 구분선을 허용되는 `TextView` 구분선으로 교체한다.
- 대형·3×2 레이아웃에 일반 `View`가 다시 들어오지 않도록 회귀 테스트를 추가한다.
- Android 테스트와 릴리스 빌드를 통과시킨 뒤 최신 APK를 휴대폰 Download 폴더로 전송한다.

## 2026-08-28 Android 위젯 이미지·레이아웃 개선 계획

- 캐릭터 PNG의 투명 여백을 제거하고 픽셀 아트용 비보간 확대를 적용해 더 크고 선명하게 표시한다.
- 대형 순위 행은 전송량을 제한한 해상도를, 대표 전용 위젯은 더 높은 해상도를 사용한다.
- 3×2 주간 위젯은 7개 날짜 행과 요약 행이 세로 공간을 균등하게 채우도록 재배치한다.
- 기존 2×2·3×2 위젯을 유지하고 두 구성을 좌우로 합친 고정형 5×2 위젯을 새로 등록한다.
- 위젯 단위 테스트, 전체 테스트와 Android 릴리스 빌드를 통과시킨 뒤 APK를 휴대폰으로 전송한다.

## 2026-08-28 Android 주간 위젯 표시 정리 계획

- 7개 날짜 중 첫 행은 기준 경험치로 표시하고 상승량 합계·일평균·예상 레벨업 계산에서 제외한다.
- 주간 행의 날짜를 축약하고 글자를 한 단계 키우되 한 줄 말줄임으로 가로 경계를 지킨다.
- 즐겨찾기 랭킹 헤더의 인원수 표시를 제거한다.
- 대표 카드의 `PRIMARY`를 `대표캐릭터`로 바꾸고 이미지 뒤의 라운드 패널을 제거한다.
- 관련 Rust·Android 회귀 테스트와 릴리스 빌드를 검증한 뒤 APK를 휴대폰으로 전송한다.

## 2026-08-28 Android 전체 위젯 글자 확대 계획

- `즐겨찾기 랭킹` 제목과 2×2 현재 경험치율을 제외한 모든 위젯 글자를 1sp 키운다.
- 작은 레벨·경험치 보조 정보는 총 2sp 키우고 한 줄 말줄임을 유지한다.
- 주간 기준일은 상승량을 계속 집계하지 않되 화면의 `(기준)` 표시는 완전히 제거한다.
- Android 회귀 테스트와 전체 테스트, 릴리스 빌드를 검증한 뒤 APK를 휴대폰으로 전송한다.

## 2026-08-28 Android 위젯 조작성·대기 모션 개선 계획

- 작은 레벨·경험치 글자를 12sp로 올리고 갱신 시각을 제외한 더 작은 글자도 12sp로 맞춘다.
- 대형 위젯의 최소 조절 크기를 3×3으로 낮추고 순위 배지와 행 여백을 축소한다.
- 대형 위젯 헤더에 새로고침 버튼을 추가해 앱을 열면서 시작 즉시 동기화를 수행한다.
- 대표 캐릭터의 NEXON `A00.0~3` 대기 프레임을 캐시하고 `ViewFlipper`로 2×2·5×2 위젯에서 반복한다.
- 애니메이션 프레임은 RemoteViews 전송 한도를 넘지 않도록 장당 160px로 제한한다.
- Android·Rust·프런트엔드 테스트와 릴리스 빌드를 검증한 뒤 APK를 휴대폰으로 전송한다.

## 2026-08-28 Android 위젯 아이콘·레이아웃 정돈 계획

- 새로고침 문자를 중앙 정렬된 벡터 아이콘으로 교체하고 대표 표기를 왕관 아이콘으로 통일한다.
- 위젯 닉네임은 한 줄 전체를 유지하면서 가용 폭에 맞춰 자동으로 축소한다.
- 대기 프레임의 공통 128×128 좌표계를 유지해 프레임별 확대율과 위치 흔들림을 제거한다.
- 이미지 없는 주간 성장 위젯의 위아래 내부 여백을 늘린다.
- Android 회귀 테스트와 전체 테스트, 릴리스 빌드를 검증한 뒤 APK를 휴대폰으로 전송한다.

## 2026-08-28 Android 위젯 축소 대응·모션 복원 계획

- 대표 왕관과 닉네임을 붙여 배치하고 소형 위젯 헤더의 불필요한 양끝 정렬을 제거한다.
- 대기 프레임 네 장의 공통 투명 외곽선을 계산해 동일 영역으로 확대하여 위치 고정과 가시적 움직임을 함께 유지한다.
- 대형 위젯 폭이 230dp 이하이면 경험치율과 획득량의 소수부를 생략한다.
- 대형 순위 번호의 원형 배경을 제거하고 번호·아바타·정보의 왼쪽 여백을 축소한다.
- Android 회귀 테스트와 전체 테스트, 릴리스 빌드를 검증한 뒤 APK를 휴대폰으로 전송한다.

## 2026-09-03 Android 위젯 백그라운드 자동 갱신 계획

- WorkManager의 최소 허용 주기인 15분 간격 고유 작업을 등록하고 네트워크 연결을 실행 조건으로 둔다.
- 앱 프로세스가 종료된 상태에서는 JNI를 통해 Rust 동기화를 실행하여 기존 Android 보안 저장소의 API 키와 SQLite 데이터를 그대로 사용한다.
- API 호출량을 제한하기 위해 대표 캐릭터와 즐겨찾기 캐릭터의 실시간·누락 일일 기록만 갱신한다.
- 갱신된 DB에서 모바일 위젯 전용 7일 스냅샷을 만들고 네 종류 홈 위젯과 캐릭터 이미지 캐시를 즉시 갱신한다.
- 예약 중복, 네트워크 오류 재시도, 앱 종료 및 기기 재부팅 후 지속 동작을 테스트한다.
- Android·Rust·프런트엔드 테스트와 릴리스 빌드를 검증한 뒤 APK를 휴대폰으로 전송한다.
