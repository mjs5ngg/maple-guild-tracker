# 구현 결정 기록

- 개인 키 저장 차단 대응을 반영했다. localStorage 예외는 읽기null/쓰기false로 변환하며 삭제 실패를 성공이라고 표시하지 않는다. finally에서도 busy를 해제하고 메모리60초 제한을 유지한다. 48개 테스트·TypeScript·Linux 웹 빌드 통과. 실제 유효키 브라우저 실증이나 대표 중복 조회 제거는 완료 범위가 아니다.

- 개인 조회의 초기 localStorage 읽기와 finally 저장이 예외를 던지면 화면/조회가 멈추는 경로를 확인했다. Sites 기존 구조 보존 지침을 적용해 저장 실패를 안내하고 메모리 조회를 허용한다. Cloudflare 로그인 화면 열기는 queued 응답이며 사용자가 보았다고 단정하지 않는다.

- 공개 접속 작업 시작. 공식 pkg.cloudflare.com의 서명 저장소에서 cloudflared2026.8.3을 Linux에 설치하고 실행 확인. tunnel.example.yml의 ingress validate, personal 호스트3101 연결·미등록호스트404 매칭 테스트 통과. 실터널 생성/실도메인 변경/외부 공개는 아직 하지 않았다. mapleexp.com 및 mapleexptracker.com은 Verisign RDAP404지만 판매처 가격·구매 가능 확정은 아니다. 구매/계정 절차가 대기해도 개인 키·신규 사용자 검증은 다음 출시 작업으로 유지한다.

- 사용자의 지적에 따라 공개 베타 출시 보드를 체크리스트 최상단의 유일한 현재 우선순위로 변경했다. 기존 완료/미완료가 섞인 목록은 이력으로 내렸다. Linux 서버는 루프백만 열려 있고 cloudflared/인증 설정은 없음을 확인했다. 공개 HTTPS 두 출처와 신규 사용자 전체 흐름을 우선하며 카카오/네이버·추가 운영 보강은 후순위다.

- Sites의 기존 구조 보존 지침에 따라 현재 React/로컬 Linux 호스팅을 유지한다. 화면 재설계나 외부 Sites 배포 없이 대시보드 조회 오류만 별도 상태로 분리한다.

- 브라우저 탭5에서 로그인된 대표 엘크라우치와 Linux 최신03:52 갱신 순위가 보임을 확인했다. 새 Google 인증 절차는 수행하지 않았다. 최신 데이터가 보이는데 상단 TypeError: Failed to fetch 잔존, src/web/main.tsx load 성공 시 setError 해제 없음 확인. 조회 오류를 저장/팝업 오류와 분리해 복구 시 조회 오류만 지우는 후속 수정이 필요하다.

- 2026-09-09 Linux 운영 주소에서 Google 시작 응답303, accounts.google.com 대상, localhost3100 callback, HttpOnly/SameSite=Lax를 비밀 출력 없이 확인했다. callback 입력 누락은400으로 거부됐다. 테스트 로그인 요청은10분 만료이며 실제 Google 인증 요청을 따라가거나 사용자 계정을 변경하지 않았다. 수집221 completed/369 성공/0 실패를 확인했다. 실제 브라우저 인증 완료와 구분한다.

- 백업 전 용량 검사 연결 완료. WSL 가상 디스크뿐 아니라 실제 C와 D 여유도 검사한다. Windows/Linux 각각2개 테스트와 실제 백업 서비스 Result=success 확인. 5GiB 기준은 MAPLE_BACKUP_MIN_FREE_BYTES로 조정 가능하다. 검사는 백업 시점에만 실행하며 디스크가 임계값 아래면 기존 백업을 삭제하지 않고 실패한다.

- 백업 공간 검사는 기본 5GiB를 환경 설정으로 변경 가능하게 한다. 매 백업 실행 전 검사이며 실시간 용량 감시/알림은 아니다. 부족하면 자동 삭제하지 않고 백업 서비스가 실패하도록 한다.

- 별도 내부 SSD의 D:\MapleEXPBackups를 새로 만들고 ACL 상속을 끊어 현재 사용자/SYSTEM/관리자만 허용했다. 정상 dump를 임시 파일에 복사하고 cmp 후 이름 변경하며 기존 파일은 동일성만 검사한다. 백업 서비스 drop-in 후처리와 해당 경로 쓰기 허용을 설치했다. 서비스 실제 실행 Result=success, 첫 아카이브의 원본/복사본 SHA-256 일치 확인. 자동 삭제는 없으며 동일 PC의 두 디스크이므로 도난/화재/랜섬웨어 전체 대비는 아니다. 용량 감시는 아직 별도 작업이다.

- 2026-09-09 저장장치 읽기 점검. C는 디스크0, D는 디스크1의 별도 내부 NVMe이며 D 여유 약509GB. E는 약4GB USB이므로 운영 백업 대상으로 선택하지 않았다. 아직 D에 데이터를 복사하지 않았다. 현재 체크리스트에 남은 전환 전 미완료 문구를 최신 실수집/백업 완료 상태로 수정하고 실제 재로그인·재부팅·별도 보관을 분리했다.

- 운영 백업 maple-20260908T173550Z-CMzaJ4.dump를 Linux17의 maple_backup_verify_20260909_0300으로 단일 트랜잭션 복원했다. 사용자1·identities1·daily_snapshots11,511·guild_daily_snapshots31 확인. archive600 및 백업 Result=success 확인. 운영 DB는 변경하지 않았다. 같은 디스크의 복원 시험이므로 장비/디스크 장애 대비 별도 보관 완료를 뜻하지 않는다. 검증 DB에도 계정 기록이 있으므로 공개하지 않는다.

- 운영 백업 서비스가 만든 29,680,651바이트 아카이브의 파일600 권한과 성공 결과를 확인했다. 같은 운영 DB로 덮어쓰지 않고 새 검증 DB에 복원하며 원본 백업과 복원 DB는 보존한다.

- Linux 전환 후 수집217이 369 성공/0 실패로 완료됐다. Windows 현재 사용자 Interactive/Limited 로그인 트리거의 Maple EXP Linux Runtime 작업을 등록했다. 숨김 PowerShell 명령으로 WSL sleep infinity 실행, 실행시간 제한 없음·중복 IgnoreNew·실패1분 재시도 설정이다. .ps1 파일 실행은 해당 PowerShell 정책으로 실패해 정책을 변경하지 않고 직접 명령 실행 방식으로 수정했으며 최종 Running/267009(실행 중), Linux active 확인. 임시로 만든 스크립트는 제거했다. 로그인 전 부팅 자동 기동과 재부팅 시험, Google 재로그인은 미검증이다.

- 2026-09-09 02:35 KST 실제 운영 전환. Windows 수집 215 completed/369 성공/0 실패 확인 후 PID4256 경로 검증·중지. linux-live-20260909-0235.dump를 생성해 Linux 17/5433 maple_live_20260909_0235에 복원했다. 운영 키/Google 설정을 stdin으로 /etc/maple-exp/server.env(root600)에 전달하고 maple-exp 서비스를 enable/start했다. Windows localhost 상태 database/collector/Google 모두 true, Linux 수집216 running 확인. 운영 백업 Result=success 및 다음04:30 타이머 활성화 확인. Python 문법 검증 통과. Windows 원본은 보존했으나 이후 데이터는 Linux에 축적되므로 단순 Windows 재가동은 금지하고 최신 역방향 이전을 먼저 검토한다. 실제 Google 재로그인과 Windows 재부팅은 아직 검증하지 않았다.

- 사용자가 실제 Linux 운영 전환을 명시적으로 요청했다. 운영자 Windows 저장 키와 Google 설정을 Linux root 전용 설정 파일로 비공개 전달하는 것을 이전 범위로 해석한다. 개인 사용자 키는 이전하지 않는다. 원본 DB와 Windows 실행 파일은 되돌리기용으로 보존한다.

- systemd 시험 도구를 추가하고 bash 문법 및 실제 임시 서비스 실행을 검증했다. 운영 구성과 같은 읽기 전용 파일시스템·일반 사용자·권한 제한·10초 재시작 설정에서 SIGKILL 후 PID 변경/NRestarts 증가/database=true/collector=false를 확인했다. trap으로 서비스 중지 후 unit not-found 및 3200 리스너 없음 확인. 첫 직접 WSL 명령의 인수 전달 오류로 실행된 별도 Linux 프로세스는 실행 경로 확인 후 종료했다. Windows 운영 프로세스는 변경하지 않았다.

- systemd 복구 시험은 별도 이름 maple-exp-rehearsal, 3200/3201, 복원 DB와 키 없는 환경으로 수행한다. Windows 운영 프로세스와 Linux PostgreSQL은 종료하지 않는다. 시험 후 임시 서비스만 중지한다.

- 병행 서버 검증에서 SERVER_PORT=3200/DIRECT_PORT=3201로 복원 DB를 연결했다. database=true/collector=false와 Google·카카오·네이버 미설정, 정적 화면 두 개 200, /api/me 401을 확인했다. 테스트 실행은 timeout 45초로 자동 종료하도록 제한했다. Windows 단위 12개(통합 7개 제외)·clippy와 Linux 통합 포함 19개·개발 빌드가 통과했다. 정적 파일은 기존 빌드이므로 별도 포트의 실제 브라우저 흐름 완료로 간주하지 않는다.

- 서버가 3100/3101에 고정되어 있어 운영 서버와 분리된 검증을 위해 SERVER_PORT/DIRECT_PORT를 추가한다. 루프백 바인딩은 그대로 두고 포트 0·잘못된 값·중복 포트는 시작 전에 거부한다. Linux 검증에는 키를 옮기지 않는다.

- 2026-09-08 PGDG에서 PostgreSQL 17.11을 설치했다. 16/main은 5432 그대로, 17/main은 5433이며 둘 다 online이다. 17에 mapledev 역할과 개발 DB를 준비하고 표본 백업/복원·서버 테스트 18개를 통과했다. Windows 실제 DB 백업 linux17-rehearsal-20260908-220111.dump를 Linux ~/.local/share/maple-migration(700)에 600 권한으로 복사해 maple_rehearsal_20260908_220111 새 DB에 단일 트랜잭션 복원했다. daily_snapshots 11,113건을 확인했다. 이 DB는 검증용 복사본이며 운영 서버·OAuth/NEXON 키·연결 설정은 변경하지 않았다. 운영 전환 시에는 새 백업과 앱 수준 검증이 필요하다.

- PostgreSQL 공식 Ubuntu 안내의 PGDG 저장소 설치 경로를 확인했다. Linux 16을 업그레이드/삭제하지 않고 17을 별도 클러스터로 준비한다. 운영 서버의 연결 주소와 키는 이 단계에서 변경하지 않는다.

- 새 DB 복원 도구의 bash 문법, Linux PostgreSQL 16 임시 DB 정상 복원, 동일 대상 재실행 거부, 잘못된 파일 거부 후 기존 복원값 유지, 파일 600 권한을 검증했다. 실제 Windows DB·자격 증명·실행 서버는 변경하지 않았다.

- 복원 도구는 기존 DB 삭제나 덮어쓰기를 지원하지 않는다. createdb 성공 시에만 단일 트랜잭션으로 복원하고 실패한 새 DB도 자동 삭제하지 않는다. Windows 운영 PostgreSQL 17과 Linux 개발 16의 차이를 확인했으므로 실제 운영 데이터 이전은 이번 도구 검증과 분리한다.

- 2026-09-08 정기 백업 구성 검증 완료. systemd-analyze verify 통과, 타이머가 04:30 KST로 해석됨을 확인했다. 기본 경로와 mktemp로 생성한 별도 경로 각각에서 파일 600 권한과 별도 임시 DB 복원이 통과했다. 임시 DB만 테스트 종료 시 삭제했고 실제 DB는 건드리지 않았다. Linux 서버 PostgreSQL 통합 포함 18개 테스트도 통과했다. 타이머 설치/활성화와 운영 DB 이전은 아직 수행하지 않았다.

- 정기 백업은 매일 04:30 KST, 놓친 실행 보충, 자동 삭제 없음으로 구성한다. Linux 원본 운영 전환 전에는 타이머를 활성화하지 않는다. 백업본은 운영 DB와 다른 디스크로도 보관해야 하며 이번 로컬 백업만으로 장비 장애 복구까지 완료라고 하지 않는다.

- 계정 탈퇴 API와 직접 문구 입력 확인 화면 구현. 계정 격리 테스트에 잘못된 문구·외부 출처·임의 user_id 거부, 본인 연결 요청/설정/세션 삭제, 타 계정/공용 캐릭터 보존, 삭제 후 세션 차단을 추가했다. PostgreSQL 포함 서버 18개·프런트 45개·clippy·웹/서버 빌드 통과 후 실행 서버 적용. 실제 사용자 계정 삭제 요청은 보내지 않았다. 제공자 사이트 OAuth 승인 철회와 브라우저 개인 키 삭제는 별도다.

- 배포 준비의 계정 탈퇴를 구현한다. 삭제 범위는 본인 users/favorites/identities/sessions/login_attempts 연결 요청이며 공용 원본 자료는 보존한다. 별도 출처의 개인 API 키는 이 서버에서 접근/삭제할 수 없으므로 그 화면의 키 삭제 기능을 안내한다. 실제 사용자 계정 삭제는 수행하지 않는다.

- 사용자가 가입 전 경험치를 제외하지 말라고 명시했다. 과거 소속 날짜 제한 정책을 철회하고 현재 길드원을 대상으로 선택 기간의 전체 획득량을 합산한다. 기존 날짜별 명단은 삭제하지 않는다. 배포 점검에서 Vite가 process.env만 읽어 .env의 PUBLIC_ORIGIN을 반영하지 않고 로컬 출처로 빌드할 수 있음을 확인해 수정한다. 공개 배포 미완료이므로 광고 적용은 아직 시작하지 않는다.

- 날짜 소속을 isGuildMember/guildMembership 응답으로 전달하고 웹 길드 기간 순위에 적용했다. 날짜 명단과 동일 OCID의 당시 이름으로 일치 여부를 판정하고, 누락은 false로 만들지 않는다. 미가입 날짜 제외·명단 누락 부분 표시·즐겨찾기 전체 집계 유지·과거 닉네임 일치·사용자별 명단 범위 테스트를 통과했다. 프런트 38개, PostgreSQL 포함 서버 18개, clippy, 웹 두 출처와 Windows release 빌드 성공 후 수집 완료를 확인하고 실행 서버를 교체했다. 과거에만 등장한 미등록 캐릭터의 신규 식별/보충은 아직 남아 있다.

- 웹 길드 순위는 현재 길드 명단으로 대상을 정하고 과거 소속 날짜만 합산한다. 날짜별 닉네임은 같은 OCID의 해당 날짜 기본 응답으로 확인하며 기록 누락을 현재 닉네임으로 추측하지 않는다. 즐겨찾기 순위·개인 그래프는 기존 전체 성장 기록을 유지한다. 기존 React/서버와 로컬 실행을 보존하는 Sites 지침을 적용하며 외부 호스팅은 하지 않는다.

- 날짜별 길드 이력 1단계 완료. Windows/Linux 각각 실제 PostgreSQL 포함 18개 테스트·clippy·release 빌드가 통과했다. Windows DB를 .local-runtime/backups의 custom archive로 백업·목록 검증 후 추가 전용 migration 0004를 적용했다. 실제 2026-08-09~2026-09-07 명단 30개와 보충 작업 0건, 실행 173번 completed/368 성공/0 실패를 확인했다. Windows 실행 바이너리는 이번 변경의 debug 버전이며 release 산출물도 빌드 완료했다. 현재 명단과 기존 원본 기록은 변경/삭제하지 않았다. 다음 단계는 기간별 순위에 날짜 명단을 연결하고 과거 등장 캐릭터를 안전하게 식별·보충하는 작업이다.

- 날짜별 길드 이력은 현재 명단 복제가 아니라 날짜 지정 guild/basic 응답을 별도 저장하는 단계부터 구현한다. 성공한 빈 명단은 저장 행이 있고 미수집은 행이 없는 것으로 구분한다. 주기당 최대 30개 날짜 명단을 활성 길드에 한해 최신 날짜부터 공정하게 처리하고 실패를 영속 재시도한다. https://openapi.nexon.com/ko/game/maplestory/?id=16 의 과거 일자 조회 안내를 확인했다. 순위 정책 연결과 과거 탈퇴자 보충은 아직 이 단계의 완료 범위가 아니다.

- 9efdb1a를 Linux 별도 체크아웃에 fast-forward로 반영하고 PostgreSQL 통합 포함 서버 17개·clippy·release 빌드를 재검증했다. Windows 키·실제 DB는 이동하지 않았다. 운영 서버는 Windows release 바이너리로 계속 실행하며 Linux 서비스 활성화는 별도 단계다.

- 대시보드 기록 조회를 캐릭터별 2N번에서 대상 한정 두 번으로 묶었다. SELECT DISTINCT ON의 기준점 정렬은 기존 자정 거리→관측 시각을 유지한다. 신규 실제 PostgreSQL 테스트가 사용자 범위, 빈 기록, 30일 경계, 캐릭터별 기준점과 동률 선택, sync.status 응답을 검증한다. 서버 17개 전체 테스트·clippy·release 빌드 통과 후 Windows 실행 서버를 release로 교체했다. 현재 길드원 368명 모두 30개 과거 기록, 활성 보충 0건이다. 남은 17건은 현재 대표/길드/즐겨찾기가 아닌 캐릭터의 보류 작업이므로 그대로 보존한다.

- 후속 실행에서 수집 상태 수정본을 Windows 개발 바이너리로 적용하고 실제 브라우저의 수집 진행 중 문구를 확인했다. NEXON OPTIONS 응답은 개인 조회 출처 127.0.0.1:3101과 x-nxopen-api-key 헤더를 허용했다. 이는 CORS 사전 요청 확인이며 유효 개인 키로 전체 브라우저 흐름을 검증한 것은 아니다. 구형 서버 부담을 줄이기 위해 대시보드당 캐릭터 수의 두 배로 반복되는 기록 조회를 묶는다. 표시/계산 규칙은 바꾸지 않는다.

- 2026-09-08 작업 흐름 점검. 사용자가 전체 완료/진행/할 일 반영 여부 확인을 요청했다. 기존 파일은 존재하지만 Google 미완료 체크와 완료 기록의 충돌, PC/Android 완료와 서버 이관 미완료의 혼재, 폐기된 알림 작업의 역사 표시 부족을 발견했다. checklist.md 상단에 현재 전체 현황을 단일 기준으로 추가하고 예전 작업은 이력으로 구분했다. 뒤의 문장은 당시 상태이므로 최신 상태를 뜻하지 않는다.
- 같은 점검에서 실제 DB는 현재 길드 370명/캐릭터 370명, 과거 스냅샷 6,000건/보충 5,100건, 최근 실행 153번 completed(370 성공/0 실패)를 확인했다. 첫 수집 완료와 전체 30일 보충 완료를 분리한다. 수집 상태 표시 수정은 프런트 37개·서버 단위 11개·TypeScript·웹/서버 개발 빌드 통과했고 실행 바이너리 교체는 아직 하지 않았다.

- 실제 첫 수집에서 sync_runs.status는 running이지만 웹 API가 status를 생략하고 화면은 finishedAt이 없으면 수집 대기로 표시했다. 실행 상태를 전달하고 상태별 문구를 사용한다. 수집 중 성공·실패 건수는 아직 확정되지 않으므로 0건 완료처럼 표시하지 않는다.

- 2026-09-08 후속 검증. 사용자가 추가 발급한 Google 비밀 키를 일회용 입력 도구로 로컬 .env에 저장했고, 실제 Google 동의 화면에서 승인 후 인증된 대시보드로 복귀했다. 이전 항목의 연결 미완료 상태는 해소됐다. 대표캐릭터 엘크라우치를 저장하고 서버 첫 수집에서 스카니아/아이엠캔들 길드를 확인했다. 비밀 키는 출력하거나 Git에 저장하지 않았다. 공개 서비스·다른 소셜 로그인 연결은 아직 별도 작업이다.

- 2026-09-08. 일회용 폼의 no-referrer가 브라우저 POST Origin을 null로 만드는 문제를 same-origin 정책으로 수정했다. null·외부 출처 거부는 유지하고 실제 브라우저에서 입력 검증 단계까지 진입함을 확인했다. 서버 연결 전 대화 중단으로 최초 키 표시 탭이 정리되어 키를 저장하지 못했다. Google 클라이언트 Additional information에서 기존 키 재열람 불가와 Add secret 버튼을 확인했다. 키 변경 단계는 사용자 조작으로 넘기며 발급된 새 창을 닫지 않도록 안내한다. 실제 서버 Google 연결은 여전히 미완료다.

- 사용자 승인을 받아 Google 브랜딩과 Maple EXP Local Web 클라이언트를 생성했다. 복귀 주소는 http://127.0.0.1:3100/auth/google/callback 하나다. JSON 다운로드 이벤트가 발생하지 않아 브라우저의 실제 표시값을 로컬 일회용 입력 도구로 전달한다. 비밀을 도구 출력·채팅·Git에 표시하지 않으며 기존 .env에 Google 두 항목만 갱신한다.

- Google 콘솔 로그인 후 기존 My First Project에 블로그 OAuth가 있음을 확인하여 변경하지 않았다. 별도 Maple EXP Tracker 프로젝트(fiery-nimbus-507909-e9)를 생성했다. OAuth 브랜딩 폼에 앱 이름을 입력했으나 사용자 지원 이메일의 로그인 사용자 대상 노출 승인을 받기 전이므로 저장·OAuth 클라이언트 생성은 수행하지 않았다. 결제나 유료 리소스는 연결하지 않았다.

- 수집 회귀 테스트에서 실패 대상 요청 횟수가 3회로 재현되었으며 수정 후 1회가 됐다. 대표 변경 이름으로 구독을 재조회하여 같은 주기에 길드원을 수집함을 확인했다. Linux 전체 16개(통합 5개 포함), clippy, Windows 단위 11개 및 양 OS release 빌드가 통과했다. Windows debug 빌드는 실행 중 exe 잠금으로 실패하여 실행 파일을 지우지 않고 release 빌드로 검증했다. OWNER-HANDOFF.md에 외부 등록 대행 절차와 사용자 인증·구매 승인 경계를 기록했다.

- 사용자는 개발 진행과 외부 서비스 준비 대행을 원하며 직접 조작을 최소화하도록 요청했다. 계정 인증·본인 확인·유료 결제·공개 배포 승인만 남기는 절차를 준비한다. 현재 확인한 수집기 결함은 실패한 대표 조회가 같은 주기에서 반복되는 점과 닉네임 저장 직후 구독 계산이 이전 이름을 사용하는 점이다. 먼저 재현 테스트로 고친다.

- 공통 웹 빌드를 Windows/Linux에서 검증했고 기존 34개 프런트 테스트가 통과했다. Linux release 빌드와 systemd 구성 검증, 백업 파일 600 권한 및 임시 DB 복원(한글·큰 정수) 테스트가 통과했다. 실제 Windows 서버는 database=true, collector=true를 유지한다. 서비스는 아직 활성화하지 않았으며 정기 백업·Windows 부팅 연동·실제 OAuth 연결은 남아 있다.

- Linux 배포를 재현 가능하게 만드는 다음 단위로 공통 웹 빌드, systemd 구성, DB 백업·복원 검증을 진행한다. Windows 서버를 몰래 빈 Linux DB로 교체하지 않고, 서비스 구성은 활성화하지 않는다. 사용자 소셜 인증 정보 없이 인증 우회 계정을 만들지 않는다.

- 재부팅 후 WSL2의 가상화 오류가 해소되었고 Ubuntu-24.04 설치를 시작했다. Linux에는 별도 개발 DB를 사용하여 Windows 원본 DB를 변경하지 않는다.

- 승인된 기존 앱 키를 keyring windows-native로 서버 메모리에서만 읽도록 연결했다. .env에는 비밀 값 대신 NEXON_USE_WINDOWS_CREDENTIAL=1만 추가했다. 실제 Nexon 캐릭터 식별자 조회 HTTP 200을 확인했고 서버 상태는 collector=true, database=true다.
- WSL 2.7.13을 설치하고 관리자 승인으로 VirtualMachinePlatform을 활성화했다. Windows가 RestartNeeded=True를 반환했다. 펌웨어 가상화와 SLAT는 활성화되어 있으며 Ubuntu는 아직 등록되지 않았다. 사용자의 작업을 보호하기 위해 자동 재부팅하지 않았다.
- 재부팅 후 scripts/install-wsl.ps1로 Ubuntu-24.04 설치를 이어가고 Linux 환경을 구성한다. Windows 자격 증명은 Linux에 복사하지 않았으며 현재 서버는 Windows에서 실행한다. 소셜 제공자 발급 정보는 미설정이다.
- 서버 단위 11개, 실제 PostgreSQL 통합 4개 및 clippy가 통과했다. 비밀 키를 출력하거나 Git에 추가하지 않았다.

- 사용자가 기존 PC 서비스 키의 중앙 수집 재사용과 Linux 환경 구성을 승인했다. Windows는 유지하고 WSL2/Ubuntu를 우선한다. 현재 프로세스는 관리자 권한이 아니며 WSL이 설치되지 않은 상태다.

## 후속 작업 범위

- 사용자 발급 정보가 없어도 검증 가능한 중앙 수집기의 무한 실패 반복·과거 보충 편중을 우선 수정한다.
- 닉네임이 같아도 OCID가 다르면 소유자 변경일 수 있으므로 과거 기록을 자동 병합하지 않는다. 동일 OCID의 이름 변경만 자동 추적한다.
- 기존 로컬 앱 데이터와 실제 사용자 기록 삭제, 외부 공개는 수행하지 않는다.
- 보충 작업과 마지막 처리 캐릭터 정보를 DB에 보관하여 재시작·실패 시에도 이어서 공정하게 처리한다. 재시도는 30분부터 최대 6시간까지 지연한다. 기존 원본은 삭제하지 않는다.
- 동일 OCID 이름 변경은 대표·즐겨찾기를 갱신하고 이전 이름과 관측을 보존한다. 다른 OCID 이름 충돌은 자동 병합하지 않고 저장을 중단한다.
- 수집용 advisory lock 연결을 풀에서 분리하여 취소 시 잠긴 연결이 풀로 돌아가지 않게 한다. 같은 주기의 길드 조회도 캐시로 중복 제거한다.
- 서버 단위 11개 및 실제 PostgreSQL 통합 4개 테스트가 통과했다. 통합 테스트에는 로컬 모의 Nexon HTTP 서버의 429·수집 중복 제거·미이용 제외·보충 재개 검증이 포함된다. 실제 Nexon 및 소셜 로그인 발급 정보 검증은 여전히 남아 있다.

## 2026-09-07 웹 개발 시작

- 추가 정책 질문 없이 권장안으로 진행한다. 테스트는 현재 Windows PC, 외부 공개는 하지 않는다.
- 소셜 로그인은 제공자 고유 ID로 식별하며 이메일 자동 병합을 금지한다. 키는 계정 동기화하지 않는다.
- 7일은 168시간, 화면 사용·수동 조작만 활동으로 기록한다. 자동 폴링은 활동을 연장하지 않는다. 대표 1명·즐겨찾기 30명 한도다.
- 브라우저 직접 조회값은 공용 DB에 반영하지 않는다. 키 기억하기는 사용자 선택, 광고·분석 코드와 출처를 분리한다.
- 현재 PostgreSQL/Docker/실행 가능한 WSL 배포판을 확인하지 못했다. 서버 DB 검증과 실제 소셜 로그인은 별도 준비가 필요하다. 이를 가짜 성공이나 익명 로그인 우회로 대체하지 않는다.
- 이후 공식 EDB PostgreSQL 17.11 바이너리를 프로젝트 .local-runtime 아래에 준비하고 전용 로컬 DB를 생성했다. 시스템 서비스·방화벽·기존 앱 DB는 변경하지 않았다. 개발 DB와 비밀 설정은 Git에서 제외한다.
- 서버 10개 단위 테스트, 별도 실제 PostgreSQL 통합 테스트 1개, 웹 포함 프런트엔드 34개 테스트가 통과했다. TypeScript·두 웹 빌드·Rust clippy가 통과했다.
- 127.0.0.1:3100 웹과 3101 개인 키 출처의 HTTP 200, 실제 DB 연결 상태를 확인했다. 운영자 키와 소셜 발급 정보는 아직 없어서 수집기·로그인은 미설정 상태다.
- 서버 코드와 웹은 초기 개발판이다. 실제 OAuth·Nexon 브라우저 호출·장시간 부하 및 WebMCP 실행은 검증하지 않았다. 원본 삭제 없이 날짜별 길드 이력·식별자 변경 이관과 Android/PC 서버 연결을 후속 항목으로 남겼다.
- Sites 스킬의 로컬 미리보기·설정 대기 표시·출처 분리를 적용했다. 사용자 요청대로 외부 Sites 등록·호스팅은 하지 않았고 기존 React 및 로컬 Rust/PostgreSQL 구성을 유지했다.

## 2026-09-05 사용자 작업 가이드

- 계산 오류는 수정하되 자정 근처 날짜 경계는 수집 시각으로 추정 표시하고 전일 API 데이터 확보 후 보정한다. 경계 오차·보정 가능성을 설정에 안내한다.
- 오늘 획득량 0으로 평균이 내려가는 정책은 명시적 사용자 선택이므로 유지한다. 대기 모션·위젯 디자인은 사용자 확인 이후로 보류한다.
- 사용자 답변에 따라 최근 7일·30일은 오늘을 포함한다. 획득량 동점은 레벨 → 현재 경험치 → 닉네임으로 PC·앱·위젯에 통일한다. 7일 성장 위젯 첫날은 기준점으로 유지한다.
- 연속하지 않는 스냅샷의 증가량은 일간으로 취급하지 않는다. 원본을 보존하고 누락 보충 후 파생값을 재계산한다. 부분 수집 상태를 구분하고 응답 수신 시각을 저장한다.
- Rust 30개, 프런트엔드 27개, Android 단위 테스트와 clippy가 통과했다. v0.3.13 Windows NSIS·포터블 및 Android ARM64 릴리스 빌드가 성공했다. 실제 기기 실행 검증은 하지 않았다.
- APK를 CrossDevice의 민제의 S24+ (3)/storage/Download로 복사했고 원본과 SHA256 일치를 확인했다.
- 중앙 서버의 로그인·한도·상업 이용 제약과 단계별 전환안은 SERVER-TRANSITION-REVIEW.md에 기록했다. 추가 키가 동일 애플리케이션 한도를 늘리지는 않는다.
- 중앙 서버·광고·멤버십은 검토 요청이다. 현재 앱의 데이터 삭제나 운영 서버 공개·결제 구현은 하지 않는다.

## 2026-08-19

- 신규 프로젝트로 시작한다. 상위 작업 폴더는 Git 저장소가 아니다.
- 지원 범위는 한국 메이플스토리, Windows 10·11 x64, 개인 로컬 사용으로 한정한다.
- 환산, HEXA 환산, 전투력은 다루지 않고 환산주스탯 사이트에는 요청하지 않는다.
- 대표 캐릭터의 월드와 길드는 NEXON Open API 응답에서 확인한다.
- 최초 동기화는 완료된 31개 날짜 스냅샷을 조회하여 30개 일간 증가량을 만든다.
- 일일 기준은 KST이며 오전 2시 15분 이후 전일 데이터를 동기화한다.
- API 키는 Windows 자격 증명 관리자에 보관하고 SQLite·설정·로그에는 기록하지 않는다.
- 프런트엔드는 React와 TypeScript, 네이티브 계층은 Tauri 2와 Rust, 저장소는 SQLite를 사용한다.
- 경험치표는 버전과 체크섬을 저장한다. 지원하지 않는 레벨이나 역행 데이터는 계산하지 않는다.
- Tauri 공식 autostart 플러그인과 내장 트레이 기능을 사용한다.
- 경험치표는 KMS New Age 기준 200~299레벨을 고정 데이터로 포함한다. 이 범위를 벗어나면 추정하지 않고 `계산표 갱신 필요` 상태로 남긴다.
- NEXON 오류 응답을 인증, 호출 제한, 데이터 준비 중, 점검, 네트워크 오류로 분류하고 최대 동시 호출 수를 5개로 제한한다.
- 앱 시작 시 누락 날짜를 확인하고, 실행 중에는 15분마다 완료 기준일과 동기화 상태를 확인한다.
- 대시보드와 `--widget` 실행 모드를 실제 Windows WebView에서 확인했다. 위젯의 `나＋즐겨찾기`와 `길드 상위` 전환도 확인했다.
- `cargo test` 9건과 프런트엔드 테스트 4건이 통과했으며, `cargo clippy --all-targets -- -D warnings`, TypeScript 빌드, 릴리스 빌드가 통과했다.
- NSIS 설치 범위는 관리자 권한이 필요 없는 현재 사용자로 설정했다.
- 실제 NEXON 계정 데이터 동기화는 사용자 API 키가 제공되지 않아 실행하지 않았다. 최초 설정 단계에서 키와 대표 캐릭터를 검증하도록 구현했다.
- API 키는 별도 파일을 요구하지 않고 앱 최초 설정 화면에서 직접 입력한다.
- 실제 NSIS 첫 화면을 확인한 결과 기본 언어가 영어였으므로 설치 언어를 한국어로 고정했다.
- 배포 편의를 위해 관리자 권한 없는 NSIS 설치본과 설치 없이 실행 가능한 포터블 실행 파일을 최상위 `release` 폴더에 함께 생성한다.
- 첫 실사용에서 355명 길드의 신원 연결 완료 시 진행률이 100%로 보인 뒤, 31일치 캐릭터 요청 약 1만 1천 건을 메모리에 모두 모을 때까지 화면이 갱신되지 않는 문제를 확인했다.
- 개발 단계 API 키의 일일 1,000회 제한을 고려하여 각 응답을 즉시 저장하고, 429 응답 시 수집 대기로 종료하며, 다음 실행에서 이미 저장한 스냅샷을 건너뛰고 이어받도록 수정한다.
- 수정 후 Rust 테스트 10건, 프런트엔드 테스트 4건, Clippy 경고 0건과 Windows 릴리스 패키징을 확인했다.
- 서비스 단계 키로 변경할 수 있도록 기존 대표 캐릭터 조회로 새 키를 검증한 뒤 Windows 자격 증명 관리자 값만 교체한다.
- 캐릭터 외형은 NEXON CharacterBasic의 `character_image` URL을 사용하며, SQLite에는 URL만 저장하고 이미지 파일을 별도로 복제하지 않는다.
- 기존 DB에는 마이그레이션으로 `characters.image_url`을 추가하며, 최신 완료일 기본 정보로 외형 URL을 갱신한다.
- 키 교체·이미지 수정 후 Rust 테스트 11건, 프런트엔드 테스트 4건, Clippy 경고 0건과 Windows 릴리스 패키징을 확인했다.
- 정식 사용자 표시 명칭은 `길드원 따라가기`로 확정한다. 내부 패키지명, 앱 식별자와 자격 증명 서비스명은 기존 설치 데이터 호환을 위해 유지한다.
- 공개 저장소 주소는 `https://github.com/mjs5ngg/maple-guild-tracker`로 생성했다.
- 실사용 DB를 확인한 결과 길드원 382명의 길드 멤버십은 31일치지만 캐릭터 스냅샷은 `2026-08-19` 한 날짜만 있고 경험치 증가량은 0건이었다. 최신 날짜만 채워지면 과거 보충을 건너뛰는 조기 종료 조건이 `자료 없음`의 원인이다.
- NEXON Open API 데이터는 평균 15분 지연되므로 초 단위 실시간 사냥 판정은 불가능하다. 화면은 30초마다 로컬 데이터를 다시 읽고, 최신 API 표본은 기본 5분 간격으로 수집하되 사용자에게 지연 특성을 명시한다.
- 활동 중 판정은 최근 두 최신 API 표본 사이에서 경험치가 증가했고 마지막 표본이 20분 이내일 때로 한정한다.
- 캐릭터 걷기 표시는 NEXON 공식 이미지 URL의 `action=A02`와 `action=A03` 프레임을 교대로 사용한다.
- 과거 누락 검사는 최신 날짜만 보지 않고 날짜별 길드 멤버십과 캐릭터 스냅샷의 결손을 직접 비교한다. 기존 실사용 DB에서는 보충 시작일 `2026-07-20`과 누락 10,853건이 탐지되는 것을 읽기 전용 쿼리로 확인했다.
- 최신 활동 표본은 현재 길드원과 즐겨찾기를 대상으로 최대 동시 5개씩 수집한다. 429 등으로 과거 보충이 실패하면 최신 표본 수집을 이어서 호출하지 않는다.
- 개선 후 Rust 테스트 13건, 프런트엔드 테스트 6건, Clippy 경고 0건과 TypeScript 프로덕션 빌드가 통과했다.
- 개선본 NSIS 설치 파일과 포터블 실행 파일을 `Guildmate-Follow-Setup.exe`, `Guildmate-Follow-Portable.exe`로 다시 생성했다.
- 공개 GitHub 릴리스 `v0.1.0`을 게시하고 설치형과 포터블 파일이 포함된 자산 4개를 공개 페이지에서 확인했다.
- `v0.1.0` 실사용 DB에서 31일 스냅샷 11,762건이 저장됐지만 `xp_deltas`는 0건이고 여러 `sync_runs`가 running 상태로 남는 현상을 확인했다.
- 원인은 닉네임 변경 캐릭터의 동일 OCID가 기존 ID와 새 이름 ID에 갈라진 것이다. 새 이름 행에는 OCID 연결이 없어 마지막 날짜 한 건을 계속 재수집했다.
- OCID 소유 캐릭터를 기준으로 중복 행의 스냅샷·길드 소속·대표·즐겨찾기를 병합하고, 비정상 종료된 실행을 다음 동기화에서 failed로 정리한다.
- 캐릭터 수집 100% 이후에는 별도의 `경험치 계산 n/전체` 진행 상태를 표시한다.
- `v0.1.1`은 Rust 테스트 14건, 프런트엔드 테스트 6건, Clippy 경고 0건과 프로덕션·NSIS 빌드를 통과했다.
- GitHub 릴리스 `v0.1.1`을 최신 릴리스로 게시하고 설치형·포터블 자산을 확인했다.

## 2026-08-22

- NEXON 공식 액션 정의와 달리 기존 구현은 별도 액션인 `A02`의 walk1과 `A03`의 walk2를 애니메이션 프레임처럼 교대했다.
- 실사용 캐릭터 이미지로 확인한 결과 `A03.0`은 무기를 든 다른 자세로 보이며 `A02.0~A02.3`이 한 걷기 액션의 연속 프레임이다.
- 활동 중 판정은 최근 20분 안에 수집된 두 최신 표본 사이에서 계산 가능한 경험치가 1 이상 증가했는지를 사용한다. 별도의 최소 경험치 임계값은 없다.
- 기존 화면 비율은 선택 기간 획득량을 현재 레벨 필요 경험치로 나눈 파생값이었다. 사용자 요구에 맞춰 NEXON `character_exp_rate`를 현재 비율로 사용하고 오늘 획득량을 별도로 계산한다.
- 실사용 DB의 대표 캐릭터 최신 원본은 레벨 281, 경험치율 `33.757`이며 최신 표본과 `2026-08-21` 완료 스냅샷의 레벨·경험치가 같았다.
- 대시보드 캐릭터 칸은 58px 기준에 95~225% 배율, 새 기본값 115%를 적용한다. NEXON 이미지 캔버스는 128×128로 좁혀 칸 안에서 캐릭터가 더 크게 보이게 한다.
- `v0.1.2`는 Rust 테스트 16건, 프런트엔드 테스트 8건, Clippy 경고 0건과 TypeScript·Tauri NSIS 빌드를 통과했다.
- `Guildmate-Follow-Setup.exe`와 `Guildmate-Follow-Portable.exe`의 제품명과 파일 버전이 각각 `길드원 따라가기`, `0.1.2`인지 확인했다.
- 기존 `나와의 격차`는 현재 캐릭터 위치 차이가 아니라 선택 기간 획득량에서 대표 캐릭터의 선택 기간 획득량을 뺀 값이었다. 일간 파생값은 레벨업을 고려하지만 서로 다른 현재 레벨 간 누적 위치는 비교하지 않았다.
- 후속 개선에서는 순위 기준과 격차 기준을 분리한다. 순위는 선택 기간 획득 경험치, 격차는 현재 레벨·현재 경험치를 경험치표로 연결한 대표 캐릭터 대비 성장 위치 차이를 사용한다.
- Tauri 공식 `setTheme` API로 각 창의 네이티브 테마를 변경할 수 있음을 확인했다. React 테마 변경과 함께 호출하여 Windows 제목 표시줄을 맞춘다.
- 2026년 3월 19일 KMS 1.2.413 업데이트에서 210~259레벨 필요 경험치가 감소했다. 현재 성장 위치 격차 계산을 위해 해당 구간의 현재 Non-GMS 경험치표로 버전을 갱신한다.
- 캐릭터 이미지는 기존 배율 조절과 별도로 원본 캔버스를 2.2배 확대해 카드의 잘린 영역 안에 더 꽉 차게 표시한다. 전체 화면 기본 배율은 기존 106%에서 두 단계 높은 110%로 변경했다.
- 외부 즐겨찾기의 대표 캐릭터 카드는 `대표캐릭터` 타원 배지를 표시하며, 클릭 또는 키보드 입력 시 검색을 해제하고 순위표 대표 행을 `scrollIntoView(block: center)`로 이동한다. 브라우저 기본 스크롤 범위에서 제한되므로 위아래에 인위적인 공백을 만들지 않는다.
- 그래프 선 색상은 표시 순서 대신 캐릭터 내부 ID의 결정적 색상 함수로 정한다. 새 즐겨찾기를 추가해도 기존 캐릭터의 색상은 유지된다.
- 다크·라이트 테마는 웹 콘텐츠, 스크롤바와 Tauri 네이티브 창 테마에 함께 적용하며 대시보드에서 전환한다. 미니 위젯은 저장된 테마 변경을 따라간다.
- 새 앱 아이콘은 투명 배경의 주황·빨강 단풍잎을 새로 생성해 Tauri 전체 아이콘 세트로 변환했다. 생성 원본은 `src-tauri/icons/maple-leaf-v2.png`에 보관한다.
- `v0.1.3`은 Rust 테스트 18건, 프런트엔드 테스트 10건, Clippy 경고 0건, TypeScript 프로덕션 빌드와 Tauri NSIS 빌드를 통과했다.
- `Guildmate-Follow-Setup.exe`와 `Guildmate-Follow-Portable.exe`의 제품명은 `길드원 따라가기`, 파일 버전은 `0.1.3`으로 확인했다.
- 오늘자 성장 그래프 값은 순위표의 `today_exp`와 동일하게 최신 완료일 스냅샷부터 최신 API 표본까지의 증가량을 사용한다. 계산 가능한 최신 표본이 없으면 0으로 추정하지 않고 점을 추가하지 않는다.
- 외부 즐겨찾기는 순위표에 포함되지만 현재 길드원 행이 아니므로 카드 클릭 이동을 제공하지 않는다. 별 해제는 카드 안에서 즉시 저장하고 다시 조회해 목록에서 제거한다.
- `v0.1.4`는 Rust 테스트 20건, 프런트엔드 테스트 10건, Clippy 경고 0건과 TypeScript·Tauri NSIS 빌드를 통과했다.
- `Guildmate-Follow-Setup.exe`와 `Guildmate-Follow-Portable.exe`의 제품명은 `길드원 따라가기`, 파일 버전은 `0.1.4`로 확인한다.
- 첨부 화면에서 창 미리보기 아이콘은 새 단풍잎이지만 작업 표시줄 고정 아이콘은 이전 자산이다. 실행 파일 리소스와 번들 아이콘을 명시하고, 같은 설치 경로를 기억하는 Windows 고정 바로가기 캐시도 갱신될 수 있도록 실행 파일 이름 변경 여부를 검토한다.
- 새 캐릭터 이미지 100%는 이전 화면 조절값 150%와 같은 물리 크기로 정의한다. 저장 키를 새 버전으로 분리해 기존 150% 설정이 중복 확대되지 않게 한다.
- `오늘 획득`은 최신 완료일 스냅샷과 최신 실시간 API 표본의 차이만 사용한다. `daily` 완료 기록의 전일 획득량과 혼용하지 않는다.
- 작업 표시줄의 이전 고정 아이콘 캐시를 끊기 위해 메인 실행 파일 이름을 `maple-guild-tracker.exe`에서 `guildmate-follow.exe`로 변경했다. 번들 아이콘 목록을 명시하고 NSIS 설치 완료 시 `SHChangeNotify`로 Windows 셸에 아이콘 갱신을 알린다.
- 캐릭터 이미지 표시값 100%는 CSS 물리 배율 150%로 환산한다. 대시보드와 위젯은 `display-settings-v3`를 사용하며 Tauri 전역 이벤트로 열린 위젯에 즉시 전달한다.
- 배율 팝업은 확대되는 `.app-shell` 밖의 `document.body` 포털로 렌더링하고 열 때 화면 좌표를 한 번만 계산한다. 슬라이더 조작 중 팝업 자체의 위치·크기는 변하지 않는다.
- 대시보드 `경험치` 모드는 레벨과 현재 경험치 순서, `기간별 경험치` 모드는 기존 선택 기간 획득량 순서다. 기간 모드의 열 값은 `gained_exp`, 전체 모드와 위젯 괄호 안 값은 `today_exp`를 사용한다.
- 위젯 새로고침은 로컬 재조회 전에 최신 NEXON API 표본 수집을 실행한다. 오늘 랭킹과 전체 성장 위치 랭킹을 별도로 저장한다.
- 그래프 형식은 `smooth`, `line`, `bar` 세 가지이며 `chart-kind:<기간 키>`로 기간별 저장한다.
- `v0.1.5`는 Rust 테스트 20건, 프런트엔드 테스트 12건, Clippy 경고 0건과 TypeScript·Tauri NSIS 빌드를 통과했다.
- `v0.1.6`에서는 대시보드와 위젯의 세로·가로 스크롤바 두께를 10px에서 20px로 두 배 확대한다.
- `v0.1.7`에서는 대시보드 우측 하단의 고정 버튼으로 화면 최상단까지 부드럽게 이동한다. 버튼은 화면 배율의 영향을 받는 `.app-shell` 밖에 둔다.
- `v0.1.8`의 일간 성장 그래프는 최근 완료일 점을 제외하고 그 이후의 오늘 실시간 점만 표시한다. 오늘 점을 계산할 수 없으면 어제 값을 대신 표시하지 않는다.
- 랭킹 불일치 원인은 메인 길드 랭킹이 `data.rankings` 전체를 사용해 외부 즐겨찾기를 포함한 반면 위젯 길드 모드는 `is_current_member`만 사용한 점과, 두 화면의 기본 정렬 기준이 달랐던 점이다.
- 정상화 후 메인 길드 랭킹과 위젯 길드 상위는 공통 길드원 필터를 사용한다. 외부 즐겨찾기는 위젯 `나＋즐겨찾기`와 즐겨찾기 카드에만 남긴다.
- `v0.1.9`에서는 위젯 랭킹 저장 키를 새 버전으로 분리해 최초 정렬을 메인과 같은 전체 경험치로 맞춘다. 프런트엔드 테스트 14건 중 외부 즐겨찾기 제외와 공통 정렬 기준을 검증한다.
- NEXON 공식 사전 준비 안내 기준으로 서비스 단계는 서비스명, 개발 환경, URL 정보, 소개와 대표 이미지 입력이 필요하다. 유효한 서비스 URL과 API 활용 목적을 모두 기재하면 등록 완료 후 키가 자동 발급된다.
- 서비스 등록용 공개 URL은 `https://github.com/mjs5ngg/maple-guild-tracker`를 사용한다. API 키 값 자체는 공개 저장소·이슈·스크린샷에 넣지 않도록 안내한다.
- `v0.1.10`은 설정창 도움말에서 NEXON Open API 주소, 서비스 URL, 서비스명, 개발 환경, 소개와 API 활용 목적을 각각 클립보드로 복사할 수 있게 한다.
- 스크롤바 20px 지정이 보이지 않은 원인은 최신 WebView2가 `scrollbar-width: thin`을 지원하면서 WebKit 픽셀 너비보다 얇은 표준 너비를 우선 적용할 수 있기 때문이다. `v0.1.11`에서는 이를 `auto`로 되돌리고 문서와 전체 내부 스크롤바에 20px을 명시한다.
- 대시보드의 `daily` 조회는 `gained_exp`를 최근 완료일의 `xp_deltas`에서 읽어 어제 값을 사용하지만, 위젯은 `today_exp`로 별도 정렬한다. 일간 기간별 경험치에서는 백엔드 단계에서 `gained_exp`를 `today_exp`로 통일해 요약·순위·표시가 같은 오늘 기준을 쓰게 한다.
- 기존 사냥 중 판정은 마지막 두 실시간 표본만 비교하므로 같은 API 값이 연속 저장되면 직전 20분 안에 증가 기록이 있어도 즉시 꺼진다. 최근 20분 범위의 최초·최신 표본 사이에 양의 증가가 있는지를 판정 기준으로 사용한다.
- `v0.1.12`에서는 일간 요약과 기간별 랭킹을 `today_exp`로 통일하고, 대시보드 열기 명령에 최소화 해제를 추가한다. 일간 카드의 보조 문구에서는 어제 선택 기간 값을 제거한다.
- 2026-08-26 요청에 따라 휴대폰 알림 기능은 부분 보완이 아니라 전면 폐기한다. 기존 DB의 알림 테이블만 파괴적 삭제를 피하기 위해 미사용 상태로 보존한다.
- Android 홈 위젯은 별도 API 호출이나 API 키 접근을 하지 않는다. 앱이 로컬 대시보드를 읽을 때 대표·즐겨찾기 데이터와 공개 캐릭터 이미지를 Android 전용 캐시에 기록하고 위젯은 그 캐시만 표시한다.
- 대형 위젯의 랭킹 기준은 오늘 실시간 획득량 내림차순이며 동률은 레벨과 현재 경험치율로 정렬한다. 대표 캐릭터는 즐겨찾기 해제 불가 구조와 별개로 항상 포함한다.
- 사용자가 말한 1×2는 가로 2칸·세로 1칸, 2×2는 가로 2칸·세로 2칸으로 해석한다. 두 소형 위젯은 `resizeMode=none`으로 고정한다.
- `v0.3.0` Android APK의 최종 매니페스트에는 인터넷 권한만 남고 알림·전경 서비스 권한과 알림 서비스가 없다. 즐겨찾기 대형, 대표 1×2, 대표 2×2 공급자 세 개가 등록된다.
- 대형 위젯은 높이 290dp 미만에서 3명, 290dp 이상에서 4명, 360dp 이상에서 최대 5명을 표시한다. 이는 참고 앱의 반응형 행 수 구조만 활용한 것이며 색상·정보 배치·그래픽은 새로 구성했다.
- 통합 배포 결과 Windows 설치본·포터블 실행 파일과 서명된 Android ARM64 APK가 `release`에 생성됐다. ADB에는 연결된 기기가 없어 이번 실행에서는 기기 복사를 생략했다.
- 2026-08-27 사진의 대형 위젯 행이 비정상적으로 커진 직접 원인은 반복 행에 `layout_weight=1`을 준 구조다. 목록 수와 무관한 고정 높이 행과 실제 스크롤을 위해 `ListView`·`RemoteViewsService` 컬렉션 위젯으로 교체한다.
- 새 3×2 위젯의 최근 일주일 경험치는 현재 경험치율과 최근 7일 일별 획득 흐름으로 해석한다. 총 획득량은 같은 7일 데이터의 합계로 표시한다.
- Android 홈 화면 격자 크기는 런처마다 다르므로 3~5칸 제한은 `targetCellWidth=3`, `minResizeWidth`, `maxResizeWidth`, 가로 전용 `resizeMode`를 함께 사용하며 런처가 제공자 제한을 존중하는 범위에서 적용한다.
- 본문의 이동 대상 순서는 성장 기록 `history`, 길드 순위 `ranking`이므로 왼쪽 메뉴도 같은 순서를 사용한다.
- 즐겨찾기 순서는 캐릭터 ID 배열을 로컬 저장소에 보관한다. 카드 전체가 아니라 시각적으로 분리된 손잡이만 `draggable`로 지정해 기존 카드 클릭 이동과 별 해제를 방해하지 않는다.
- `v0.1.13`은 왼쪽 메뉴 순서, 손잡이 전용 즐겨찾기 재배치와 최초 설정 API 발급 도움말을 포함한다. 즐겨찾기 정렬 함수는 저장된 순서와 새 캐릭터 후순위 배치를 자동 테스트한다.
- `v0.1.13`의 즐겨찾기 재배치는 HTML5 `dragstart/drop`에 의존해 WebView에서 잡기 커서만 보이고 드롭 이벤트가 이어지지 않을 수 있다. 포인터 이동 중 대상 카드 ID를 읽어 즉시 순서를 바꾸는 방식으로 교체한다.
- `v0.1.14`에서는 손잡이의 `pointerdown`과 전역 `pointermove`, `pointerup`, `pointercancel`을 사용한다. 누른 채 다른 카드 위로 이동할 때 즉시 순서를 저장하며 드래그 중 목록 전체에 잡는 커서를 유지한다.
- 즐겨찾기 기본 정렬은 레벨 내림차순이며 같은 레벨은 현재 레벨 경험치 내림차순, 닉네임 오름차순으로 고정한다. 저장 순서가 없으면 기본 정렬을 사용하고 실제 배열이 기본값과 다를 때만 정렬 버튼을 활성화한다.
- `v0.1.15`에서는 즐겨찾기 제목 옆의 정렬 버튼으로 저장된 사용자 순서를 제거한다. 기존 사용자가 이미 드래그 순서를 저장한 경우에는 그 순서를 보존하면서 버튼을 활성화해 직접 기본 레벨순으로 돌아갈 수 있게 한다.
- 연속 드래그가 막힌 원인은 재배치로 대상 캐릭터가 다른 위치로 밀려난 뒤에도 `favoriteDropTargetRef`가 그 캐릭터 ID를 기억해 재진입을 무시했기 때문이다. 드래그 중인 카드 위를 다시 지날 때 잠금을 초기화해야 같은 대상을 새 위치에서 다시 사용할 수 있다.
- `v0.1.16`에서는 드래그 중인 카드가 포인터 아래에 들어오면 강조 대상과 마지막 대상 ID를 초기화한다. 따라서 같은 캐릭터가 재배치로 다른 칸에 밀려나도 다시 정상적인 이동 대상으로 인식된다.
- Android판은 기존 로컬 데이터 모델과 NEXON API 계산을 공유한다. 데스크톱 전용 트레이·항상 위 위젯·로그인 자동 시작은 Android 기본 앱에서 제외하며 모바일 메인 대시보드에 집중한다.
- 공식 Tauri Android 빌드는 Android Studio의 SDK Platform, Platform-Tools, NDK, Build-Tools와 Command-line Tools 및 Rust Android 대상을 요구한다. 확인 결과 PC에 Android Studio, SDK 36, NDK 27과 Platform-Tools가 설치되어 있었고 환경 변수만 비어 있어 빌드 스크립트에서 설치 경로를 자동 연결한다.
- Android 최소 지원 버전은 Tauri 공식 최소값인 Android 7.0 SDK 24로 정한다. Android 식별자는 하이픈이 없는 `com.mjs5ngg.guildmatefollow`를 사용한다.
- Windows 개발자 모드가 꺼진 환경에서는 Tauri CLI가 Rust `.so` 심볼릭 링크 생성 단계에서 실패한다. `package-android.ps1`은 이 오류만 식별해 파일 복사와 Gradle 패키징으로 이어가며, 다른 빌드 오류는 숨기지 않고 중단한다.
- Android 직접 설치본은 ARM64 릴리스 APK로 만들고 로컬 개발 서명으로 v2·v3 서명을 적용한다. Play Store 배포 시에는 개발 서명을 사용하지 않고 정식 업로드 키와 AAB로 교체해야 한다.
- `v0.2.0`은 프런트엔드 테스트 19건, Rust 테스트 22건, Clippy 경고 0건, Windows NSIS 빌드와 Android ARM64 APK 빌드를 통과했다. APK 메타데이터는 앱 ID `com.mjs5ngg.guildmatefollow`, 최소 SDK 24, 대상 SDK 36이며 v2·v3 서명을 검증했다.
- 앞으로 앱 업데이트 시 사용하던 `npm run package:windows`가 Windows 설치본·포터블 EXE 생성 후 `package-android.ps1`을 연속 실행한다. Android 단계까지 성공해야 통합 배포가 성공한 것으로 처리하며 세 파일을 같은 `release` 폴더에 모은다.
- Android 최초 설정은 `save_setup`이 끝난 뒤에만 첫 동기화 진행률을 내보내므로 OCID·기본 정보·길드 조회 동안 화면이 0%로 고정되어 실제 정지처럼 보였다. 설정 자체를 5단계로 알리고 Android 보안 저장 호출은 차단 작업과 15초 제한으로 감싼다.
- `v0.2.1`은 최초 설정 진행 상태 개선 후 프런트엔드 19건, Rust 22건, Clippy 경고 0건과 Windows·Android 통합 배포를 통과했다. 새 ARM64 APK는 v2·v3 서명 검증을 완료했다.
- `v0.2.1` 실기기 오류의 실제 원인은 `android-native-keyring-store`가 참조하는 `ndk-context`를 현재 Tauri 런타임이 초기화하지 않는 점이다. `Keyring.kt`가 메인 Rust 라이브러리를 먼저 불러오고 JNI 초기화 함수로 `applicationContext`를 전달한 다음 기존 Tauri `onCreate`를 실행한다.
- `v0.2.2`는 내보낸 JNI 초기화 심볼, Kotlin 컴파일, 프런트엔드 19건, Rust 22건, Clippy 경고 0건, Windows·Android 통합 배포와 APK v2·v3 서명 검증을 통과했다.
- 최초 설정은 과거 30일 수집만 직접 기다리고 오늘 실시간 수집은 8초 뒤 백그라운드 루프에 의존했다. 앞으로 과거 수집 실패 시 누락 작업을 한 번 즉시 재시도하고 `sync_live_now`까지 마친 뒤 대시보드로 전환한다.
- Android 뒤로가기는 WebView 기록을 사용한다. 각 열린 창은 고유 ID를 누적하고 `popstate`에서 자신의 ID가 사라진 창만 닫아 도움말 위에 설정창이 있을 때 도움말만 먼저 닫히게 한다.
- `v0.2.3`은 프런트엔드 19건, Rust 22건, Clippy 경고 0건, TypeScript·Vite 빌드, Windows NSIS와 Android Kotlin·APK 빌드 및 v2·v3 서명 검증을 통과했다.
- 즐겨찾기 전용 순위는 카드의 사용자 지정 순서가 아니라 길드 순위와 같은 기준을 사용한다. 전체 경험치 모드에서는 레벨과 현재 경험치로 정렬하고 기간별 모드에서는 백엔드의 선택 기간 획득량 순서를 유지한다.
- Android WebView가 백그라운드로 이동하면 걷기 프레임 타이머와 원격 이미지 디코딩이 함께 중단될 수 있다. 화면 복귀 시 이미지 요소를 강제로 다시 만들고 걷기 프레임을 초기화한다.
- `v0.2.4`는 모바일 하단 즐겨찾기 탭과 즐겨찾기 전용 순위표를 추가했다. 백그라운드에서는 아바타 프레임 전환을 중지하고 `visibilitychange`, `pageshow`, `focus` 복귀 시 이미지 요소를 다시 생성한다.
- `v0.2.4`는 프런트엔드 21건, Rust 22건, Clippy 경고 0건, TypeScript·Vite 빌드, Windows NSIS와 Android Kotlin·APK 빌드 및 APK v2·v3 서명 검증을 통과했다. ADB에서 연결된 기기는 발견되지 않아 자동 복사는 수행하지 않았다.
- 기간과 랭킹 기준은 SQLite나 API 키 저장소가 아니라 Tauri WebView의 앱별 `localStorage`에 저장한다. 같은 React 대시보드를 쓰는 Windows와 Android에서 모두 영구 보존되며 앱 종료 시 별도 종료 이벤트에 의존하지 않는다.
- 아이콘은 생성형 재편집으로 단풍잎 모양을 바꾸지 않고 기존 `maple-leaf-v2.png`의 투명 영역만 흰색으로 합성한다. 합성 원본에서 Tauri 아이콘 도구로 Windows·Android 전체 크기를 다시 만든다.
- `v0.2.5`에서는 `dashboard-period-v1`과 `dashboard-ranking-mode-v1` 키에 선택을 즉시 저장한다. 일간·7일·30일·유효한 직접 지정 기간과 전체·기간별 경험치 모드를 Windows 및 Android 재실행 후 복원한다.
- `v0.2.5` 아이콘은 `maple-leaf-white.png`를 원본으로 하며 Windows 아이콘은 흰 정사각형, Android 런처 아이콘은 흰색 둥근 배경을 사용한다.
- `v0.2.5`는 프런트엔드 24건, Rust 22건, Clippy 경고 0건, TypeScript·Vite 빌드, Windows NSIS와 Android Kotlin·APK 빌드 및 APK v2·v3 서명 검증을 통과했다.
- 사냥 중 판정은 최근 20분 안의 첫·마지막 공식 API 표본 사이에서 절대 경험치가 증가했는지를 사용한다. 오랫동안 앱을 쓰지 않으면 첫 수동 수집은 기준점일 뿐이므로 한 번의 API 응답만으로 사냥을 추정할 수 없다.
- 수동 동기화는 최신 수집, 누락·일별 동기화, 최신 재수집 순으로 바꾸고 1분 뒤 최신 표본을 한 번 더 수집한다. 기존 5분 자동 수집을 기다리지 않고도 두 시점 사이의 증가를 확인할 기회를 만든다.
- `v0.2.6`에서는 즐겨찾기 순위를 메인 본문에서 제거했다. Windows 사이드바와 Android 하단 탐색의 즐겨찾기 탭이 별도 화면 상태를 열며, 성장 기록과 길드 순위는 메인 화면으로 돌아간 뒤 해당 위치로 이동한다.
- 앱이 백그라운드에서 돌아올 때도 최신 표본을 즉시 수집하고 1분 후 후속 표본을 예약한다. 수동 동기화와 화면 복귀 모두 기존 5분 자동 주기에만 의존하지 않는다.
- `v0.2.6` 아이콘의 단풍잎은 흰 캔버스 중앙 76% 크기로 배치했다. Windows 아이콘과 Android 일반·적응형 런처 미리보기에서 잎 끝이 잘리지 않음을 확인했다.
- `v0.2.6`은 프런트엔드 24건, Rust 22건, Clippy 경고 0건, TypeScript·Vite 빌드, Windows NSIS와 Android Kotlin·APK 빌드 및 APK v2·v3 서명 검증을 통과했다.
- `v0.2.6` 배포 시 ADB에서 연결된 Android 기기는 발견되지 않아 APK 자동 복사는 수행하지 않았다.
- Android 알림 감시는 `PeriodicWorkRequest`의 공식 최소 주기인 15분을 사용한다. Doze와 제조사 절전 정책으로 실제 실행은 늦어질 수 있으며 사용자가 앱을 강제 종료하면 다시 열기 전까지 보장할 수 없다.
- Android 13 이상에서는 `POST_NOTIFICATIONS` 권한을 런타임에 요청한다. 권한 거부 상태에서는 시스템 알림을 보낼 수 없으므로 앱이 전면에 있을 때 네이티브 안내창으로 설정 변경을 유도한다.
- WorkManager는 기존 Rust 네이티브 라이브러리의 JNI 함수를 호출한다. API 키는 Android Keystore에서 Rust가 직접 읽으므로 Kotlin과 일반 SharedPreferences에는 비밀값을 저장하지 않는다.
- 대표캐릭터가 아닌 즐겨찾기만 감시한다. 첫 조회는 기준점만 저장하고, 비활동 상태에서 경험치 증가가 처음 확인될 때 한 번 알린 뒤 증가가 멈출 때까지 중복 알림을 보내지 않는다.
- 알림 권한 거부, 최근 작업 오류, 30분 이상 성공 기록 없음, 60분 이상 작업 정체를 앱 내 안내 대상으로 삼는다. 앱이 전면에 있을 때 5분마다 상태를 확인하되 안내창은 최대 30분에 한 번만 표시한다.
- `v0.2.7`은 프런트엔드 24건, Rust 26건, Clippy 경고 0건, TypeScript·Vite 빌드, Windows NSIS와 Android Kotlin·APK 빌드 및 APK v2·v3 서명 검증을 통과했다. APK에는 알림·네트워크·부팅 복원 권한과 WorkManager 구성이 포함되며 ADB에서 연결된 기기는 발견되지 않았다.
- 앱 시작 시 사냥 상태가 늦게 나타난 원인은 `Dashboard` 첫 마운트가 조회만 수행하고, 백엔드 루프가 8초 뒤 일별 동기화를 먼저 마친 후에야 실시간 표본을 수집했기 때문이다. 첫 마운트에서 실시간 수집을 직접 시작해 이 순서를 분리한다.
- `v0.2.8`에서는 설정된 대시보드가 처음 나타나는 즉시 `sync_live_now`를 호출한다. 최근 20분 표본이 있으면 즉시 사냥 상태를 다시 계산하고, 없으면 1분 후 후속 수집으로 비교 가능한 두 표본을 만든다.
- `v0.2.8`은 프런트엔드 24건, Rust 26건, Clippy 경고 0건, TypeScript·Vite 빌드, Windows NSIS와 Android Kotlin·APK 빌드 및 APK v2·v3 서명 검증을 통과했다.
- 실제 PC DB에서는 370명의 실시간 표본이 5분 간격으로 남았지만 `v0.2.8`의 프런트엔드 1분 후속 표본은 남지 않았다. WebView 타이머는 최소화·백그라운드 전환에 영향을 받으므로 시작 판정의 필수 단계로 사용할 수 없다.
- 최근 20분 실제 표본 네 개를 비교했을 때 370명 중 14명의 증가가 확인돼 경험치 비교식 자체는 작동했다. 이번 보강은 비교식이 아니라 시작 시 비교 표본 확보의 신뢰성을 높이는 데 집중한다.
- `v0.2.9`의 Rust 백그라운드 루프는 설정 완료를 5초 간격으로 확인한 뒤 첫 실시간 수집을 우선 실행한다. 후속 수집은 90초 뒤와 그로부터 210초 뒤인 시작 약 5분 시점에 별도 비동기 작업으로 실행한다.
- `v0.2.9`은 프런트엔드 24건, Rust 26건, Clippy 경고 0건, TypeScript·Vite 빌드, Windows NSIS와 Android Kotlin·APK 빌드 및 APK v2·v3 서명 검증을 통과했다. 현재 ADB 연결 기기는 없었다.
- 모바일 알림 상태는 User-Agent나 웹 권한 API로 추정하지 않는다. Android `NotificationManagerCompat`, 런타임 권한과 기존 WorkManager 상태 저장값을 네이티브 플러그인에서 읽어 설정창에 전달한다.
- PC의 즐겨찾기 순위는 모바일처럼 대시보드 전체를 교체하지 않고 기존 길드 순위 패널의 데이터와 제목만 교체한다. 좌측 주·보조 순위 탭은 같은 상태를 반영해 선택할 때 위치가 서로 바뀐다.
- `v0.2.10` 모바일 설정은 알림 권한, 시스템·채널 허용, 백그라운드 감시 상태와 마지막 성공 시각을 분리해 보여준다. 사용 불가 상태에서는 Android 알림 설정을 직접 열거나 감시 작업을 즉시 재확인할 수 있다.
- `v0.2.10` PC 사이드바는 현재 선택한 순위를 큰 주 탭으로, 다른 순위를 작은 보조 탭으로 표시한다. 보조 탭을 누르면 두 탭의 위치와 기존 하단 순위표 내용이 함께 교체된다.
- `v0.2.10`은 프런트엔드 26건, Rust 26건, Clippy 경고 0건, Windows NSIS와 Android Kotlin·APK 빌드 및 APK v2·v3 서명 검증을 통과했다. APK의 네이티브 알림 상태 플러그인도 R8 결과에 보존됐으며 ADB에서 연결된 기기는 발견되지 않았다.
- Android `JSONObject.optString("error", "")`는 JSON null을 빈 문자열이 아니라 문자열 `"null"`로 반환했다. 이 값이 감시 오류 저장소에 남아 최근 작업 성공 시각이 있어도 감시를 비정상으로 판정한 것이 사진의 `null`과 상충 상태가 함께 나타난 원인이다.
- `v0.2.11`은 오류 필드가 실제 문자열일 때만 저장하고 기존 `"null"` 오염값을 무시한다. 알림 권한·시스템 채널과 최근 1시간 안의 작업 성공 기록이 모두 정상이어야 `알림 사용 가능`으로 표시한다.
- 감시 첫 성공 전에는 `백그라운드 감시 확인 중`, 작업 오류나 장기 미실행은 `알림 사용 불가`로 표시한다. 후자는 즉시 재확인과 앱 정보의 배터리 사용 제한 해제 방법을 함께 안내한다.
- 설정 도움말과 닫기 버튼은 절대 위치를 해제하고 별도 도구 행에 배치해 알림 상태 카드의 경계와 겹치지 않는다.
- `v0.2.11`은 프런트엔드 29건, Rust 26건, Clippy 경고 0건, TypeScript·Vite 빌드, Android Kotlin·R8·APK 빌드, Windows NSIS 및 APK v2·v3 서명 검증을 통과했다. ADB에서 연결된 Android 기기는 발견되지 않았다.
- 설정창에는 성격이 다른 두 도움말이 있으므로 공통 우상단 버튼으로 묶지 않는다. 알림 도움말은 알림 카드 안에, API 발급 도움말은 API 키 변경 영역에 각각 배치한다.
- `v0.2.12` 알림 도움말은 감시 대상, 첫 기준점, Android 약 15분 주기, 사용 가능 판정, 중복 방지와 배터리 제한 해제 절차를 설명한다. API 도움말과 동일한 뒤로가기 스택을 사용해 최상단 팝업만 먼저 닫는다.
- `v0.2.12`는 프런트엔드 29건, Rust 26건, Clippy 경고 0건, TypeScript·Vite 빌드, Windows NSIS와 Android APK 빌드 및 APK v2·v3 서명 검증을 통과했다. ADB 연결 기기는 없었다.
- Android 시스템 알림 발송 코드는 앱 전면에서도 실행 가능하지만, 감지 작업을 시작하는 경로가 앱 생성 시 한 번과 WorkManager 15분 주기뿐이었다. 앱을 계속 사용하는 동안의 별도 감시 예약이 없었던 것이 전면 알림 지연 원인이다.
- `v0.2.13`은 Android `onResume`에서 즉시 감시를 시작하고 앱이 전면에 있는 동안 5분 간격으로 반복한다. `onPause`에서는 전면 확인만 제거하고 WorkManager 15분 백그라운드 작업은 유지한다.
- 최초 알림 권한 승인 콜백에서도 즉시 감시를 예약한다. 시스템 알림 발송에는 Activity 전면 여부 조건이 없으므로 증가 이벤트가 만들어지면 앱 사용 중에도 같은 알림 채널로 표시된다.
- `v0.2.13`은 프런트엔드 29건, Rust 26건, Clippy 경고 0건, TypeScript·Vite 빌드, Windows NSIS와 Android Kotlin·APK 빌드 및 APK v2·v3 서명 검증을 통과했다.
- 실제 기기 재현에서 배터리 제한 없음, 권한 정상, 비활동 즐겨찾기라는 조건인데도 앱 종료 후 40분 동안 알림이 없었고 앱 실행 직후 알림이 왔다. 증가 판정과 발송은 정상이며 WorkManager 주기 작업이 닫힌 동안 실행되지 않은 것이 원인이다.
- WorkManager 주기 실행은 정확한 타이머가 아니므로 알림 신뢰성이 필요한 사냥 세션은 사용자에게 보이는 Android 전경 서비스가 담당한다. 데이터 동기화 전경 서비스는 Android 15 이상에서 24시간당 6시간 제한이 있으므로 서비스 자체 실행은 그보다 짧게 제한한다.
- `v0.2.14`는 앱이 전면일 때 시작한 `dataSync` 전경 서비스를 최대 5시간 45분 유지한다. 서비스는 `즐겨찾기 알림 감시 중` 상시 알림을 표시하고 Activity 종료 후에도 5분마다 공유 감시 함수를 직접 실행한다.
- WorkManager와 전경 서비스가 동시에 확인할 때는 프로세스 내부 Mutex로 직렬화한다. 서비스가 시스템에 의해 종료되면 `START_STICKY` 복원을 요청하며 명시적 제한 종료 뒤에는 WorkManager 예약을 다시 확인한다.
- `v0.2.14` APK에서 인터넷·알림·전경 서비스·dataSync 권한, `FavoriteMonitoringService` 매니페스트 선언, R8 보존과 APK v2·v3 서명을 확인했다.
- `v0.2.14`는 프런트엔드 29건, Rust 26건, Clippy 경고 0건, TypeScript·Vite 빌드, Windows NSIS와 Android Kotlin·R8·APK 빌드를 통과했다. ADB 연결 기기는 없었다.
- `v0.3.1` 대형 위젯은 `RemoteViewsService` 기반 `ListView`로 전환해 즐겨찾기 수와 무관하게 각 행을 64dp로 고정했다. 적은 목록은 상단부터만 차지하고, 목록이 위젯 높이를 넘으면 위젯 안에서 스크롤된다.
- 기존 2×1 공급자는 제거했다. 새 주간 위젯은 `targetCellWidth=3`, `targetCellHeight=2`, 가로 전용 리사이즈와 180~320dp 범위를 사용해 런처 기준 3~5칸을 요청하며 세로 크기는 110dp로 고정한다.
- `v0.3.1`은 Rust 23건, 프런트엔드 26건, Android Kotlin 단위 테스트, Clippy 경고 0건, TypeScript·Vite 빌드, Windows NSIS와 Android R8·APK 빌드 및 APK v2·v3 서명 검증을 통과했다. APK에는 새 공급자 세 개와 목록 서비스가 등록됐고 알림 권한은 없으며, ADB 연결 기기는 발견되지 않았다.
- `v0.3.1` 실제 기기에서는 주간·2×2 위젯은 추가되지만 대형 위젯만 `위젯을 추가할 수 없습니다`로 실패했다. 컬렉션 위젯 전환으로 새로 생긴 `RemoteViewsService` 연결과 대형 레이아웃을 우선 조사한다.
- ADB 연결 기기가 없어 삼성 런처의 예외 로그를 직접 회수하지는 못했다. APK에는 공급자와 서비스가 정상 등록돼 있었고 다른 두 위젯이 작동하므로, 대형 위젯에만 있는 원격 서비스 어댑터 경로가 기기별 실패 지점으로 좁혀진다.
- `v0.3.2`는 Android 12 이상에서 공식 권장 인라인 `RemoteCollectionItems`를 사용해 서비스 바인딩 없이 고정 행 목록을 전달한다. Android 7~11만 기존 `RemoteViewsService`를 호환 경로로 사용하며, 불필요한 행 클릭용 PendingIntent 템플릿도 제거했다.
- `v0.3.2`는 Rust 23건, 프런트엔드 26건, Android Kotlin 단위 테스트, Clippy 경고 0건, TypeScript·Vite 빌드, Windows NSIS와 Android R8·APK 빌드 및 APK v2·v3 서명 검증을 통과했다. 현재 ADB 연결 기기는 없었다.
- `v0.3.2`의 인라인 컬렉션에서도 실제 기기의 대형 위젯 추가 실패가 반복됐다. 서비스 바인딩이 단독 원인은 아니며 대형 위젯에만 여러 개 포함되는 캐릭터 비트맵의 `RemoteViews` 전송량도 함께 제거해 검증해야 한다.
- 첨부 양식의 3×2 위젯은 이미지·그래프 대신 최근 7일의 날짜, 레벨, 경험치율, 당일 획득량과 일평균·남은 경험치·예상 레벨업 날짜를 텍스트로 표시하는 것으로 해석한다.
- 대형 위젯은 목록 행마다 원본 캐릭터 비트맵을 인라인 컬렉션에 직렬화했다. 한 장만 전달하는 소형 위젯과 달리 여러 장의 합계가 Binder 전송 한도에 근접할 수 있으므로, 표시 전에 가로·세로 비율을 유지한 최대 96픽셀 비트맵으로 축소한다.
- 예상 레벨업은 현재 레벨 필요 경험치에서 현재 경험치를 뺀 값에 최근 7일 중 수집된 일별 획득량의 산술평균을 적용한 단순 추정이다. 평균이 없거나 0이면 날짜를 임의 계산하지 않고 `계산 불가`로 표시한다.
- `v0.3.3`은 Rust 23건, 프런트엔드 26건, Android Kotlin 위젯 단위 테스트, Clippy 경고 0건, TypeScript·Vite 빌드, Windows NSIS와 Android R8·APK 빌드 및 APK v2·v3 서명 검증을 통과했다. 현재 ADB 연결 기기는 없었다.
- `v0.3.3` 실제 기기에서 대형·3×2 위젯이 모두 `위젯을 추가할 수 없습니다`로 실패하고 2×2 위젯만 정상 렌더링됐다. 실패한 두 레이아웃에만 `RemoteViews` 허용 목록에 없는 일반 `android.view.View` 구분선이 포함되어 있어 런처의 레이아웃 인플레이트 실패 원인으로 판단한다.
- `v0.3.4`는 대형·3×2 레이아웃의 일반 `View` 구분선을 `RemoteViews`가 허용하는 `TextView`로 교체하고 재도입 방지 테스트를 추가했다. Android ARM 테스트, 프런트엔드 26건, Rust 23건, ARM64 릴리스 빌드와 APK v2·v3 서명 검증을 통과했으며 APK를 `민제의 S24+ (3)\storage\Download`로 전송해 원본과 SHA-256 일치를 확인했다.
- 위젯 캐릭터 이미지가 작은 직접 원인은 원본 PNG의 투명 여백을 유지한 채 최대 96px로 축소하고 선형 보간한 것이다. 순위 행은 전송량 제한을 지키면서 여백 제거 후 112px로, 대표 전용 위젯은 여백 제거 후 256px로 비보간 확대한다.
- 새 결합 위젯은 기존 하단 두 위젯을 대체하지 않고 좌측 2×2 대표 카드와 우측 3×2 주간 성장표를 한 행에 배치한 고정 5×2 공급자로 추가한다.
- `v0.3.5`는 캐릭터 투명 여백 제거와 비보간 확대, 52dp 순위 아바타, 256px 대표 아바타, 높이 가중치 기반 3×2 주간표와 신규 5×2 결합 위젯을 포함한다. Android 위젯 테스트, 프런트엔드 26건, Rust 23건, ARM64 릴리스와 APK v2·v3 서명 검증을 통과했으며 APK 내부의 네 공급자 등록을 확인했다. APK는 `민제의 S24+ (3)\storage\Download`로 전송해 SHA-256 일치를 확인했다.
- 주간 위젯의 첫 날짜는 비교 시작점이므로 `gained_exp`를 표시하지 않고 `primary_weekly_exp`, 일평균과 예상 레벨업 계산에서도 제외한다. 표시된 나머지 날짜 중 실제 상승량이 있는 값만 평균에 포함한다.
- `v0.3.6`은 첫 주간 행을 `(기준)`으로 표시하고 집계에서 제외하며, 날짜를 `MM.dd`로 축약해 9sp 본문이 위젯 폭을 넘지 않게 했다. 즐겨찾기 인원수와 영문 `PRIMARY`, 대표 이미지 라운드 패널을 제거했다. 프런트엔드 26건, Rust 23건, Android 위젯 테스트와 ARM64 릴리스·APK v2·v3 서명을 검증했고 휴대폰 Download 전송본의 SHA-256 일치를 확인했다.
- 위젯 글자 크기 한 단계는 1sp로 정의한다. 일반 문구는 1sp, 7일 행·일평균·오늘 획득·길드원 레벨/경험치처럼 작은 보조 정보는 2sp 올리며 `즐겨찾기 랭킹` 17sp와 2×2 현재 경험치율 18sp는 유지한다.
- `v0.3.7`은 지정된 두 예외를 유지하면서 모든 위젯 글자를 확대하고 기준일의 상승량 접미사 전체를 생략한다. 프런트엔드 26건, Rust 23건, Android 위젯 테스트와 ARM64 릴리스·APK v2·v3 서명을 검증했으며 휴대폰 Download 전송본의 SHA-256 일치를 확인했다.
- `v0.3.8`은 대형 위젯 우하단 갱신 시각을 입력 형식과 무관하게 `YYYY-MM-DD HH:mm` 24시간법으로 정규화하고 해당 글자만 이전 8sp 크기로 복원한다.
- 홈 위젯 공급자 프로세스만으로 Rust 동기화를 직접 실행할 수 없으므로 대형 위젯 새로고침은 명시적인 사용자 탭으로 앱을 열고 기존 시작 즉시 동기화를 수행한다. 별도의 Kotlin API·DB 구현은 만들지 않는다.
- 대표 대기 모션은 NEXON 이미지 URL의 `action=A00.0~3`, 128×128 렌더링을 별도 캐시하고 Android RemoteViews 허용 클래스인 `ViewFlipper`에 4장을 넣어 구현한다. 전송 비트맵은 장당 최대 160px로 제한한다.
- `v0.3.9`는 갱신 시각 8sp를 제외한 위젯 최소 글자 크기를 12sp로 맞추고, 대형 위젯을 3×3까지 축소할 수 있게 했다. 순위 원형 배지는 22dp로 줄이고 새로고침 버튼은 앱을 열어 기존 시작 즉시 동기화를 실행한다.
- 대표 캐릭터의 `A00.0~3` 프레임은 일반 캐시와 분리해 저장하며 2×2·5×2 위젯에서 650ms 간격으로 순환한다. 프런트엔드 26건, Rust 23건, Android 위젯 단위 테스트와 ARM64 R8 릴리스·APK v2·v3 서명을 검증했고 휴대폰 Download 전송본의 SHA-256 일치를 확인했다.
- 대기 모션이 흔들려 보인 직접 원인은 프레임마다 투명 여백을 별도로 잘라 각 이미지의 기준점과 확대율이 달라진 것이다. 대기 프레임은 NEXON이 제공한 동일한 128×128 캔버스를 자르지 않고 표시해 캐릭터 위치를 고정한다.
- 닉네임은 임의 글자 수 기준이 아니라 Android TextView의 균일 자동 크기 조절을 사용한다. 대표 문구를 고정 폭 왕관 아이콘으로 바꿔 실제 닉네임 가용 폭도 늘린다.
- `v0.3.10`은 새로고침·왕관 벡터 아이콘, 10~13sp 범위의 한 줄 닉네임 자동 축소, 고정 캔버스 대기 모션과 주간 위젯 위아래 7dp 추가 여백을 포함한다. 프런트엔드 26건, Rust 23건, Android 위젯 테스트와 ARM64 R8 릴리스·APK v2·v3 서명을 검증했으며 휴대폰 Download 전송본의 SHA-256 일치를 확인했다.
- `v0.3.10` 실제 화면에서 프레임 전체 128×128 캔버스를 축소한 결과 캐릭터가 지나치게 작아져 움직임도 식별하기 어려워졌다. 네 프레임의 합집합 외곽선을 공통으로 적용하면 기준점은 고정하면서 캐릭터만 크게 표시할 수 있다.
- 대형 위젯의 3칸 축소 상태는 `OPTION_APPWIDGET_MIN_WIDTH`가 230dp 이하인 경우로 분기한다. 이때 경험치율과 한국어 단위 획득량만 정수로 줄이고 원본 데이터나 넓은 위젯 표시는 바꾸지 않는다.
- `v0.3.11`은 대표 왕관을 닉네임 앞에 고정하고 소형 헤더 닉네임을 왼쪽 정렬한다. 대기 모션은 네 프레임 합집합 외곽선을 192px로 공통 확대해 위치·크기를 고정하면서 움직임을 다시 식별할 수 있게 했다.
- 대형 위젯의 순위 칸은 원형 배경 없는 14dp 숫자로 줄였고 행 좌우 여백과 아바타 앞 여백도 축소했다. 프런트엔드 26건, Rust 23건, Android 위젯 테스트와 ARM64 R8 릴리스·APK v2·v3 서명을 검증했으며 휴대폰 Download 전송본의 SHA-256 일치를 확인했다.
- Android 정기 작업은 WorkManager 정책상 최소 15분이며 Doze와 배터리 최적화에 따라 지연될 수 있다. 일반 앱 종료와 재부팅 뒤에는 유지되지만 강제 중지 상태에서는 사용자가 앱을 다시 열 때까지 실행되지 않는다.
- 백그라운드 Worker는 API 키를 별도 저장하지 않고 기존 `android-native-keyring-store`를 JNI Rust 계층에서 읽는다. 전체 길드 대신 대표 및 즐겨찾기만 조회해 15분 주기의 API 호출량을 제한한다.
- `v0.3.12`는 네트워크 연결 조건의 15분 고유 WorkManager 작업을 앱 시작 또는 첫 위젯 추가 시 등록한다. Worker는 위젯이 없으면 API를 호출하지 않고, 실패하면 30초부터 지수 백오프로 재시도한다.
- 백그라운드 JNI 동기화는 대표·즐겨찾기의 실시간 정보와 누락된 최근 완료일 스냅샷을 저장한 뒤 모바일 전용 7일 스냅샷을 생성한다. 프런트엔드 26건, Rust 24건, Android 위젯 단위 테스트와 ARM64 JNI 심볼·R8 릴리스·APK v2·v3 서명을 검증했고 휴대폰 Download 전송본의 SHA-256 일치를 확인했다.
# Linux 환경 검증 결과 — 2026-09-07

- Ubuntu 24.04.4 LTS, WSL2, systemd에서 PostgreSQL 16.15, Rust 1.98.1, Node 18.19.1을 설치했다.
- `/home/mapledev/maple-guild-tracker`는 GitHub에서 복제한 별도 작업본이다. Windows .env와 DB 및 키는 복사하지 않았다.
- Linux에서 서버 테스트 15개(실제 PostgreSQL 통합 4개 포함), 화면 테스트 34개, TypeScript, 웹 두 화면 빌드, clippy가 통과했다. HTTP 상태에서 database=true와 개인 조회 화면 200을 확인했다.
- Linux 테스트 서버는 종료하고 Windows 서버를 기존 자격 증명 사용 방식으로 다시 실행했다. Linux 운영 전환·DB 이전·비밀 키 전달은 아직 수행하지 않았다. 소셜 제공자 등록 정보도 별도 연결이 필요하다.
# 2026-09-09 비로그인 서비스 및 무료 주소 검토 시작

- 2026-09-10 중단 재개. 개인 실행 단위 캐시로 대표 id/basic 중복호출 제거(실패는 캐시안함, 다음 실행 재조회). 테스트53개·TypeScript 통과 및 Pages b4568fb1 배포. 정식주소 HTML 최신 index-DvtXd3s0.js 참조200 및 CSP 유지 확인. 내장브라우저에서 개인조회 버튼 클릭 후 팝업 탭이 나타나지 않았으며 실제 키는 입력/조회하지 않았다. 실제 브라우저 키/CORS/공용기록불변 시험은 남음. Google 실제 공개로그인은 앞선 사용자 조작 후 계정설정·로그아웃·기존대표와길드목록 표시로 확인됐음.

- Google 공개callback 추가 사용자 승인 후 저장. 콘솔 저장완료 알림, 재진입해 localhost3100 및 https://antirust-chair-grove.ngrok-free.dev/auth/google/callback 두 주소 유지 확인. 공개 /auth/google/start를 사용자 사이드창에 열어 실로그인 확인 진행. 클라이언트/비밀키 변경 없음.

- 메인 공개 전환 완료. /home/mapledev/maple-releases/20260910-public 바이너리+web-dist, /etc/systemd/system/maple-exp.service.d/public-release.conf로 WorkingDirectory/ExecStart 지정. root600 기존env는 미변경. cargo release가 기존경로 바이너리를 교체했으므로 재시작 전 /proc/MainPID/exe에서 실제 이전실행본을 before-public-20260910/maple-exp-server에 보존하고 이전web-dist도 복사함. 롤백은 단순override제거가 아니라 이전릴리스 WorkingDirectory/ExecStart로 override 교체 필요. 0005는 추가테이블이므로 이전앱과 호환. 서비스/ngrok active, 외부device/me/dashboard200 Secure쿠키·새bundle·status DB/collector설정true·운영/status404 확인. 빈대표 시험기기1개 생성(수집대상없음). Google콘솔 callback은 localhost 하나만 등록돼있음을 확인, 공개callback추가 전 승인 대기.

- 메인 공개 배포는 기존 운영 경로를 덮지 않고 릴리스 디렉터리와 systemd drop-in으로 전환한다. root600 비밀 파일은 보존하며 PUBLIC_ORIGIN만 ExecStart env로 덮는다. 기존 Linux dirty파일은 앞서 Windows에서 복사한 시험소스다.

- 2026-09-10 기기 인증 성공. Wrangler 인증은 암호화 파일+Windows Credential Manager 키로 저장 확인. pages:write/account:read/user:read/offline_access만 부여. 기본 다른scope부족 경고는 의도적이며 추가권한 요청하지 않음. `pages deploy web-dist/direct --project-name maple-exp-personal --branch main` 성공, 배포 f6ad54b8.maple-exp-personal.pages.dev, 정식 maple-exp-personal.pages.dev HTML/JS200 및 CSP/no-store/no-referrer/nosniff 확인. JS에 공개 ngrok원점 존재·localhost3100 없음. 메인 운영 서버와 OAuth callback은 아직 연결 전이며 전체실증 미완료.

- Pages 업로드/공개 승인을 받았으나 IAB 파일 선택 클릭 후 native 파일창이 나타나지 않음. Wrangler4.130.0 공식 CLI로 전환. Windows keyring용 @napi-rs/keyring1.3.0 글로벌 설치. login --browser=false --scopes account:read user:read pages:write --use-keyring 실행(세션66117), IAB탭6 Wrangler OAuth 승인 화면 대기. 추가 지속권한은 사용자 확인 필요. 아직 인증/업로드/배포 안 됨. 다음에는 CLI세션 유효 여부 확인 후 승인 완료시 pages deploy web-dist/direct --project-name maple-exp-personal 사용. 요청 scope에 offline_access가 CLI에서 자동추가됨.

- Cloudflare 기존 로그인 확인. Workers/Pages 프로젝트는 아직0개. 개인 조회 정적 파일만 Pages에 배포하는 절차로 진행하며 기존 Linux/ngrok 메인 호스팅은 유지한다.
- maple-exp-personal Pages 프로젝트 생성 완료. 할당주소 maple-exp-personal.pages.dev. web-dist/maple-exp-personal-20260910.zip(66166바이트) 준비. 공개 ngrok/Pages 출처로 웹 빌드 및51테스트 통과. _headers에 CSP/NEXON만 connect-src/no-store/nosniff/no-referrer 포함. 아직 업로드·사이트 배포 버튼은 실행하지 않음. Windows 업로드 조작 스킬의 공개 게시 직전 확인 요건으로 사용자 승인 후 계속한다. 메인 운영 주소와 바이너리는 미변경.

- 신규 기기 제한은 베타 전체100회/시간의 안전 상한으로 시작한다. ngrok 전달 IP의 신뢰 경계 미확인 상태에서 사용자 입력 헤더로 우회 가능한 IP 제한을 만들지 않는다. 전체 상한은 공격 시 신규 정상 이용자도 지연될 수 있는 한계가 있으며 기존 세션 재사용은 제외한다. 설정 변경10회/시간은 로그인·익명 모두 동일하게 적용한다.
- 0005_request_budgets와 원자 UPSERT 적용. 버킷 첫 요청부터1시간, 한도 초과429.20개 동시 요청에서10개만 허용·만료후초기화·기존세션 재사용·프로필11번째429를 실제PostgreSQL로 검증해 전체23개 통과. Linux 시험을 위해 해당 소스 파일만 Windows에서 복사했으므로 Linux 작업본이 Git 기준 dirty이다. 운영 바이너리·DB는 변경하지 않았다. 제한 행은 현재 자동 삭제하지 않으며 향후 만료된 익명 설정 정리 정책과 함께 정리한다.

- 2026-09-10 재개. 서버 기기 세션 API와 달리 화면은 로그인에 종속돼 있다. 초기 세션 생성과 화면 구분부터 독립 변경으로 연결한다. 계정 만료를 임의 무시하지 않으며 운영 서버는 남용 제한 적용 전 교체하지 않는다.
- 화면은 device → me 순서로 초기화한다. 비로그인은 로그인 선택 안내, 계정 연결·탈퇴는 signedIn일 때만 노출한다. 프로필 저장 시 signedIn을 보존한다. 만료 오류를 표시하고 로그인 해제로 복구할 수 있다. 프런트51개·tsc·웹 양쪽 빌드 통과. 대시보드 번들500kB 경고는 기존 용량 개선 작업으로 남긴다. 공개 운영 교체·브라우저 실증·익명 남용 제한은 미수행.

- ngrok 실제 연결 완료. /etc/maple-exp/ngrok.env root600, 일회용 입력 서버 저장 후 종료. maple-exp-ngrok active/enabled. https://antirust-chair-grove.ngrok-free.dev/ 화면200 및 /api/status JSON200 database/collector=true 확인(ngrok 공식 skip-browser-warning 헤더 사용). 일반 첫방문에는 ngrok 안내가 표시된다. PUBLIC_ORIGIN/웹 개인출처/OAuth는 아직localhost이므로 연결시험 성공이지 전체 서비스 배포 완료가 아니다. 운영 대시보드의 고정 검증대기 표기는 다음 상태연동 작업 필요.

- 사용자가 ngrok 인증 토큰 Linux 저장과 연결을 승인함. 인증 페이지 로그인 유지 확인. 토큰은 출력하지 않고 일회용 로컬 입력을 통해 전달한다.

- 재개 확인. main 3e474ae, 메인/운영 서비스 active, ngrok inactive 및 /etc/maple-exp/ngrok.env 없음. 중단 지점은 터널 인증 연결 전이다. 운영 화면 임시 서비스를 정식 systemd 파일로 교체해 재시작 복구를 준비한다. 다음 release 서버 배포에서는 main의 자동3103 바인딩과 별도 운영 서비스가 충돌하지 않도록 리스너 중복을 제거해야 한다.

- 운영 대시보드 http://127.0.0.1:3103/ 실행. maple-exp-operations 임시 systemd 서비스에서 debug 바이너리 --operations-only 사용, 기존 release 수집 서버 재시작 없음. HTTP200, database=true, 최근수집completed,20회 응답 확인. ngrok 인증/외부배포 미완료, 운영화면 재부팅 자동시작 미설정. 사용량은 실시간 미연동이며 계정확인값을 날짜와 함께 표시한다.

- 테스트 배포와 운영 화면 요청. 운영 정보는 별도 루프백3103에서만 제공하고3100 공개 라우터에 운영 API를 추가하지 않는다. 실제 ngrok 연결 전 미배포로 표시한다. 계정 화면에서 무료·월HTTP Unlimited·월1GB 확인한 사실은 실시간 사용량과 구분한다.

- ngrok 3.39.11 공식 배포 바이너리를 Linux mapledev ~/.local/bin/ngrok에 설치. http --help로 url/inspect/log 옵션 확인. 무료 가입 화면 열었으며 약관 동의/회원가입/공개 연결은 수행하지 않음. 토큰은 NGROK_AUTHTOKEN 환경 변수로 전달하는 root600 파일을 사용할 예정이며 저장소에는 예시만 둔다. 서비스 템플릿은 로컬 요청 검사 및 로그 비활성, 자동 시작 미등록 상태.

- ngrok 무료 배포 요청 수용. 기존 Linux 서버 유지. ngrok 공식 무료 한도 문서의 월20,000회/1GB와 최신 가격표의 일회성 $5 credit 차이는 계정에서 확인할 것. Tailscale Funnel은 대역폭 제한/상업 운영 적합성을 확정하지 못해 자동 채택하지 않음. ngrok 하나의 무료 호스트로 두 출처를 대체하지 않는다.

- 기기 구독 API 구현 및 Linux 개발 DB 테스트21개 전부 통과. 로그인 계정 쿠키 우선, 로그아웃 후 별도 기기 쿠키 설정 복귀, 익명 계정 연결/탈퇴 차단 확인. 운영 바이너리 재시작/배포하지 않음. 응답 경량화 이후 기존 테스트가 제거된 가상 offset 필드에 의존하여 실패하여 실제 character_exp 필드로 같은 기준값 검증을 유지하도록 수정함.

- 후속 구현. 기기 쿠키와 계정 쿠키를 분리하여 동일 기기 익명 구독을 로그인 후에도 보존한다. 기기 설정은 서버 수집 구독과 결합되므로 브라우저 쿠키 삭제 시 복구할 수 없음을 명시할 예정이다. 공개 전 남용 제한과 프런트 연결은 별도 검증한다.

- 사용자 확정. 모든 기능 비로그인 허용, 로그인은 기기 간 설정 동기화에만 필요. 도메인 구매 보류, 대체 광고 조사, 나머지 부하 경감 제안 수용.
- 서버 자동 수집에는 익명 사용자의 대상 구독도 필요하다. 로컬 설정만 만들고 서버 수집이 작동한다고 간주하지 않는다. 익명 구독 남용 제한과 로그인 시 기존 계정 설정 보존이 선행 조건이다.
- 첫 독립 구현은 웹 응답의 불필요한 원본 필드 제거. 계산/날짜 소속 판단은 그대로 두고 직렬화 경계에서만 축소하며 DB에는 원본을 유지한다.
- 2026-09-12 공개 대시보드에서 `Unexpected token '<', "<!DOCTYPE"...` 오류 확인. 무료 ngrok 안내 HTML이 API JSON 대신 반환된 정황이며, API 호출 전체에 공식 우회 헤더를 적용하고 비JSON 응답을 별도 연결 오류로 처리한다. 같은 화면의 수집 실패 30건은 이 프런트 파싱 오류와 분리해 진단한다.
- 대시보드와 브라우저 상태 도구의 API 요청에 `ngrok-skip-browser-warning: 1`을 적용하고 HTML 응답을 사용자용 연결 오류로 분류했다. 전체56개 테스트·TypeScript·공개 환경 웹 빌드 통과. `/home/mapledev/maple-releases/20260912-dashboard-api-v2`로 전환하고 공개 번들에 헤더 포함, `/api/status` JSON200을 확인했다.
- 운영 화면은 예전 debug 바이너리가 이미 적용된 0005 마이그레이션을 몰라 `VersionMissing(5)`로 재시작 중이었다. 검증된 공개 릴리스 바이너리의 `--operations-only`로 교체하여 로컬 `/status` JSON200을 복구했다.
- Windows 예약 작업 `Maple EXP Linux Runtime`이 재부팅 뒤 Ready 상태여서 WSL 명령 종료마다 서버와 수집이 중단됐다. 작업을 다시 실행하고 누락 실행 보완을 위해 StartWhenAvailable을 켰다. 이후 한 완전 주기가 406성공·0실패로 끝났으며, 화면의 30실패는 직전 한 주기의 과거 보충 요청 실패였고 영구 실패가 아니었다.
- 2026-09-12 새 기준은 Cloudflare 혼합형이다. 사용자 웹·API는 workers.dev 동일 출처, 개인 키 직접 조회는 기존 Pages, 수집기·NEXON 원본·운영 화면은 로컬에 둔다. 공개부에는 정규화 변경분만 보낸다. 플랫폼 자동 유료 전환과 도메인 구매는 하지 않는다.
- 운영 DB 실측285MB 중 observations가251MB이며 2026-09-08~12 약4일간148,110건이다. 0.5GB 외부 PostgreSQL 무료 한도에 그대로 이관할 수 없으므로 D1 5GB와 변화 기반 쓰기를 사용하고, 로컬 고빈도 원본에도 보존 기한을 적용한다.
- 시각 방향은 절제된 메이플풍, 재설계 범위는 공개 웹·PC·Android·모든 위젯으로 확정했다. 초기 수익화는 외부 광고 스크립트가 아닌 쿠팡/링크프라이스 제휴 카드이며 승인 링크 전에는 숨긴다.
- 공개 웹은 대시보드 성공 시각을 ref로 기록하고 15분이 지나야 자동 재조회한다. 진행 중 Promise를 공유해 interval·탭 복귀 요청을 합친다. activity는 최초·탭 복귀·사용자 조작 모두 공통 1시간 gate를 통한다. 개인 새로고침 완료도 같은 gate를 사용한다.
- 2026-09-12 13:09 KST에 요청 절감 빌드를 `/home/mapledev/maple-releases/20260912-request-budget`로 배포했다. 공개 `/api/status`와 로컬 운영 `/status`가 JSON 200을 반환하고 두 systemd 서비스가 새 릴리스에서 실행되는 것을 확인했다.
- 15분 원본 정리는 KST의 완결된 날짜 단위로 처리한다. 48시간 경계에 걸친 하루는 더 보존해 안전 쪽으로 기울이고, gzip 검증과 원본 건수 확인이 끝난 파일만 원자적으로 확정한 뒤 해당 날짜 행을 삭제한다. `daily_snapshots`는 정리 대상에 포함하지 않는다.
- 첫 보존 실행에서 `psql -c`의 변수 치환이 적용되지 않아 SQL 구문 오류로 중단됐으며 파일·행 변경은 없었다. 날짜 정규식 검증 뒤 안전한 DATE 리터럴을 사용하는 방식으로 수정했다. 재실행은 2026-09-08 26,208건과 09-09 36,284건을 42MB gzip으로 보존한 뒤 삭제했고, 남은 관측은 09-10 이후 86,836건이며 일별 스냅샷 14,101건은 유지됐다. 타이머는 매일 05:15 KST에 실행된다.
- Cloudflare 공개부 로컬 구현은 익명 기기, 프로필, 활동, 대시보드, Google OAuth, 로그아웃·탈퇴와 정규화 ingest를 제공한다. D1은 큰 정수를 TEXT로 보존하고 원본 `basic` 필드가 있는 ingest를 거부한다. 실제 HTTP 시험에서 설정 저장 후 17자리 경험치가 손실 없이 반환됐고 잘못된 서명은 401, 같은 배치는 409였다.
- 기존 Wrangler 토큰은 Pages 쓰기만 있고 Workers·D1 쓰기 권한이 없어 원격 목록이 인증 오류로 거부됐다. 공식 `wrangler login` 권한 갱신을 시작했으며 계정 승인 후 원격 D1 생성과 배포가 가능하다. 로컬 구현과 수집 전송부 작업은 이 승인과 독립적으로 계속한다.
- 새 Wrangler가 끌어온 개발 의존성 감사에서 기존 Vitest 3 계열의 개발 서버 경로 탐색 취약점이 확인됐다. Node 24와 호환되는 Vitest 5로 올렸고 65개 테스트 통과 및 npm audit 취약점 0건을 확인했다. 운영 의존성에는 원래 취약점이 없었다.
- 로컬 수집기는 Cloudflare 활성 대표를 먼저 조회해 길드를 확인하고, 원격 대상·로컬 활성 사용자·공유 길드원을 합쳐 수집한다. 공개 전송은 표시 필드만 정규화하고 관측 시각을 제외한 체크섬이 바뀐 현재값, 신규 일별값·오늘 기준점·길드 명단만 outbox에 넣는다. 실패 상태에서 새 변경이 생기면 엔터티 키별 최신값과 미전송 일별값을 단일 배치로 병합한다.
- 새 Rust 마이그레이션을 포함해 Linux 실제 PostgreSQL 격리 통합 테스트 25개가 전부 통과했다. `20260912-edge-outbox` 릴리스로 운영 바이너리를 전환했고 0006 마이그레이션 적용, 공개·운영 상태 JSON 200을 확인했다. 원격 Edge 환경변수는 Worker 배포 전이므로 아직 넣지 않았고 outbox는 비어 있다.
- 새 바이너리 전환 뒤 첫 완전 수집 537은 406명 성공·실패 0으로 종료됐다. 전환을 위해 중간 실행 536을 한 번 중단했지만 다음 시작에서 `interrupted`로 정상 마감되어 복구됐다.
- 프런트 재설계는 Tailwind 4, Radix Dialog·Tabs·Tooltip, TanStack Query와 자체 호스팅 Pretendard를 사용한다. 실제 화면 교체 전에 네 가지 고정 크기 시안을 만들고 이를 브라우저 스크린샷 회귀 기준으로 사용한다.
- 공개 웹 실제 화면을 대표 요약, 공용 길드·즐겨찾기 순위, 지연 로딩 성장 패널, 독립 설정 대화상자로 교체했다. 오늘·7일·30일과 순위 기준·테마는 브라우저에 보존하며 대표 순위·격차는 즐겨찾기 탭에서도 길드 기준을 유지한다. null 획득량에는 양수 기호를 붙이지 않고 `자료 없음`으로 표시한다.
- 1440px 실제 로컬 서버 데이터와 연결해 새 레이아웃을 확인했다. 새 익명 브라우저에서 대표 미지정·최근 동기화·빈 기록 상태가 무너지지 않았고, 네 가지 고정 시안은 Chrome 스크린샷 회귀 시험을 통과했다. Vitest와 Playwright 수집 경로는 분리했다.
- 2026-09-12 새 웹 번들을 `/home/mapledev/maple-releases/20260912-web-redesign`로 배포하고 공개·운영 서비스를 함께 전환했다. 공개 ngrok의 새 JS/CSS와 `/api/status`, 운영 `/status` 200 및 양쪽 systemd 활성 상태를 확인했다. 재시작 시 실행 중 수집 539는 interrupted로 보존되고 540으로 즉시 재개됐다.
- 개인 조회 새 디자인은 Cloudflare Pages 배포 `59d2b8c6.maple-exp-personal.pages.dev`를 거쳐 정식 `maple-exp-personal.pages.dev`에 반영했다. 정식 주소 HTML·CSP·no-store·no-referrer·nosniff를 확인했다. Wrangler 자격 증명은 여전히 Pages 쓰기만 있어 Worker·D1 배포에는 재승인이 필요하다.
- Cloudflare 권한 재승인 후 APAC D1 `maple-exp-public`을 만들고 원격 마이그레이션과 Worker 비밀 3종을 적용했다. 비밀은 출력하지 않고 Windows 환경과 Linux root 전용 설정에만 전달했다. 공개 주소는 `https://maple-exp-public.mjs5ng.workers.dev`이며 `/api/status`, 익명 기기와 설정 API를 확인했다.
- 첫 실제 outbox는 현재 413명·일별 10,843건으로 단일 요청 한도를 넘어 거부됐다. 수집기를 현재 400·일별 500·기준점 400·길드 2 단위의 결정적 서브배치로 바꾸고 시작 시 미전송분을 먼저 재개하도록 수정했다. 22개 배치가 D1에 모두 반영됐고 outbox는 비워졌다. 오류 내용은 URL이나 비밀 없이 상태 코드만 보존한다.
- 원격 익명 프로필의 첫 대시보드 조회는 413개 OCID를 SQL 변수로 펼쳐 D1 변수 한도를 넘겼다. OCID 목록을 단일 JSON 매개변수와 `json_each` 조인으로 바꿔 HTTP 200을 복구했다. 일별 행에서는 반복되는 장문 이미지 URL을 반환하지 않으며, 브라우저 압축 전송량은 약 417KB다.
- 공개 웹과 개인 키 Pages를 Worker/Pages 정식 제공 주소 기준으로 다시 빌드했다. Worker 버전 `4933e640-2312-4ae4-8b3d-35705e6b379f`, 개인 Pages 배포 `9f7503cf.maple-exp-personal.pages.dev` 이후 정식 `maple-exp-personal.pages.dev`에 반영됐다. Google OAuth Worker callback은 입력까지 완료했으나 외부 설정 저장 확인이 남았다.
- 사용자의 명시적 승인으로 Google OAuth 클라이언트에 `https://maple-exp-public.mjs5ng.workers.dev/auth/google/callback`을 저장했다. 설정 화면을 다시 열어 세 번째 반환 주소가 보존된 것을 확인했다.
- 공개 웹 활동 판정은 한 관측 구간의 절대 획득량이 10억 초과·1조 미만일 때만 최근 감지 시각을 갱신한다. 10억 이하·1조 이상·계산 불가 값은 기존 상태를 덮지 않는다. 걷기 상태는 무기한 고착되지 않도록 마지막 정상 감지 후 20분 동안만 표시한다.
- 활동 상태용 D1 마이그레이션과 Worker 버전 `8e8f22a2-7b49-458e-b241-b79688f1ca56`을 배포했다. Linux 첫 교체는 불완전한 WSL 소스 때문에 `VersionMissing(6)`으로 중단됐으나 DB 변경 없이 전체 서버 소스와 마이그레이션을 다시 동기화해 `/home/mapledev/maple-releases/20260912-web-activity`로 복구했다. 이후 수집 563은 413명 성공·실패 0으로 완료되고 outbox가 비었으며 D1에서 최근 활동 8명이 확인됐다.
- 공개 화면에서 확대된 대표 이미지, 0~100% 경험치율 축과 7일 흐름을 확인했다. 좌측 즐겨찾기 선택 시 목록 제목과 범위가 즐겨찾기로 바뀌고 길드 순위 선택 시 전체 길드로 복귀하는 것을 실증했다. 개인 키 직접 조회 Pages도 배포 `76b26aa6.maple-exp-personal.pages.dev`를 거쳐 정식 주소에 갱신했다.
# 2026-09-12 공개 웹·Android 개편 결정

- 사냥 상태는 증가량 0에서만 정지로 바뀐다. 0 초과 10억 이하와 1조 이상은 현재 상태를 보존하고, 10억 초과 1조 미만은 활동으로 바꾼다. 계산 불가·역행도 보존한다.
- 따라잡기 보고서는 최대 10명이다. 비로그인 프리셋은 기기 로컬 저장소, 로그인 프리셋은 D1 동기화를 사용한다.
- 공개 웹은 상단 탐색과 1440px 이상에서만 보이는 좌우 160px 광고 레일을 사용한다. 광고 링크가 없으면 레일을 숨긴다.
- 이번 변경 대상은 공개 웹과 Android이며 PC 앱은 사용자가 명시적으로 요청할 때까지 반영구적으로 보류한다.
- Android는 공개 서버 연결형으로 전환하고 개인 키 조회만 기기 안에서 수행한다.
- 2026-09-13 구현. 공개 웹은 상단 해시 탐색, 개요·길드·즐겨찾기·따라잡기 분리, 독립 순위·그래프 기간, 가상 순위, 고정 0~100% 그래프와 `UP!`, 레벨업·추월 예상, 로컬·계정 프리셋 코드를 갖춘다. 순위 동점은 레벨·현재 경험치·닉네임 순서를 유지한다.
- 수집기는 최신 캐릭터를 최대 8개 병렬 조회하고 최신 변경분을 과거 보충 전에 Edge outbox로 보낸다. 개인 서비스키 조회는 최대 5개 병렬이며 캐릭터별 고정 대기를 제거했다.
- Android 본문은 `maple-exp-public.mjs5ng.workers.dev`를 직접 열고 홈 위젯은 WebView 익명 세션으로 공개 API를 우선 조회한다. 공개 응답은 Android에 고정한 동일 경험치표로 레벨업 구간을 포함해 계산하며 실패 시 기존 로컬 SQLite 동기화로 후퇴한다.
- 원격 D1의 0003 적용은 Cloudflare 무료 플랜 일일 쓰기 한도 초과(code 7500)로 중단됐다. 자동 유료 전환하지 않았고, 기존 공개 Worker와 Linux 수집기는 호환되지 않는 중간 배포를 하지 않아 계속 기존 버전으로 운영한다. 한도 초기화 뒤 `npm run deploy:edge`로 공개 출처 강제 빌드·마이그레이션·배포를 순서대로 실행한다.
- 현재 검증은 Vitest 74개, Rust 18개 통과(데이터베이스 필요 9개 제외), Android JVM 23개와 공개 위젯 변환 1개 통과, ARM64 릴리스 APK 서명 검증까지 완료했다. Google Android Bearer/PKCE와 실제 S24+ 설치·공개 전환 비교는 남았다.
- Android Google 로그인은 WebView 내 OAuth를 사용하지 않는다. 신뢰된 공개 페이지의 최소 JS 브리지가 시스템 브라우저를 열며, 익명 기기 쿠키로 시작 요청을 인증하고 S256 PKCE 일회용 교환 코드만 `guildmatefollow://auth`로 받는다. 교환된 계정 세션은 WebView CookieManager에 넣으며 API 키는 서버나 로그인 URL에 포함하지 않는다. 로컬 Worker에서 익명 기기→Android 로그인 시작 URL 발급까지 HTTP 200으로 검증했고 실제 Google 왕복은 원격 0004 적용 뒤 실기기에서 확인한다.
- 2026-09-13 원격 D1 일일 쓰기 한도 초기화 뒤 0003·0004 마이그레이션과 Worker 배포가 성공했다. 공개 Worker 버전은 `39674273-bf77-4bd4-8708-8d839628ec8c`이며 익명 기기·계정 상태·7일 대시보드가 모두 HTTP 200을 반환했다. Android 로그인 시작은 `accounts.google.com`과 `https://maple-exp-public.mjs5ng.workers.dev/auth/google/callback`을 생성한다.
- 개인 새로고침 Pages는 `f4fab67f.maple-exp-personal.pages.dev`를 거쳐 정식 `maple-exp-personal.pages.dev`에 반영했다. 정식 주소의 최신 번들, CSP와 `no-store`를 확인했다.
- Linux는 GitHub `2179acf`를 깨끗한 `/home/mapledev/maple-build-20260913-edge-chase`에서 시험·빌드하고 `/home/mapledev/maple-releases/20260913-edge-chase`로 전환했다. 이전 `/home/mapledev/maple-releases/20260912-web-activity`는 롤백용으로 보존한다. 첫 수집 630은 424명 성공·실패 0으로 끝났고, 이전 HTTP 400 outbox를 새 형식으로 병합 전송한 뒤 outbox가 비었다.
- 최종 검증은 Vitest 76개, Rust 18개 통과와 DB 의존 9개 제외, Android JVM 24개, Playwright 고정 시안 4개 및 실제 운영 주소 데스크톱·모바일 해시 탐색 통과다. ARM64 APK는 버전명 `0.4.0`, 버전코드 `4000`, `guildmatefollow://auth` 딥링크, v2·v3 서명을 확인했다. SHA-256은 `D5A8DCB7BA6393421CDACC444162BDE4A5C518F96867815053799F2ED1644229`다.
- 검증 시 ADB 연결 기기가 없어 S24+ 설치와 실제 Google 왕복·위젯 갱신은 수행하지 못했다. APK는 `release/guildmate-follow-android-arm64-v0.4.0.apk`에 보존한다.
- 최종 원격 D1 확인에서 현재 캐릭터 424명, 일별 기록 11,265건, 사냥 상태 1명과 수집 완료 시각 `2026-09-13T01:25:58.704876Z`가 확인됐다. Windows 휴대용 장치에도 S24+나 Samsung MTP는 없고 이동식 USB `E:`만 보여 기기 전송은 생략했다. 기존 ngrok 서비스는 실패 상태지만 새 공개 입구가 Cloudflare Worker이므로 사용자 경로에는 영향이 없으며, 계획대로 안정화 기간 뒤 정리한다.

# 2026-09-13 공개 웹 후속 개선 결정

- 개요 화면의 넓은 중앙 공백은 그리드가 아니라 그리드 자식 `.surface`의 자동 좌우 여백으로 카드가 열 안에서 축소된 것이 원인이다. 자식 카드를 열 전체 폭으로 고정하고 42:58 동일 높이 구성으로 바꾼다.
- 따라잡기 현재 보고서는 상위 화면 메모리에서 유지해 해시 탭 왕복에는 보존하고 완전 새로고침에는 초기화한다. 프리셋의 기존 로컬·계정 저장은 유지한다.
- 추월 기준은 레벨별 필요 경험치를 합친 절대 성장 위치뿐이다. 경험치율은 표시용이며 비교 행에 절대 격차를 함께 표시한다.
- 웹 미니 위젯은 Chrome·Edge Document Picture-in-Picture를 우선하고 미지원 또는 거부 시 페이지 안 고정 패널로 후퇴한다. 원본 탭 종료·새로고침 때 PiP가 닫히는 브라우저 제약을 안내한다.
- PC 앱은 변경하지 않으며 정상인 PC 아바타의 네 프레임 순서와 180ms 주기만 공개 웹에 적용한다.
- 상위 성장은 대표·길드·즐겨찾기를 OCID로 합친 뒤 항상 오늘 획득량으로 정렬하며, 그래프 기간과 순위 기간은 분리한다.
- 웹 미니 위젯은 별도 조회 없이 현재 Query 캐시의 행을 사용하고, Document PiP 거부·미지원 시 화면 내부 드래그 패널로 전환한다.
- 2026-09-13 공개 웹 후속 개선을 Worker 버전 `b0538030-6c79-4081-bcc4-a60cc17653c4`로 배포했다. Vitest 85개, TypeScript, Edge TypeScript, Playwright 시각 회귀 4개가 통과했고 운영 주소에서 메뉴 해시, 423명 길드의 개별 선택 유도, 2명 즐겨찾기의 그룹 선택, 따라잡기 기간의 탭 왕복 보존을 확인했다.

# 2026-09-13 공개 갱신 중단 진단

- Worker 실시간 로그에서 `D1 free tier daily row write limit` 초과를 확인했다. 로컬 outbox에는 430개 현재 상태가 3시간 이상 남아 있어 공개 웹이 실제로 갱신되지 않았다.
- 현재 상태 체크섬에서 `observedAt`만 제외하고 `activityDecisionAt`은 포함한 탓에, 경험치가 0인 약 430명도 매 주기 판정 시각 변화만으로 전부 D1에 다시 기록됐다. 판정 상태가 같으면 시각만 달라져도 다시 내보내지 않도록 두 시각 필드를 모두 제외한다.
- 최근 실행의 30·60건 실패는 최신 캐릭터 성공 431건과 별개인 과거 길드 명단 보충 실패가 합산된 수치다. 현재 갱신 성공 여부와 보충 상태를 분리한다.
- 수정 커밋 `89c4ecf`를 Linux에서 다시 시험·릴리스 빌드하고 `/home/mapledev/maple-releases/20260913-refresh-fix`로 배포했다. 기존 `20260913-edge-chase` 릴리스는 롤백용으로 보존했다.
- 배포 직후 실행 675는 최신 대상 431명 성공·실패 0·`completed`로 15.6초 만에 끝났다. 전역 호출 간격을 적용해 동시 작업의 순간 요청 폭주도 제거했다.
- 이미 소진된 D1 무료 일일 쓰기 한도는 코드로 초기화할 수 없고 UTC 자정인 2026-09-14 09:00 KST에 복구된다. 그때까지 변경분은 로컬 outbox에 유지되며 초기화 뒤 자동 재전송된다. 유료 플랜 자동 전환은 하지 않는다.

# 2026-09-13 사용자 서비스 키 분산 조회 결정

- 공개 웹은 페이지가 열려 있는 동안 15분 자동 조회하고, PC와 Android는 운영체제 예약 작업을 사용한다. 브라우저가 닫힌 동안의 15분 표본은 복원하지 않는다.
- 개인 조회 결과와 경험치 기록은 사용자 기기에만 저장하며 서버나 다른 이용자에게 공유하지 않는다. 로그인은 대표·즐겨찾기·프리셋 설정만 동기화한다.
- 서비스 키 종류를 확인하는 공식 API는 없으므로 서비스 키 확인 동의와 반복 429 진단으로 안내한다. 개발키 선택지는 제거한다.
- API 키는 기존처럼 별도 조회 출처에만 저장하고 정확한 origin·일회용 nonce·MessageChannel을 사용한다. 광고와 제3자 스크립트는 조회 출처에 넣지 않는다.
- 사용자별 서비스 키 사용 방식은 NEXON 약관에 명시적인 허용 사례가 아니므로 공개와 문의를 병행하고, 불허 답변 시 중앙 수집 구조로 롤백한다.
- 2026-09-13 구현. 직접 조회 Pages는 키를 자체 localStorage에만 보관하고 OCID·현재값·30일 기록은 자체 IndexedDB에 저장한다. 대시보드는 일회용 nonce와 전용 MessageChannel로 상태와 스냅샷만 받으며 키 필드가 섞인 명령은 거부한다.
- 최신 길드 전체 조회는 동시성 32와 전역 초당 100건 상한을 사용한다. 429가 발생하면 호출 간격을 두 배로 늘리고, 현재값은 과거 보충 전에 즉시 화면·저장소에 반영한다. 과거 기록은 캐릭터 단위로 확정해 중단 후 해당 캐릭터부터 재사용한다.
- Worker `/api/dashboard`는 `legacyBootstrap:true`인 이전 자료로만 한 번 읽고, 로컬 직접 데이터가 생긴 브라우저에서는 다시 요청하지 않는다. 로그인·프로필·활동·프리셋은 서버 동기화를 유지한다.
- Android WebView의 직접 조회 키는 읽기 인터페이스 없이 Keystore에도 저장한다. 조회 결과는 최대 1,000명·31일 범위로 네이티브 SQLite에 가져오며 WorkManager가 현재 길드 명단을 다시 확인하고 알려진 길드원·즐겨찾기를 15분마다 조회한다.
- PC 자동 동기화 주기는 5분에서 15분으로 변경했고 최초 설정과 키 교체 모두 본인 명의 서비스 단계 키 확인을 요구한다.
- 2026-09-14 최종 검증. Vitest 87개, PC Rust 30개, 서버 Rust 20개 통과와 데이터베이스 환경 의존 9개 제외, Android JVM 테스트, Playwright 시각 회귀 4개, ARM64 릴리스 빌드와 APK v2·v3 서명이 모두 통과했다. APK는 `release/guildmate-follow-android-arm64-v0.4.0.apk`이고 SHA-256은 `B713726DBFC3DA0EB1D3A6AD75D5CFD7549A03725B4441CF5B784F2828C5D732`다.
- 개인 조회 Pages는 `c47afb8b.maple-exp-personal.pages.dev`를 거쳐 정식 주소에 반영했고, 공개 Worker는 버전 `b7d4b000-7559-4f1c-87c5-8e538b5bd084`로 배포했다. 정적 대시보드 `_headers`가 Worker의 새 CSP를 덮어쓰던 문제를 찾아 양쪽 정책에 `frame-src https://maple-exp-personal.pages.dev`를 일치시켰다.
- 운영 주소 실증 결과 `/api/status`는 HTTP 200과 `dataMode=device-direct`, `collectorRollback=true`를 반환한다. 대시보드는 개인 조회 Pages만 프레임으로 허용하고, 개인 조회 Pages는 정식 대시보드만 부모 출처로 허용한다. 배포 번들에서 IndexedDB, Android 저장 브리지와 NEXON 인증 헤더 사용도 확인했다.
- 중앙 수집기는 48시간 결과 비교를 위한 롤백 대기로만 남긴다. 비교 완료 전에는 삭제하거나 운영자 키를 폐기하지 않으며, 이후 7일 안정화가 끝나야 내부 수집 API를 차단한다.

# 2026-09-14 공개 웹 표시·운영 주소 진단

- 공개 Worker와 개인 조회 Pages는 HTTP 200이며 `dataMode=device-direct`다. Whale에서만 최신 경험치가 없는 현상은 중앙 터미널과 무관하고, 브라우저마다 `maple-exp-personal.pages.dev` 저장소가 분리되므로 Whale에 서비스 키를 한 번 저장해야 하는 구조다.
- 기존 WSL은 중지되어 있었고 로그인 예약 작업은 `Ready`였다. `Maple EXP Linux Runtime`을 실행하자 WSL과 `maple-exp.service`, 운영 화면, 백업·보존 타이머가 터미널 없이 활성화됐다. 이 경로는 48시간 롤백 비교용이며 새 공개 화면의 직접 조회 필수 조건이 아니다.
- Whale의 축 겹침은 고정 높이 카드 안에 고정 27rem 차트와 가변 높이 헤더를 함께 넣어 내용이 카드 밖으로 넘치는 CSS가 원인이다. 카드 내부를 flex로 바꿔 차트가 남은 높이를 사용하게 한다.
- Cloudflare 공식 정책상 정적 자산 요청은 무료·무제한이고 Worker 실행만 무료 계정 하루 100,000건에 포함된다. NEXON 캐릭터 조회는 브라우저에서 NEXON으로 직접 나가므로 Cloudflare Worker 요청 수나 D1 쓰기를 늘리지 않는다.
- 현재 계정에는 Worker가 `maple-exp-public` 하나뿐이며 `guildmate.workers.dev` 계정 서브도메인은 사용 가능함을 API로 확인했다.
- Cloudflare 계정 서브도메인을 `guildmate`로 전환하고 새 사용자용 Worker `app`을 같은 D1에 배포했다. 정식 무도메인 주소는 `https://app.guildmate.workers.dev`이며 기존 중앙 수집 롤백 Worker는 `https://maple-exp-public.guildmate.workers.dev`에서 유지한다.
- 개인 직접 조회 Pages의 `frame-ancestors`도 새 사용자용 주소로 제한했다. 공개 Worker와 Pages 양쪽 CSP, HTTPS 200, `dataMode=device-direct`를 확인했다.
- Linux 롤백 수집기의 `EDGE_PUBLIC_ORIGIN`은 새 롤백 Worker 주소로 바꾸고 서비스를 재시작했다. Windows 로그인 예약 작업은 `-WindowStyle Hidden`으로 WSL을 유지하므로 터미널 창은 필요하지 않다.
- 비밀 설정 자동화도 사용자용 `app`의 Google 비밀과 롤백용 `maple-exp-public`의 수집 서명을 서로 다른 Worker에 넣도록 분리했다. 재실행 시 중앙 수집기가 사용자용 Worker로 잘못 연결되는 경로를 막았다.

# 2026-09-15 D1 한도 경고 대응

- Cloudflare가 2026-09-15 00:00 UTC까지 D1 무료 티어 일일 100,000행 쓰기를 차단했다. KST 초기화 시각은 같은 날 09:00이다.
- Linux 저널에서 중앙 수집기가 밤새 길드 전체와 과거 보충을 계속 실행한 사실을 확인했다. `maple-exp.service`를 중지하고 자동 시작을 해제했으며 운영 화면과 백업·보존 타이머는 유지했다.
- 분산 조회 구조에서는 익명 사용자 설정을 D1에 저장할 필요가 없다. 익명 방문의 기기 세션 생성과 시간당 활동 갱신도 제거해 D1 쓰기는 로그인 설정 동기화와 프리셋에만 사용한다.
- Document Picture-in-Picture의 최상단 출처 표시줄은 브라우저가 제공하는 보안 UI라 웹 문서가 제거하거나 드래그 영역으로 대체할 수 없다. 지원 브라우저에서는 돌아가기 버튼만 숨기고, 페이지 내부 대체 위젯에서는 기존 헤더 드래그를 유지한다.
- 화면 수정 뒤 Vitest 87개, 웹 공개·직접 빌드, Edge TypeScript 검사와 고정 시안 Playwright 4개를 통과했다. 새 주소를 포함한 Android 0.4.0 ARM64 APK도 v2·v3 서명 검증을 통과했으며 SHA-256은 `5DB0177B6610A408FBD1D054623AF7F075C50375226796F0001E104CDB9AE0BA`다.
- 익명 시작에서 `/api/device` 호출을 없애고 대표·즐겨찾기를 브라우저 localStorage에만 저장한다. 로그인 전 `/api/me`는 D1을 읽지 않으며, 로그인 직후 로컬 설정이 있을 때만 계정 프로필로 한 번 이관한다.
- 새 분산 조회 구조에서 쓰임이 사라진 `/api/activity`도 웹 호출을 제거하고 기존 클라이언트 호환용 무쓰기 응답으로 바꿨다. `/api/device`와 `/api/activity` 모두 실주소에서 `deprecated:true`와 무쿠키 응답을 확인했다.
- 공개 Worker 버전 `f93400f3-e0af-4bcf-bf61-529537503bff`와 개인 조회 Pages 배포 `fbcd7b1d.maple-exp-personal.pages.dev`를 반영했다. 중앙 수집기는 `inactive`·`disabled`, 운영 화면은 `active` 상태다.
- 공개 주소·표시 안정화 커밋 `f06c874`와 익명 D1 쓰기 제거 커밋 `5c52df5`를 GitHub `main`에 반영했다.
- 2026-09-15 모바일 Chrome의 설정 화면 차단을 실서비스에서 재현했다. 배포 직전 일반 `npm run web:build`가 `__DIRECT_ORIGIN__`에 로컬 기본값 `http://127.0.0.1:3101`을 넣고 Worker 정적 자산을 덮어쓴 것이 직접 원인이다. CSP 차단은 정상 보안 동작이며 production build 자체가 운영 Pages 주소를 기본 사용하도록 고친다.
- production build는 개발용 `.env`보다 `https://app.guildmate.workers.dev`와 `https://maple-exp-personal.pages.dev`를 우선하며, 명시적 배포 환경변수만 기본값을 덮는다. 빌드 후 대시보드 번들에 로컬 개인 조회 주소가 있거나 운영 주소가 없으면 실패한다.
- 개인 조회 Pages 배포 `5ac1c793.maple-exp-personal.pages.dev`와 Worker 버전 `da587b4e-4aec-47e9-85d4-df746278f485`를 반영했다. S24+ 모바일 Chrome 사용자 에이전트와 412×915 화면에서 설정 iframe과 자동조회 iframe을 함께 열어 운영 URL 2개, 콘솔·네트워크 오류 0건을 확인했다.
- 2026-09-15 성능 진단에서 과거 보충 요청마다 상태 메시지를 보내 최대 약 13,000회의 React 상태 갱신을 유발하고, 과거 응답의 이미지 URL 등 반복 필드와 `history.find`·KST 포맷을 반복하는 기간 계산이 확인됐다. 1,000명×31일 모의 계산은 기존 방식 약 1,001ms, 날짜 인덱스 단일 순회는 약 50ms였다.
- 보충 중 기간 순위는 수집된 날짜만 합산하고 `일부 수집 n/30일`을 표시하는 방식을 확정했다. 누락일은 계속 0으로 계산하지 않는다.
- 기존 v1 IndexedDB 기록을 20명 단위로 양보하며 현재·일별 저장소로 이관하고, 이후 15분 조회에서는 메모리의 현재 캐시를 재사용한다. 과거 응답에서는 이미지·직업·길드 등 반복 필드를 제거하고 레벨·경험치·경험치율만 보존한다.
- 대시보드 갱신은 현재 정보, 7일 완료, 30일 완료의 최대 세 번으로 제한했다. 요청별 진행 상태는 250ms마다 합쳐 보내고 React 반영은 transition으로 처리한다.
- 별도 선택형 기록 프로토콜은 데이터 경로와 Android 호환을 크게 복잡하게 하는 반면 압축 30일 payload는 충분히 작다. 1,000명×31일의 7일·30일 계산이 약 70ms에 끝나는 것을 확인해 압축 단계 payload를 채택했다.
- IndexedDB v1에서 v2로 31일 기록을 실제 이관하는 회귀 테스트를 추가했다. 전체 Vitest 89개, Edge 타입 검사, 고정 시안 Playwright 4개와 공개 웹 빌드가 통과했다.
- 개인 조회 Pages 배포 `9eb57081.maple-exp-personal.pages.dev`와 Worker 버전 `7ad2d115-5065-4929-a33d-d751f653557d`를 운영 주소에 반영했다. 익명 실주소에서 초기 화면과 길드 순위 해시 탐색이 정상 동작함을 확인했다.
- Android 네이티브 가져오기가 웹의 압축 과거 기록과 문자열 경험치를 받지 못하던 호환 문제를 추가로 바로잡았다. ARM64 릴리스 빌드와 v2·v3 서명 검증을 통과했으며 APK SHA-256은 `2B7C3D6DF86ED9E9DEB97390E265562E69DCFDC9FF45BBB4EBC84D4411F3EFCF`다.
- 문제 상황과 같은 400명×31일, 총 12,400개 v1 기록 이관을 자동 재현했다. 이관 중 이벤트 루프 양보와 400명·31일 복원까지 포함한 저장소 시험 및 1,000명 기간 계산 시험 12개가 총 1초 미만에 통과했다.
- 모바일 개요 그래프는 자동 높이 flex 자식 안에서 Recharts 컨테이너 높이가 0이 될 수 있는 CSS 충돌이 원인이다. 대표 설정 화면의 순간 노출은 로컬 프로필보다 교차 출처 직접 조회 캐시가 늦게 도착하는 경쟁 조건이다.
- 최신 조회는 현재 10ms 간격인 초당 100건 상한, 캐시된 이름의 24시간 OCID 선행 재검증, 과거 길드 명단 우선 수집 때문에 지연된다. 초당 250건을 선택하되 429에서 즉시 절반 이하로 감속하고 최신 캐릭터와 7일 기록을 우선한다.
- 2026-09-15 모바일 표시와 직접 조회 개선을 구현했다. 모바일 Recharts 영역은 20rem의 확정 높이와 0이 아닌 최소 측정값을 사용하고, 캐릭터 원본 투명 여백의 확대율은 유지한 채 세로 위치만 위로 옮겼다.
- 로컬 대표 설정을 세션 요청의 즉시 placeholder로 사용하고 직접 조회 IndexedDB 복원이 끝나기 전에는 설정이 없는 화면 대신 저장 기록 로딩 상태를 표시한다. 캐시 복원 Promise가 끝난 뒤에만 새 조회가 시작돼 오래된 캐시가 최신 결과를 덮는 경쟁도 제거했다.
- 최신 조회는 이름별 OCID와 현재 길드 ID를 재사용하고 초당 250건에서 시작한다. 429에서는 호출 간격을 즉시 두 배로 늘리고 50회 연속 성공마다 1ms씩 회복하며, Retry-After의 초·HTTP 날짜 형식을 모두 지원한다.
- 과거 기록은 단계별로 최대 1,000건씩 IndexedDB 트랜잭션에 저장하고 묶음 사이 이벤트 루프에 양보한다. 현재값 표시 시간과 전체 완료 시간, 요청 수와 429 횟수를 직접 조회 상태에 남긴다.
- 검증 결과 Vitest 92개, 공개·직접 웹 TypeScript와 프로덕션 빌드, Edge TypeScript, Playwright 고정 시안 4개가 통과했다.
- 개인 조회 Pages 배포 `d552caf5.maple-exp-personal.pages.dev`와 공개 Worker 버전 `90eac35b-f261-4282-b3e5-405dfecf8c7e`를 운영 주소에 반영했다. 두 정식 주소와 `/api/status`는 HTTP 200이고 공개 상태는 `device-direct`, 개인 조회 CSP는 정식 대시보드만 부모로 허용한다.

# 2026-09-15 공개 서비스 마감 결정

- 따라잡기 프리셋 이름 수정은 별도 창이 아니라 목록 안 편집으로 제공한다. Enter와 확인 버튼은 저장, Escape와 취소 버튼은 취소로 동작한다.
- 상단 진행 막대는 최신값만이 아니라 현재 조회·7일·30일 보충·저장까지 한 번의 전체 작업 완료율을 나타낸다.
- 사용자는 API 키의 브라우저 저장을 항상 유지하기로 했다. 키는 개인 조회 Pages의 저장소에만 남고 대시보드와 Worker로 보내지 않는 보안 경계를 유지한다.
- Google 로그인 신규 진입은 현재 숨긴다. 기존 로그인 세션의 설정·프리셋 호환은 유지하되 로그인 기능 준비 안내만 표시한다.
- 중앙 수집과 레거시 캐릭터 API는 운영 기본값에서 닫고, 명시적인 환경 설정이 있을 때만 롤백 경로로 연다.
- 직접 조회는 현재값을 먼저 최대 250ms 간격으로 화면에 반영하고, 30일 보충까지 하나의 실제 완료율로 표시한다. 대표·즐겨찾기 변경 시 진행 중인 낡은 요청은 AbortController로 취소한다.
- Worker 쓰기·OAuth뿐 아니라 세션 쿠키가 붙은 계정 API와 OAuth 콜백도 D1 접근 전에 IP 단위 요청 제한을 적용한다. 중앙 수집 API와 레거시 대시보드는 `LEGACY_ROLLBACK_ENABLED=1`일 때만 열린다.
- 개인정보·이용 안내 주소와 주간 D1 암호화 백업 작업, GitHub CI·Dependabot을 추가했다. 백업은 평문을 남기지 않고 Windows 사용자 DPAPI로 암호화하며 90일을 보존한다.
- 보안 경계 중심 표준 스캔에서 보고할 취약점은 발견되지 않았다. 하위 검토 위임이 금지된 세션이어서 공개 Worker·직접 조회·배포·백업 경계를 부모가 검토했으며, 나머지 전체 파일은 완전 보안 감사 범위에서 제외했다.
- 최종 검증은 Vitest 93개, Edge 및 공개 웹 TypeScript, PC 웹 빌드, Rust 20개 테스트, Playwright 고정 시안 4개, npm 고위험 감사 0건을 통과했다. PostgreSQL이 필요한 Rust 통합 테스트 9개는 환경 조건에 따라 제외됐다.
- 개인 조회 Pages 배포 `087b27d2.maple-exp-personal.pages.dev`와 Worker 버전 `f4e536b3-ca5a-4369-921d-17cfc07428ba`를 운영 주소에 반영했다. 전파 후 정식 주소의 강화 CSP·HSTS와 `/api/dashboard` 기본 410 차단을 확인했다.
# 2026-09-15 공개 웹 광고 결정

- 초기 수익화는 카카오 AdFit과 쿠팡 파트너스·링크프라이스 제휴 카드의 혼합 방식으로 한다.
- 광고 배치는 데스크톱 양측 160×600과 콘텐츠 하단 728×90, 모바일 콘텐츠 하단 320×100으로 확정했다.
- 모든 사용자에게 광고 쿠키 사전 동의를 받으며 거부해도 기능을 제한하지 않는다.
- AdFit 단위 ID나 승인된 제휴 링크가 없으면 해당 DOM과 레이아웃을 모두 숨긴다.
- 개인 서비스 키 Pages, 설정 대화상자, 새로고침 진행 화면, 미니 위젯과 법적 안내 화면에는 제3자 광고를 넣지 않는다.
- 광고 노출·클릭은 D1에 기록하지 않고 공급자 보고서만 사용한다.
- 광고 기반 코드를 Worker 버전 `632b069d-d597-4acc-8338-21cd69a2899c`로 배포했다. 광고 단위 ID와 제휴 링크가 아직 없어 운영 화면에는 광고·동의창·빈 공간이 나타나지 않는다.
- 광고 설정을 주입한 빌드에서도 개인 조회 Pages 번들에 AdFit 문자열이 들어가지 않는 검사를 통과했다. Vitest 97개, Edge 타입 검사, 공개·개인 프로덕션 빌드와 시각 회귀 4개가 통과했다.
- 일반 배포가 불필요한 D1 권한이나 한도에 막히지 않도록 마이그레이션을 `deploy-edge.ps1 -ApplyMigrations`의 명시적 선택으로 분리했다.

# 2026-09-15 저조건 광고 플랫폼 재검토

- 사용자는 카카오·구글 같은 메이저 플랫폼이 아니라 가입과 트래픽 조건이 낮은 플랫폼을 원했으므로 AdFit을 기본 공급자로 둔 결정은 요구와 어긋났다.
- Adsterra는 공식 안내상 최소 트래픽 조건이 없고 짧은 심사와 160×600·728×90·320×50 일반 배너를 지원해 현재 화면과 가장 잘 맞는다.
- Monetag는 최소 트래픽 조건은 없지만 팝언더·푸시 계열 비중이 높고, HilltopAds 일반 배너는 현재 레이아웃과 다른 300×250 중심이라 후순위로 둔다.
- 팝언더·푸시·인터스티셜·SmartLink는 사용자 신뢰와 오클릭 위험 때문에 사용하지 않고 일반 배너만 허용한다.
- 공급자 스크립트 도메인은 발급 때 정해지므로 광고 전용 Pages 출처에서만 실행하고, 공개 대시보드는 sandbox iframe으로 결과만 배치한다.
- 카카오 SDK와 카카오 허용 CSP를 운영 코드에서 제거하고 `maple-exp-ads.pages.dev` 광고 전용 Pages를 만들었다. 대시보드는 광고 단위의 활성 여부만 알고 실제 키·스크립트는 광고 번들에만 포함한다.
- 이전 카카오 동의를 새 공급자 동의로 오인하지 않도록 동의 저장 키를 `ad-consent-v2`로 변경했다.
- 가짜 Adsterra 키를 넣은 격리 빌드에서 광고 키가 대시보드·개인 조회 번들에는 없고 광고 번들에만 존재하는 것을 검증했다.
- Vitest 97개, Edge 타입 검사, 공개·개인·광고 프로덕션 빌드, Playwright 시각 회귀 4개가 통과했다.
- 광고 전용 Pages 배포 `c7583c6a.maple-exp-ads.pages.dev`, 개인 조회 Pages 배포 `b0e7e32a.maple-exp-personal.pages.dev`, 공개 Worker 버전 `04b46381-0fad-4c86-97fe-f9cf848c81e4`를 운영 주소에 반영했다.
- 실제 배너 키가 아직 없어 운영 화면은 광고·동의창·빈 여백을 표시하지 않는다. 서비스 소유자의 Publisher 가입·이메일 확인·사이트 승인·정산 정보 입력 뒤 코드 네 개를 운영 설정에 넣는 단계만 남았다.
# 2026-09-15 Advertica 및 고정 광고 결정

- 사용자 계정의 Advertica 가입과 이메일 인증은 완료됐다. Advertica 공식 도움말상 사이트 사전 심사는 없고 생성한 광고 태그가 실제 사이트에서 호출되면 사이트가 자동 연결된다.
- 하단 광고는 문서 최하단이 아니라 뷰포트 하단 고정형으로 변경한다. 광고가 켜진 경우에만 본문 하단 안전 여백을 만들며 모바일 하단 탐색은 제거한다.
- 모바일 탐색은 우상단 햄버거에서 여는 오른쪽 드로어로 통합한다. 광고가 조작 버튼을 가리지 않도록 드로어는 광고보다 높은 계층에서 표시한다.
- Advertica에서는 일반 배너만 사용한다. 팝업·팝언더·탭업·탭언더·리디렉트·푸시·SmartLink는 서비스 신뢰와 조작성 때문에 제외한다.
- 공급자 변경 시 이전 동의를 승계하지 않도록 동의 저장 키를 v3로 올렸다. Advertica 태그가 없거나 동의가 거부되면 고정 광고 DOM과 본문 여백이 모두 생기지 않는다.
- Vitest 전체 99개와 공개·개인·광고 프로덕션 빌드가 통과했다. 모바일 드로어는 닫힘 상태에서 DOM을 만들지 않고 열린 상태에서 기존 네 화면과 설정 도구를 제공하는 회귀 테스트를 포함한다.
- 광고 격리 Pages 배포 `a74a1899.maple-exp-ads.pages.dev`와 공개 Worker 버전 `4b951edc-ff3b-4d21-b096-072d117dfd13`을 운영 주소에 반영했다. 공개 HTML·상태 API·광고 출처가 모두 HTTP 200이며 운영 번들에 햄버거 드로어와 고정 광고 CSS가 있고 기존 모바일 하단 탐색은 없음을 재확인했다.
- Advertica 퍼블리셔의 실제 배너 생성 화면에서 모바일 제공 규격이 320×50임을 확인했다. 도움말 해석보다 현재 계정의 발급 UI를 우선해 코드와 문서를 320×50으로 복구한다.
- `guildmate_bottom_desktop` 728×90과 `guildmate_bottom_mobile` 320×50 일반 배너 태그를 발급했다. 좌우 레일은 사용자 요청의 핵심인 하단 고정 광고가 아니므로 우선 비활성 상태로 둔다.
- 하단 광고 규격 선택을 좌우 레일의 1440px 기준에서 분리했다. 모바일 메뉴와 같은 760px 이하에서만 320×50을 사용하고 그보다 넓으면 728×90을 사용하며, 화면 폭 변경에도 즉시 다시 선택한다.
- 기본 태그에서 다운로드를 유도하는 소재가 실제 노출되어 즉시 교체했다. 허용 카테고리를 브라우저 게임·패션·건강/미용·교육·쇼핑·교통·여행으로, 캠페인 유형을 CPC·CPS·체류 시간·동영상 재생으로, 배너 유형을 애니메이션과 일반 기타로 제한하고 다운로드/플레이 버튼 배너는 제외했다.
- 안전 필터를 적용해 다시 발급한 728×90·320×50 태그를 로컬 비밀 설정에 연결하고 광고 Pages와 공개 Worker를 재배포했다. 최종 Worker 버전은 `587d650f-e2cd-4cbb-86f6-bf11dd3b6cdd`이며, 광고 출처에서 새 태그 경로와 일반 Advertica 배너가 실제 로드되는 것을 확인했다.
- 태그 값은 Git에 넣지 않고 `.gitignore`에 포함된 `.env.monetization`에만 보관한다. 이 PC의 배포 스크립트는 해당 파일을 자동으로 읽으며 다른 PC에서 배포할 때는 별도로 옮겨야 한다.

# 2026-09-15 좌우 광고와 별도 동의창 제거

- 사용자는 데스크톱 좌우 광고도 실제로 노출하고 별도 광고 저장·쿠키 동의창 없이 광고가 나오도록 요청했다.
- 좌우 광고는 1440px 이상에서만 표시하고 160×600 일반 배너를 사용한다. 하단 광고는 기존 데스크톱 728×90·모바일 320×50 구성을 유지한다.
- 별도의 광고 동의 UI와 localStorage 상태는 제거하되 Advertica를 통한 광고 및 접속정보 처리 사실과 차단 방법은 개인정보 안내에 계속 고지한다.
- `guildmate_desktop_left`와 `guildmate_desktop_right` 160×600 태그를 하단 광고와 동일한 허용 카테고리·캠페인·배너 필터로 발급해 로컬 운영 설정에 연결했다.
- 광고 동의 배너, 푸터의 광고 설정 버튼, `ad-consent-v3` 조회와 광고 로드 조건을 제거했다. 태그가 구성된 환경에서는 광고가 즉시 로드되고, 태그가 없거나 로드에 실패하면 광고 영역과 여백을 숨긴다.
- Vitest 99개와 공개·개인 조회·광고 프로덕션 빌드가 통과했다. 광고 Pages 배포는 `e9bc0be3.maple-exp-ads.pages.dev`, 공개 Worker 버전은 `1434e0bc-d795-424a-8899-081a06c92d18`이다.
- 운영 주소에서 별도 동의창 없이 하단 광고가 로드되고, 광고 전용 주소에서 좌우 태그가 각각 Advertica의 160×600 소재를 불러오는 것을 확인했다. 좌우 레일은 CSS와 화면 폭 판정에 따라 1440px 이상에서만 표시된다.

# 2026-09-16 정식 도메인 결정

- 정식 공개 도메인은 `guildfollow.com`으로 확정한다. 서비스명 `길드원 따라가기`와 대응하면서 개인 식별자를 주소에서 제거하고 특정 게임의 공식 서비스로 오인될 가능성을 낮추기 위한 선택이다.
- Cloudflare Registrar 실조회에서 신규 등록과 연간 갱신 가격이 모두 US$10.46이고 현재 구매 가능한 상태임을 확인했다.
- 공개 웹은 루트 `https://guildfollow.com`을 사용한다. 개인 키 조회와 광고 코드는 기존처럼 별도 출처에 격리하되 부모 허용 origin을 새 정식 주소로 전환한다.
- 구매 완료 전에는 Worker 라우트와 운영 기본 origin을 바꾸지 않는다. 등록 직후 사용자 정의 도메인 연결, OAuth·CSP 전환, 이전 주소 호환, 광고 심사용 문서를 한 묶음으로 적용한다.
- `guildfollow.com` 등록을 완료했다. 자동 갱신이 켜져 있고 만료일은 2027-09-16이며 Cloudflare 청구 금액은 US$10.46이다.
- 공개 Worker 버전 `e7f934fb-1e43-4512-97e3-e9d07775f378`을 루트와 `www` 사용자 정의 도메인에 연결했다. 루트와 `/api/status`는 HTTP 200이며 `www`는 DNS·인증서 전파 중이다.
- 광고 Pages에는 `ads.guildfollow.com`을 연결했고 HTTP 200과 새 부모 origin CSP를 확인했다. Advertica 공식 문서에서 별도 `ads.txt` 레코드를 제시하지 않아 추측한 판매자 ID는 게시하지 않는다.
- 개인 조회 iframe을 `key.guildfollow.com`으로 즉시 바꾸면 브라우저 동일 출처 정책 때문에 기존 IndexedDB의 API 키와 경험치 기록을 읽지 못한다. 데이터 손실을 피하기 위해 공개 주소만 먼저 전환하고 직접 조회는 `maple-exp-personal.pages.dev`를 유지한다.
- 정식 주소를 포함한 Android 0.4.0 ARM64 APK를 다시 빌드했고 v2·v3 서명 검증을 통과했다. SHA-256은 `99282666DEB4B8747AB585EC00975CDCE16586E60C6B5A953EA440A78698439F`다.
- 기존 공개 주소의 대표·즐겨찾기 자동 이전은 현재 사용자가 한 명뿐이라는 사용자 판단에 따라 제거했다. 기존 주소는 전환 기간 동안 접근 가능하며 정식 주소에서는 대표캐릭터를 한 번 지정한다.
- Cloudflare 단일 리디렉션 규칙 `WWW에서 루트로 리디렉션 [템플릿]`을 301·쿼리 유지로 활성화했다. 공용 DNS 1.1.1.1과 8.8.8.8에서 `www.guildfollow.com` 해석을 확인했다.
- Google OAuth 승인된 리디렉션 URI에 `https://guildfollow.com/auth/google/callback`을 추가하고 저장 상태를 다시 확인했다. 로그인 신규 진입 UI는 현재 운영 방침대로 숨김 상태다.
- 현재 이용자가 한 명뿐이므로 공개 주소 자동 이전 코드와 Worker 내부의 중복 `www` 리디렉션을 제거했다. `www` 처리는 Cloudflare 단일 리디렉션 규칙만 기준으로 삼는다.
- 단순화한 Worker 버전 `9804feab-05dd-40df-942f-ee398c363b5d`를 배포했다. 정식 웹·API·광고 주소의 HTTP 200, 이전 Worker 주소의 독립 접근, 공용 DNS의 `www` 해석을 확인했다.
- 따라잡기 그래프에는 각 캐릭터 선 색상의 일자별 점과 레벨업 지점 아래 `UP!`을 표시한다. 개요 대표카드의 비사냥 상태만 `A00.0~3` 대기 모션을 650ms 간격으로 재생하고 사냥 중에는 기존 걷기 모션을 우선한다.
- 광고는 삭제하지 않고 빌드 마스터 스위치를 기본 비활성화하여 배너·제휴 추천·광고 여백을 모두 숨긴다.
- 관리자 화면은 `127.0.0.1:3103` 루프백 전용을 유지하면서 정식 웹·API·개인 조회 출처 상태와 레거시 보관 DB를 구분해 표시한다. 브라우저 IndexedDB의 개인 키와 기록은 관리자 화면으로 전송하지 않는다.
- Worker 버전 `4365e93c-9b57-4dd9-b4ca-dbd185e354f7`을 광고 비활성 상태로 배포했다. 정식 웹에는 광고 iframe과 광고 여백이 없고, 기존 광고 태그는 로컬 비추적 설정에 보존한다.
- WSL 관리자 바이너리를 `/home/mapledev/maple-releases/20260916-operations-v2`에 배포하고 `/home/mapledev/maple-operations-current` 링크를 통해 systemd 서비스를 재시작했다. 정식 웹·API·개인 조회 출처와 로컬 DB가 모두 정상으로 표시되는 것을 확인했다.

# 2026-09-16 광고 플랫폼 절차 문서화

- 사용자가 직접 실행할 수 있도록 카카오 AdFit과 쿠팡 파트너스의 절차를 하나의 운영 절차서로 정리한다.
- 현재 카카오 AdFit은 사전 제휴 승인 또는 인증 코드가 필요한 경로로 안내하고, 쿠팡 파트너스는 먼저 적용할 수 있는 제휴 링크 경로로 안내한다.
- 문서에는 계정 비밀번호나 API 키를 개발자에게 전달하지 않는 원칙, `guildfollow.com` 등록, 심사 자료, 설치 후 확인과 정산 준비를 포함한다.
- 최종 산출물은 Word 문서로 바탕화면에 저장하며 렌더링 결과를 전 페이지 확인한다.
- `길드원 따라가기 광고 플랫폼 신청 및 적용 절차.docx`를 바탕화면에 생성했다. 쿠팡 우선 적용, 카카오 사전 제휴 문의, 책임 구분, 자료 전달 원칙, 최종 체크리스트와 공식 링크를 포함한다.
- 번들 환경에 LibreOffice가 없어 설치된 Word를 숨김 상태로 사용해 PDF와 5개 페이지 PNG로 변환했으며 전 페이지에서 잘림, 겹침, 깨진 표가 없음을 확인했다.

# 2026-09-16 대표캐릭터 정지 모션 순서

- 공개 웹 대표캐릭터의 비사냥 대기 모션은 `A00.1 → A00.2 → A00.3 → A00.2`를 반복한다. `A00.0`은 순환에서 제외한다.
- 사냥 중 걷기 모션과 일반 목록의 단일 정지 이미지는 변경하지 않는다.
- `Avatar.test.ts`의 3개 테스트와 공개 대시보드·직접 조회·광고 빌드가 모두 통과했다.
- 공개 Worker 버전 `bd204239-926a-4428-9eb3-ba4ba8145119`을 `guildfollow.com`에 배포했고 운영 번들에 `A00.1`, `A00.2`, `A00.3` 프레임이 포함된 것을 확인했다.

# 2026-09-16 따라잡기 반응형 카드와 아바타 선명도

- 따라잡기 비교 행은 6열 최소 너비가 카드 자체 폭보다 커지는 구간에서 텍스트가 겹칠 수 있었다. 뷰포트가 아닌 `.chase-table` 실제 너비를 기준으로 재배치하는 컨테이너 쿼리로 수정한다.
- 데스크톱 아바타 왜곡은 240픽셀 API 캔버스를 작은 요소로 축소한 다음 CSS transform으로 3배 이상 다시 키우면서 `image-rendering: pixelated`를 적용한 구조가 원인이다.
- NEXON 공식 이미지 파라미터의 width와 height는 배경 캔버스 크기이며 별도 고해상도 확대 파라미터는 없다. 240픽셀 캔버스를 원본 크기로 위치시키고 대표 카드만 정확히 2배로 표시한다.
- 비교 행의 오늘·기간·평균·추월 값을 명시적 grid-area로 분리하고, 54rem 이하와 38rem 이하 카드 폭에서 각각 별도 레이아웃을 사용한다. 정렬 도구와 프리셋·그룹 텍스트에도 최소 너비와 줄바꿈 규칙을 추가했다.
- 캐릭터 이미지는 240px 캔버스를 `transform`으로 재확대하지 않고 절대 위치로 잘라 표시하며, 대표 카드만 480px로 정확히 2배 확대한다. 작은 캐릭터는 기존보다 약 25% 크게 보이면서 데스크톱의 불균일한 픽셀 열이 줄어든다.
- 전체 Vitest 102개와 공개 대시보드·직접 조회·광고 프로덕션 빌드가 통과했다.
- 공개 Worker 버전 `be467bdd-0789-492c-8326-5babf3889a05`를 배포했다. 운영 CSS에서 `container:chase-table/inline-size`, 54rem·38rem 컨테이너 쿼리, 240px·480px 아바타 규칙을 확인했다.

# 2026-09-16 대표캐릭터 정지 모션 재조정

- 사용자 확인에 따라 비사냥 대표캐릭터의 대기 순환을 `A00.0 → A00.1 → A00.2 → A00.1`로 변경한다. 사냥 중 `A02` 걷기 순서는 유지한다.
- `Avatar.test.ts`의 정지 프레임 회귀 테스트 3개와 공개 웹 프로덕션 빌드가 통과했다.
- 공개 Worker 버전 `5df52a7a-67ca-4dca-9eee-a19835231228`을 배포했으며 운영 번들에서 `A00.0 → A00.1 → A00.2 → A00.1` 배열을 확인했다.

# 2026-09-16 도트 이미지 크기·위치 재조정

- 원본 240px crop으로 바꾼 뒤 일반 아바타가 기존 합성 결과보다 약 25% 커지고, 하단 기준 crop 때문에 중심이 아래로 이동했다.
- 브라우저가 64px로 먼저 축소한 이미지를 CSS transform으로 다시 키우는 이전 흐림 구조는 복원하지 않는다. 대신 이미지 요소를 일반 192px, 대표 504px로 직접 렌더링하고 컨테이너 정중앙에 배치해 이전 체감 크기와 위치를 재현한다.
- 반응형·아바타 회귀 테스트 5개와 공개 웹 프로덕션 빌드가 통과했다.
- 공개 Worker 버전 `0deb2724-44fe-4157-b386-d4e945763c39`를 배포했으며 운영 CSS에서 일반 192px, 대표 504px, 중앙 배치 규칙을 확인했다.
- 후속 실체감 확인 결과 `object-fit:none`이 요소 상자와 무관하게 두 위치 모두 240px 원본 비트맵을 그리는 직접 원인이었다. 대표·일반 크기 차이를 실제 픽셀에도 적용하려면 정사각형 비율을 유지하는 `object-fit:contain`이 필요하다.
- `object-fit:contain`을 일반과 대표 규칙에 명시해 실제 비트맵이 각각 192px와 504px로 렌더링되게 수정했다. 회귀 테스트 5개와 프로덕션 빌드가 통과했다.
- 공개 Worker 버전 `62ac05cf-8ffc-46db-9109-4ec322fa412e`를 배포하고 운영 CSS의 두 규칙을 직접 확인했다.
- PC에서 다시 나타난 도트 왜곡은 192/240=0.8배와 504/240=2.1배의 비정수 리샘플링에 `image-rendering:pixelated`를 강제한 결과다. 고밀도 모바일에서는 덜 드러나지만 저밀도 PC에서는 일부 도트 열이 불균일하게 보인다.
- 크기·위치는 유지하고 정밀 포인터가 있는 PC 환경에서만 `image-rendering:auto`로 고품질 보간하며, 모바일은 기존 도트 렌더링을 유지한다.
- 반응형·아바타 회귀 테스트 6개와 프로덕션 빌드가 통과했다.
- 공개 Worker 버전 `b21b09be-2e34-4eb8-8493-b9d292dc78f9`를 배포했으며 운영 CSS에서 PC 전용 `image-rendering:auto`와 기존 192px·504px 크기를 확인했다.

# 2026-09-16 대표캐릭터 발 기준 정렬

- 504px 대표 이미지의 NEXON 기준점은 원본 `y=170`을 2.1배 한 약 357px 지점이다. 정중앙 정렬은 카드 높이에 따라 발이 하단 밖으로 내려가므로, 이미지 상단을 `카드 하단 - 365px`로 두어 발 아래 8px 여백을 유지한다.
- 실제 화면에서는 위 계산이 약 33px 상향되어 과도했다. 하단 기준 계산을 폐기하고 사용자가 확인한 기존 중앙 위치에서 6px만 위로 옮겨 발이 칸 하단에 거의 닿도록 조정한다.
- 회귀 테스트 6개와 프로덕션 빌드가 통과했으며 공개 Worker 버전 `fa808130-23ec-4772-8c1c-e41850e64bc2`를 배포했다.

# 2026-09-16 따라잡기 하단 레이아웃 복구

- 첨부 화면의 세로 글자 현상은 `.surface`의 좌우 `margin:auto`가 `.chase-lower` 그리드 항목의 stretch를 막아 비교 목록을 min-content 폭으로 축소한 것이 원인이다.
- `.chase-lower>.surface`에 `width:100%`, `max-width:none`, `margin:0`, `min-width:0`을 적용한다. 900px 이하에서는 자연스러운 DOM 순서인 비교 목록 다음 프리셋 순서를 유지한다.
- 반응형·아바타 회귀 테스트 7개와 프로덕션 빌드가 통과했다.
- 공개 Worker 버전 `d53c9fab-0614-41bb-9f7a-111592d10775`를 배포했으며 운영 CSS에서 열 채움, 단일 열 전환, 역순 제거 규칙을 확인했다.

# 2026-09-16 따라잡기 선택 정렬과 중간 폭 헤더

- 따라잡기 새 보고서의 길드원·즐겨찾기는 선택창에서만 레벨 내림차순, 현재 레벨 경험치 내림차순, 닉네임 오름차순으로 정렬한다. 원본 순위 배열은 변경하지 않는다.
- 전 화면 하단의 기록 안내는 제거하고 설정창의 직접 조회 안내 아래로 옮겨 접근 경로를 한 곳으로 통합한다.
- 상단 경고 아이콘은 주변 아이콘 버튼과 같은 2.65rem 클릭 영역을 사용해 수직 중앙에 맞춘다.
- 761~1100px 중간 폭에서는 한글 단어를 글자 단위로 나누지 않는다. 공백이 있는 `길드 순위`만 `길드`와 `순위` 두 줄로 표시하고 한 단어 메뉴는 한 줄로 유지한다.
- 전체 Vitest 105개와 공개 대시보드·직접 조회·광고 프로덕션 빌드가 통과했다. 과거 데이터 호환형인 `string | number` 경험치는 `BigInt` 변환 단계에서 두 형식을 모두 받도록 보정했다.
- 공개 Worker 버전 `84b7e4dc-1760-4490-aa2e-8c2fea7240ab`을 `guildfollow.com`에 배포했다. 운영 HTML이 새 번들을 가리키고 단어 보호, 경고 아이콘 크기, 설정 기록 안내 규칙이 포함된 것을 확인했다.
- 후속 첨부 화면에서 중간 폭의 네 글자 메뉴 역시 두 글자씩 묶기를 원한 것이 확인됐다. 중간 폭 전용 표기를 `개요`, `길드/순위`, `즐겨/찾기`, `따라/잡기`로 명시하고 넓은 화면에서는 원래 한 줄 표기를 유지한다.
- 전체 Vitest 105개와 세 가지 공개 웹 빌드가 통과했고 Worker 버전 `9f107996-51ff-483b-b3e6-0f8ae56245f0`을 배포했다. 운영 번들에서 중간 폭 전용 표기와 경고 아이콘 중앙 정렬 규칙을 확인했다.
- 중간 폭에서 공간을 줄이기 위해 탐색 아이콘까지 숨긴 것은 사용자 의도와 달랐다. 두 글자 단위 표기만 유지하고 모든 탐색 아이콘은 넓은 화면과 동일하게 표시한다.
- 전체 Vitest 105개와 공개 웹 빌드 후 Worker 버전 `d81b9abb-6cd1-4a6e-a053-271a91e1a5ca`를 배포했다. 운영 CSS에서 중간 폭의 전체 글자만 전환되고 탐색 SVG를 숨기는 규칙이 없는 것을 확인했다.

# 2026-09-16 개인정보 안내 광고 문구 제거

- 광고가 비활성화된 현재 운영 상태에 맞춰 개인정보 처리 안내에서 광고 코드, Advertica 쿠키, 쿠팡·링크프라이스 제휴 관련 문구를 제거한다.
- 서비스 이용 안내의 향후 광고 가능성 문구와 비활성 광고 구현은 유지하며, 실제 광고를 다시 추가할 때 적용 플랫폼과 처리 정보에 맞춘 문구를 개인정보 안내에 새로 작성한다.
- 개인정보 처리 안내의 시행일만 2026년 9월 16일로 갱신하고 서비스 이용 안내 시행일은 유지한다.
- 전체 Vitest 105개와 세 가지 공개 웹 빌드가 통과했고 Worker 버전 `579199ea-14a4-4219-9fd5-70edf76107c2`를 배포했다. 캐시 우회 운영 번들에서 개인정보 안내의 광고·제휴 문구가 제거되고 서비스 이용 안내는 유지된 것을 확인했다.

# 2026-09-16 서비스 키 붙여넣기 버튼

- 개인 조회 출처의 서비스 키 입력란 우측에 클립보드 붙여넣기 아이콘을 배치한다. 붙여넣은 키는 React 상태에만 넣고 메시지·로그·부모 iframe 통신에는 포함하지 않는다.
- 빈 클립보드와 권한 거부를 별도 안내하며, 권한 거부 시 모바일의 길게 누르기 또는 직접 붙여넣기를 사용할 수 있게 한다.
- 교차 출처 iframe에서 사용자 클릭 기반 클립보드 읽기가 동작하도록 부모 Worker의 Permissions-Policy와 iframe allow를 개인 조회 Pages 출처에만 허용한다. 개인 조회 Pages는 자기 출처의 clipboard-read만 허용한다.
- 전체 Vitest 105개, 세 가지 공개 웹 빌드와 Worker 타입 검사가 통과했다. 개인 조회 Pages 배포 `5ceb41ed`와 공개 Worker 버전 `b1db031d-ae8d-4905-932c-d15caf68e9e8`을 반영했다.
- 운영 응답에서 대시보드는 `maple-exp-personal.pages.dev`에만 clipboard-read를 위임하고 개인 조회 Pages는 self만 허용하는 것을 확인했다. 운영 번들에 붙여넣기 버튼과 키를 노출하지 않는 성공 안내가 포함된 것도 확인했다.

# 2026-09-16 따라잡기 비교 목록 균형 및 발 기준 보정

- 좁은 비교 카드에서 아바타용 고정 열이 오늘 획득량에도 재사용되어 글자가 세로로 갈라지고 수치 칸의 폭이 불균형해졌다.
- 비교 행을 프로필, 세 개의 균등한 수치판, 추월 결과로 묶어 실제 카드 너비에 따라 안정적으로 재배치한다.
- 전체 아바타 기준은 건드리지 않고 비교 목록과 새 보고서 선택 목록에만 바닥 위치 보정을 적용한다.
- 비교 행의 오늘·기간·7일 평균은 같은 폭의 수치판으로 통합했고 좁은 카드에서는 프로필, 추월 결과, 수치판 순으로 배치한다.
- 비교 목록과 선택 목록의 아바타만 중앙 기준에서 2px 위로 옮겨 발 잘림을 제거한다.
- 전체 Vitest 105개와 공개 웹 3종 프로덕션 빌드가 통과했다.
- 공개 Worker 버전 `37d0fdef-e591-4e13-9027-eb6df0d670f5`를 배포했고 운영 CSS에서 균등 수치판과 두 선택 화면의 2px 발 위치 보정을 확인했다.

# 2026-09-16 웹 아바타 발바닥 2px 공통 정렬

- NEXON 메이플스토리 외형 이미지는 2025년 8월 21일부터 300×300 캔버스, x=150, y=200으로 고정된다. y=200을 발 기준점으로 사용한다.
- 런타임 픽셀 분석 대신 이미지 아래쪽 100px 투명 영역을 CSS 자기 크기 기준 33.333333% 이동으로 상쇄해 발 기준점을 칸 하단에서 2 CSS px 위에 고정한다.
- PC 웹, 모바일 웹, 모바일 데스크톱 보기와 웹 미니 위젯을 포함한 공개 웹의 모든 `Avatar` 사용처에 같은 공식을 적용한다.
- PC 1440·1024px, 모바일 360·412px, DPR 3의 980px 모바일 데스크톱 보기에서 일반·대표·따라잡기·선택 목록·미니 위젯 아바타의 발 기준 간격이 모두 2 CSS px임을 Chromium으로 측정했다.
- Vitest 105개, Playwright 5개, 기본 앱과 공개 웹 3종 빌드가 통과했다.
- 공개 Worker 버전 `e1dbcb9e-1535-467a-a613-93873df225da`를 배포하고 운영 CSS의 하단 앵커와 운영 JS의 300×300, x=150, y=200 요청값을 확인했다.

# 2026-09-16 모바일 대표캐릭터 이미지 칸 확대

- 420px 이하에서 대표 이미지 칸이 5.8rem까지 축소되지만 504px 이미지 렌더링 크기는 유지되어 머리와 좌우 외형이 잘렸다.
- 대표 이미지 자체를 축소하지 않고 모바일 이미지 칸을 확대하며, 공통 발 기준선은 하단 2 CSS px로 유지한다.
- 760px 이하와 420px 이하 모두 대표 이미지 칸을 8rem 정사각형으로 고정해 가장 좁은 모바일에서도 다시 축소되지 않게 했다.
- Chromium에서 PC·모바일·모바일 데스크톱 보기의 발 기준 2px, 카드 가로 넘침 없음, 이미지와 정보 영역 겹침 없음을 함께 검증했다.
- Vitest 106개, Playwright 5개와 공개 웹 3종 빌드가 통과했다. 공개 Worker 버전 `cc814bea-0bcb-4dda-bcdb-674449a11270`에서 8rem 모바일 칸과 2px 하단 앵커를 확인했다.
# 2026-09-16 관리자 이용 현황 집계

- 현재 정적 자산 요청과 사용자 NEXON 직접 조회는 중앙 서버를 거치지 않아 기존 로컬 운영 화면만으로 접속자 수를 알 수 없다.
- 별도 분석·광고 스크립트를 넣지 않고 공개 웹 자체 코드가 익명 설치 식별자의 해시만 D1에 남기는 최소 집계를 사용한다.
- 활성 신호는 브라우저별 최대 1시간에 한 번으로 제한해 Worker와 D1 무료 한도를 보호한다. 최근 15분 수치는 마지막 신호 시점 기반의 근사치이며 실제 동시 접속 소켓 수가 아니다.
- 집계 조회는 기존 내부 HMAC 인증을 재사용하되 중앙 수집 롤백 기능 활성 여부와 무관하게 로컬 관리자만 접근할 수 있게 분리한다.
- `anonymous_daily_usage`는 KST 날짜와 이중 해시된 브라우저 식별자, 최초·최근 활성 시각, 세션 시작 횟수만 보관하고 90일이 지난 행은 예약 작업에서 제거한다.
- 정식 Worker의 내부 집계 비밀값은 WSL의 root 전용 환경 파일에서 표준 입력으로 전달해 콘솔과 Git에 노출하지 않았다. 중앙 수집 롤백 주소와 분리해 관리자 통계는 `https://guildfollow.com`에서 읽는다.
- 운영 D1 마이그레이션 `0005_anonymous_usage.sql`과 Worker 버전 `978fbb83-2b87-45f4-9bb5-0813444eb8bb`을 배포했다. 로컬 관리자 바이너리는 `/home/mapledev/maple-releases/20260916-operations-usage-v2`로 교체했다.
- 실제 서명 집계 API와 `127.0.0.1:3103/status`에서 오늘·7일·30일, 최근 15분·60분과 세션 수가 반환되고 화면에도 표시되는 것을 확인했다. 배포 검증용 익명 한 건이 첫 수치에 포함된다.
- Vitest 108개, Rust 20개, Playwright 5개, Edge 타입 검사와 공개 웹 3종 빌드가 통과했다.
# 2026-09-17 D1 쓰기 한도 재초과 원인

- D1 최근 24시간 사용량은 쓰기 쿼리 51회, 쓴 행 112,187개였다. 익명 이용 현황은 5행뿐이므로 새 접속 통계가 원인이 아니다.
- `ingest_batches`에는 2026-09-16 21:05~21:35 UTC에 여러 중앙 수집 배치가 실제 승인됐고 마지막 동기화는 480명 성공으로 기록됐다.
- 사용자 서비스 키 직접 조회로 전환한 뒤에도 롤백용 Linux 중앙 수집기와 레거시 Worker 쓰기 경로가 남아 있어 대량 upsert가 재개된 것이 직접 원인이다.
- 현재 D1 한도는 이미 초과했으므로 추가 쓰기를 차단하더라도 2026-09-17 00:00 UTC 재설정 전까지 쓰기 요청은 실패한다. 저장된 자료에는 영향이 없다.
- Linux `maple-exp.service`를 중지해 `inactive/disabled`로 만들고 현재 WSL 세션에서는 runtime mask도 적용했다. 재부팅 후에도 disabled 상태라 자동 시작하지 않으며, 로컬 운영 화면은 별도 `maple-exp-operations.service`로 계속 active/enabled다.
- 레거시 `maple-exp-public.guildmate.workers.dev`를 D1 바인딩이 전혀 없는 종료 전용 Worker 버전 `f4447221-b073-4c1e-8393-76c840168961`로 교체했다. 모든 경로가 410을 반환하므로 수집기가 우발적으로 다시 실행돼도 D1을 쓸 수 없다.
- 정식 웹·API·개인 조회는 HTTP 200, 로컬 운영 화면은 DB와 이용 현황 연결 정상, 레거시 수집 API는 HTTP 410임을 확인했다.
- 기존 D1 캐릭터 491명·일별 기록 13,102건, 로컬 PostgreSQL 원본 관측 13,735건과 백업 타이머를 삭제하지 않고 보존했다.
- Vitest 109개와 Edge 타입 검사가 통과했다.

# 2026-09-17 PC 전원 독립성 실증

- WSL을 완전히 종료해 로컬 서버·PostgreSQL·관리자 화면이 모두 내려간 상태를 만들었다. 이때 `guildfollow.com`, `/api/status`, `www.guildfollow.com`, `maple-exp-personal.pages.dev`가 모두 Cloudflare 응답 HTTP 200을 유지했다.
- 공개 웹과 API는 Cloudflare Workers·정적 자산·D1에 있고, 개인 조회 엔진은 Cloudflare Pages에 있다. 캐릭터 API 요청은 이용자 브라우저에서 NEXON으로 직접 전송되므로 이 PC의 전원에 의존하지 않는다.
- PC가 꺼지면 `127.0.0.1:3103` 관리자 화면, 로컬 PostgreSQL 접근과 예약 백업만 중단된다. 기존 공개 데이터와 이용자 브라우저의 IndexedDB 기록은 영향을 받지 않는다.
- 첫 WSL 재기동 시험에서 중앙 수집기가 disabled 상태인데도 다른 시작 경로로 active가 되는 문제를 발견했다. 원본 unit을 `/etc/systemd/system/maple-exp.service.disabled-20260917`로 보존하고 `/etc/systemd/system/maple-exp.service`를 `/dev/null`에 영구 마스킹했다.
- 두 번째 WSL 완전 종료·재기동 후 공개 웹·API·개인 조회는 모두 HTTP 200, 중앙 수집기는 `inactive/masked`, 관리자 화면과 백업 타이머는 `active`임을 확인했다.

# 2026-09-17 NEXON API 점검 응답 진단

- Windows 자격 증명 관리자의 사용자 키로 `엘크라우치` OCID 조회는 HTTP 200에 성공해 키 자체가 유효함을 확인했다.
- 이어진 캐릭터 기본 정보 조회는 HTTP 400과 `OPENAPI00010`을 반환했다. NEXON 공식 공지상 2026-09-17 07:00~13:00 메이플스토리 Open API 점검 시간이다.
- 현재 개인 조회 엔진이 모든 비 429 오류를 `NEXON API 응답을 확인해 주세요.`로 축약해 실제 원인을 숨기므로, 응답 본문에서 점검 코드를 안전하게 해석해 전용 안내를 표시한다.
- `OPENAPI00010`을 `NEXON Open API 점검 중입니다. 기존 기록을 표시하며 점검 종료 후 자동으로 다시 확인합니다.`로 변환하고 다른 알 수 없는 응답은 기존 일반 안내를 유지했다.
- Vitest 111개와 공개 웹 3종 빌드가 통과했다. 개인 조회 Pages 배포 `4a85680f.maple-exp-personal.pages.dev`를 반영했고 정식 주소 번들 `index-D8L7jAcT.js`에 점검 안내가 포함된 것을 확인했다.
- 설정창 내부 문구만으로는 현재 화면에서 원인을 즉시 알 수 없으므로, 같은 점검 상태를 공개 화면 콘텐츠 최상단의 전용 배너에도 표시한다.
- 공개 화면 최상단에 경고 아이콘, `NEXON Open API 점검 중` 제목과 기존 기록·자동 재확인 설명을 표시하는 반응형 배너를 추가했다.
- Vitest 111개와 공개 웹 3종 빌드가 다시 통과했다. 개인 조회 Pages 배포 `7d2fc55c.maple-exp-personal.pages.dev`와 Worker 버전 `5d75f8bf-f38c-4707-9fde-91f708468147`을 반영했고 정식 주소의 JS·CSS에서 점검 코드와 배너 문구를 확인했다.

# 2026-09-17 따라잡기 만렙 경계 및 Android 배포

- 추월은 200레벨부터의 절대 누적 경험치와 양쪽 최근 7일 평균 획득량 차이로 계산하는 기존 원칙을 유지한다.
- 대상이 현재 300레벨이면 즉시 추월 불가로 표시한다. 대상이 예측 도중 300레벨에 도달하면 그날부터 대상의 획득량은 0으로 제한한다.
- 일반 추월 예상일과 대상의 300레벨 도달일이 같은 경우에는 사용자가 선택한 규칙에 따라 추월 성공으로 판정한다. 일반 추월일이 더 늦은 경우에는 대상이 먼저 만렙에 도달하므로 추월 불가로 표시한다.
- Android 전경 화면은 `https://guildfollow.com/`을 표시하므로 공개 웹 기능이 자동 반영된다. 네이티브 직접 조회, WorkManager 백그라운드 조회와 홈 위젯은 별도 코드 경로이므로 각각 검증한다.
- 작업 시작 시 Windows 장치·ADB 목록에는 S24+가 보이지 않았다. APK 빌드 후 기존 MTP 경로를 다시 탐색하고, 연결되지 않은 경우에는 생성물과 확인된 차단 사유를 남긴다.
- 300레벨의 비정상적인 현재 경험치 값은 무시하고 경험치표 200→300 전체 합계로 절대 성장 위치를 고정했다.
- 대상의 기존 평균 획득량이 0이면 만렙 도달 제한을 적용하지 않는다. 양수이면 일반 추월일이 대상 만렙 도달일보다 늦을 때만 추월 불가로 판정하며 같은 날은 성공이다.
- Vitest 전체 117개가 통과했다.
- Android 앱 전경은 `https://guildfollow.com/`의 동일 React 화면을 표시하므로 따라잡기 만렙 계산, 모바일 메뉴, 그래프와 점검 배너가 운영 웹 배포 즉시 동일하게 반영된다.
- 웹 직접 조회 결과는 Android 브리지로 네이티브 SQLite에 가져오고 서비스 키는 Android 보안 저장소에 저장한다. 앱 종료 후 WorkManager는 네트워크 연결 조건에서 15분 주기로 네이티브 직접 조회를 실행해 홈 위젯 자료를 갱신한다. 웹 32동시성 대신 백그라운드는 5동시성을 유지해 Android 작업 제한과 메모리 사용을 보수적으로 관리한다.
- 별도 Android 화면에 중복 구현된 따라잡기 계산은 없었으며, 전경 웹·보안 키 전달·직접 조회 import·백그라운드 조회·4종 홈 위젯 연결에서 보충할 기능 누락을 발견하지 않았다.
- Vitest 117개, Rust 30개, Android 전체 Gradle 단위 테스트와 Playwright 시각 회귀 5개가 통과했다. 공개 Worker 버전 `ee76d628-e37e-4a26-b4b2-fde5023beec0`에서 두 만렙 판정 문구가 포함된 번들을 확인했다.
- ARM64 APK를 v2·v3 방식으로 서명 검증했다. 생성물은 `release/guildmate-follow-android-arm64-v0.4.0.apk`, 크기는 22,703,154바이트, SHA-256은 `99282666DEB4B8747AB585EC00975CDCE16586E60C6B5A953EA440A78698439F`다.
- Microsoft 연결 장치 경로 `C:\Users\mjs5n\CrossDevice\민제의 S24+ (3)\storage\Download`로 APK를 복사했으며, 휴대폰 쪽 파일의 크기와 SHA-256이 원본과 일치한다.

# 2026-09-17 Android 새 앱 분리 및 단풍잎 아이콘

- 새 Android 애플리케이션 ID는 개인 식별 문자열이 없는 `com.guildfollow.mobile`로 정한다. 기존 `com.mjs5ngg.guildmatefollow`과 동시에 설치되며 앱 데이터·서비스 키·위젯·WorkManager 저장소도 별도로 생성된다.
- Tauri Android 빌드는 애플리케이션 식별자와 Java/Kotlin 소스 경로가 일치해야 한다. 따라서 Kotlin 생성·커스텀 패키지, Manifest 컴포넌트, JNI 진입점과 위젯 플러그인 식별자를 모두 `com.guildfollow.mobile`로 함께 이전했다.
- 기존 앱과 신규 앱이 동시에 설치됐을 때 로그인 반환 선택창이 생기지 않도록 신규 앱은 `guildfollow://auth`와 `/auth/android/start-v2`를 사용한다. 서버는 기존 `guildmatefollow://auth`도 계속 지원한다.
- 웹 상단에서 사용 중인 단풍잎 계열 원본 `maple-leaf-v2.png`를 Android 아이콘에 사용한다. 런처 마스크에서 가장자리가 잘리지 않도록 적응형 전경 크기를 줄이고 짙은 남청색 배경을 적용한다.
- 신규 앱 버전은 `0.5.0`, Android 패키지 ID는 `com.guildfollow.mobile`, 신규 로그인 딥링크는 `guildfollow://auth`다. 기존 앱용 `guildmatefollow://auth` 반환도 서버에서 계속 지원한다.
- Vitest 118개, Edge 타입 검사, Rust 30개, Android ARM64 단위 테스트가 통과했다. 서명 APK는 v2·v3 서명이 유효하며 `aapt` 검사에서 실제 패키지 `com.guildfollow.mobile`, 시작 Activity `com.guildfollow.mobile.MainActivity`, 버전 `0.5.0`을 확인했다.
- 생성된 APK는 `release/guildmate-follow-android-arm64-v0.5.0.apk`, 크기는 22,838,602바이트, SHA-256은 `6AEA58196D8D788E67DDC6B4F999C5830ED969015085A42CAE8F8B644D0951E5`다. Microsoft 연결 장치의 `민제의 S24+ (3)\storage\Download`로 복사했으며 휴대폰 파일의 크기와 해시가 원본과 일치한다.

# 2026-09-18 관리자 대시보드 복구 및 바로가기

- 공개 사용자 화면과 달리 관리자 대시보드는 이 PC의 `127.0.0.1:3103`에서만 제공한다. 따라서 로컬 운영 서비스와 Windows 접근 경로를 함께 점검한다.
- 바로가기는 단순 URL 링크보다 서비스 미기동을 감지해 복구를 시도한 뒤 기본 브라우저로 여는 방식으로 만든다.
- 장애 원인은 `Ubuntu-24.04` WSL 배포판 전체가 중지되어 3103 포트를 듣는 프로세스가 없었던 것이다. 운영 서비스 자체는 `enabled`이고 WSL을 시작하자 `active`로 정상 기동했으며 HTTP 200을 반환했다.
- `scripts/open-operations-dashboard.ps1`는 WSL의 `maple-exp-operations.service`를 시작하고 최대 15초간 HTTP 200을 기다린 뒤 기본 브라우저를 연다. 실패하면 숨은 터미널 대신 Windows 오류 대화상자를 표시한다.
- 바탕화면에 `길드원 따라가기 관리자 대시보드.lnk`를 만들었다. WSL을 강제로 종료한 콜드 상태에서 실행해 서비스 `active/enabled`, 대시보드 HTTP 200과 7,824바이트 응답을 확인했다.

# 2026-09-18 관리자 대시보드 15초 후 응답 없음

- 증상: 바로가기로 연 대시보드가 잠시 뒤 새로고침 실패·연결 불가가 된다. journalctl에 매 부팅마다 서비스가 시작 15~25초 뒤 `Stopping`되는 기록이 반복됐다.
- 원인: WSL `[general] instanceIdleTimeout` 기본값 15000ms. 연결된 `wsl.exe`가 하나도 없으면 systemd 서비스가 떠 있어도 배포판 전체를 종료한다. 바로가기 스크립트의 `wsl.exe ... systemctl start`가 끝나는 순간부터 15초 카운트가 시작됐다. 재현 시 15초까지 200, 20초부터 연결 실패, 배포판 `Stopped`.
- 같은 배포판의 `maple-exp-ngrok`, 백업·보존 타이머도 같은 이유로 함께 멈춘다.
- 조치: 스크립트가 `MAPLE_OPERATIONS_KEEPALIVE=1 sleep infinity`를 숨은 `wsl.exe`로 한 번만 띄워 배포판을 유지한다. 전역 `.wslconfig`는 바꾸지 않았다. 실행 후 90초 동안 `/status` 200 유지, 재실행 시 keepalive 중복 없음 확인. 끄려면 `wsl --terminate Ubuntu-24.04`.
- 상시 유지: 작업 스케줄러 `MapleOperationsDashboardKeepAlive`(현재 사용자 로그온 시, 제한 권한, 실행 시간 제한 없음, 중복 실행 무시)가 `conhost --headless`로 `scripts/keep-operations-dashboard-alive.ps1`을 창 없이 실행한다. 스크립트는 keepalive 세션이 끝나면 10초 뒤 다시 띄우고, 바로가기가 띄운 세션이 있으면 그것을 기다린다. `wsl --shutdown` 직후 20초 안에 `/status` 200으로 복구되고 100초 유지되는 것을 확인했다. 해제하려면 `Unregister-ScheduledTask MapleOperationsDashboardKeepAlive` 후 `wsl --shutdown`.

# 2026-09-19 Android 내장 화면 전환(v0.6.0)과 계정 동기화 재개

- 증상: S24+에서 v0.5.0이 `net::ERR_NAME_NOT_RESOLVED` 흰 화면. Chrome은 정상. 앱 uid의 DNS가 Tailscale VPN(100.100.100.100)을 거치며 일부 질의가 `TIMEOUT(255)`로 실패했고, 원격 URL을 여는 구조라 앱 전체가 멈췄다. INTERNET 권한·데이터 세이버(전경 허용)는 원인이 아니었다.
- 조치: 같은 `src/web` React 화면과 직접 조회 엔진을 APK에 내장(`npm run web:build:app` → `web-dist/app`, 엔진은 `/direct/`). 앱 출처 `http://tauri.localhost`. API는 `https://guildfollow.com` 절대 주소 + `Authorization: Bearer`(세션은 `android_auth` prefs, `AndroidAuth.sessionToken()`), Worker는 앱 출처에만 CORS(프리플라이트 2시간 캐시). 이용 신호는 text/plain으로 보내 프리플라이트 없음. 앱 빌드는 광고 제외.
- 확인: Tailscale을 켠 채 v0.6.0이 정상 화면으로 실행, 설정·키 입력 iframe(/direct/) 로드, 상태 표시줄·내비게이션 막대 여백 정상, WorkManager 예약 유지. 로컬 Worker에서 OPTIONS 204·Bearer `/api/me`·`/api/profile`·프리셋·비허용 출처 403 확인.
- 이전 버그 2개 수정(0006): `login_attempts` CHECK에 `android-v2` 없음, 콜백이 `android`만 허용. v0.5.0 신규 앱 로그인은 원래 항상 실패했다.
- D1 쓰기 절감: 즐겨찾기를 `users.favorites_json` 한 칸으로 합쳐 설정 저장 최대 32행→1행, 같은 값이면 0행. `/api/me` 1회 조회. 클라이언트는 즐겨찾기 변경 2초 모음, 다른 기기 변경은 화면 복귀 시 5분 간격으로만 재조회.
- 주의: 출처가 바뀌어 v0.5.0의 WebView 저장소(서비스 키·IndexedDB 기록)는 이어지지 않는다. 서비스 키 1회 재입력 필요, 위젯용 네이티브 SQLite·Keystore는 유지.
- 미완료: 운영 배포(`wrangler login` 필요, 이 PC에 Cloudflare 인증이 없어 주간 D1 백업 작업도 한 번도 성공하지 못함), 실기기 Google 로그인 왕복과 PC↔앱 설정 일치 확인.

# 2026-09-19 Dependabot 업데이트 제안 보류

- `.github/dependabot.yml` 설정에 따라 GitHub에 `dependabot/...` 브랜치 7개가 있다(2026-09-15 생성, 이번 작업과 무관). 급하지 않아 합치지 않고 `checklist.md`의 보류 항목으로만 남긴다.
- 대부분 주 버전 도약(Vite 8, TypeScript 7, plugin-react 6, lucide-react 1.x)이라 그대로 합치면 빌드나 화면이 달라질 수 있다. 진행할 때는 한 건씩 올리고 `npm test`, `npm run web:build`, `npm run web:build:app`, APK 빌드와 실기기 확인을 거친다.

# 2026-09-19 새 앱 홈 위젯 기능 복원

- 증상: 새 앱(com.guildfollow.mobile) 위젯의 틀은 뜨지만 즐겨찾기가 v0.5 시절 목록에 머물고, 주간 위젯은 과거 날짜가 "자료 없음"이었으며, 갱신 시각이 05:29에서 멈춘 것처럼 보였다. 새로고침 버튼은 앱만 열었다.
- 원인: 화면이 웹 내장으로 바뀌며 예전 앱의 "앱 화면→위젯"(`get_dashboard`→`mobile_widgets::update`) 경로가 사라졌다. 남은 웹→네이티브 가져오기(`AndroidDirect.importSnapshots`)는 (1) 어떤 캐릭터의 레벨·경험치가 null이거나 숫자 해석이 안 되면 JSON 해석 오류로 전체를 버렸고, (2) 31일 초과 기록을 거부했으며, (3) 성공해도 위젯을 다시 그리지 않았다. 갱신 시각은 사냥 판정 시각(live_updated_at) 최대값이라 상태가 그대로면 멈춰 보였다. 패키지 이름 변경(JNI 이름 등)은 원인이 아니었다.
- 조치: 가져오기 로직을 `src-tauri/src/direct_import.rs`(Android·테스트 공용)로 분리해 빈 값·해석 불가 값은 해당 캐릭터·날짜만 건너뛰고, 최신 31일만 쓰고, 한 트랜잭션으로 저장한 뒤 위젯 스냅샷을 반환한다. `MainActivity.importSnapshots`가 그 스냅샷으로 즉시 위젯을 갱신한다. 실패 사유는 logcat `RustStdoutStderr`의 `GuildWidget`으로 남는다. 새로고침 버튼은 `maple-home-widget-manual-sync` 1회 작업을 실행하고 "갱신 중…"을 최대 2분 표시한다. 갱신 시각은 스냅샷 생성 시각(UTC ISO, 위젯에서 KST로 표시)이다. 위젯이 없으면 15분 작업은 조회를 건너뛰고, Android는 앱 내부 `background_loop`를 끈다. `db::open`에 busy_timeout 5초. 죽은 코드 `PublicWidgetSnapshot.kt`와 테스트 삭제.
- 확인(S24+, Tailscale 켠 상태): 앱 새로고침 직후 새 앱 위젯 3개(즐겨찾기 랭킹·대표 정사각형·주간)가 현재 즐겨찾기·최신 값·당시 시각으로 바뀌고, 주간 위젯 09.13~09.19가 모두 채워져 예전 앱 합본 위젯과 값이 일치했다. 위젯 새로고침 버튼은 앱을 열지 않고 약 8초 만에 갱신 시각을 바꿨다. Rust 34개, Vitest 135개, Android 단위 테스트 통과.
- 참고: 홈 화면 두 번째 페이지의 위젯들은 예전 앱(com.mjs5ngg.guildmatefollow)의 것이며 별도로 계속 갱신된다.

# 2026-09-19 즐겨찾기 유실 수정, 관리자 대시보드 제공량 표시, 플레이 스토어 준비

- 즐겨찾기 유실 원인: `main.tsx`의 markSynced 효과가 화면 선반영(`setQueryData`)까지 "전송 완료"로 기록해, 전송을 건너뛰고 미전송 기록도 지웠다. 서버에는 11:09 이후 저장이 없었고, 앱을 다시 시작하면 서버의 빈 목록이 기기 값을 덮었다. 기준값은 이제 세션 조회 결과(`queryFn`)에서만 정한다. 실기기에서 `/api/profile` 200과 D1 반영, 재시작 뒤 유지를 확인했다.
- 로컬 변경은 로그인 여부와 관계없이 `recordLocalChange`로 미전송 기록에 남긴다. 전송에 실패해도 화면을 되돌리지 않고, 미전송이 있을 때만 화면·온라인 복귀 때 한 번 다시 보낸다.
- 관리자 대시보드는 자동 갱신(1분)을 없앴다. 이용 현황의 30일 전체 집계 D1 읽기가 켜 둔 동안 계속 쌓였기 때문이다. Cloudflare GraphQL(Account Analytics 읽기 토큰)로 오늘(UTC) Workers 요청, D1 읽기·쓰기 행, 저장 용량을 무료 한도 대비로 표시한다. 토큰은 `scripts/set-cloudflare-analytics-token.ps1`로 WSL `/etc/maple-exp/server.env`에 저장한다. 릴리스는 `/home/mapledev/maple-releases/20260919-operations-cloudflare-usage`.
- 플레이 스토어 준비물은 `store/play/`에 둔다. 앱 쪽 변경: TV(leanback) 선언 제거, 백업·기기 이전에서 앱 데이터 제외, 개인정보 페이지에 계정 삭제 방법(`#account-deletion`) 추가. AAB와 업로드 키 스크립트는 `scripts/package-android-play.ps1`, `scripts/create-upload-key.ps1`. 새 개인 계정은 비공개 테스트에 12명이 14일 연속 참여해야 한다.
